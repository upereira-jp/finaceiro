# QUESTOES.md — registro único de questões abertas

| Campo | Valor |
|---|---|
| **Versão** | 1.0 |
| **Data** | 24/07/2026 |
| **Regra de origem** | `CLAUDE.md` regra 10 — contradição ou lacuna vira entrada aqui, nunca improviso |

> **Este arquivo é novo, e é uma consolidação — não uma fusão.**
>
> O `QUESTOES.md` original, com a faixa `Q-001 … Q-023`, **nunca chegou a nenhuma sessão** e não está no repositório. Não foi reconstruído: paráfrase de memória viraria canônico por acidente.
>
> O que está abaixo foi **recuperado por varredura do corpus** — `PRD-v2.2` §11, `SPEC-001` §10, `ADR-0002` r2, `ADR-0003`, `P7` §5, `P8`, `RESUMO-SESSAO-2` §4 e o `QUESTOES-bloco-para-fusao.md`. Onde o bloco de fusão registrava o mapeamento `Q-nnn → AUD-nn`, ele está preservado na coluna *Era*.
>
> **Lacuna conhecida:** questões do registro original que não foram citadas em nenhum documento sobrevivente estão perdidas. Não há como saber quantas. Se o arquivo original aparecer, funde por ID.
>
> **Duas séries de numeração, e elas colidem.** A auditoria numerou as suas questões `Q1 … Q12`; o registro do projeto usa `Q-001 … Q-023`. **`Q5` da auditoria (comissão) não é `Q-005` do registro.** A sessão 2 contornava isso com o sufixo "aud.", que resolve na conversa e falha em documento. Neste arquivo as questões da auditoria aparecem como `AUD-nn` e as do registro como `Q-nnn`, e a coluna *Era* preserva o mapeamento. — *Nota herdada do `QUESTOES-bloco-para-fusao.md`, que sai do repositório com esta absorção: era a única informação daquele arquivo que a consolidação de 24/07 não havia trazido.*

---

## 1. Taxonomia de severidade

O critério de saída da F0 no `PRD-v2.2` §10 exige *"`QUESTOES.md` sem bloqueio vermelho"*. **A classificação nunca foi definida** — a palavra "vermelho" aparecia uma única vez em todo o corpus, dentro do próprio critério. Definida aqui:

| Nível | Significado | Efeito |
|---|---|---|
| 🔴 **Vermelho** | Sem esta resposta, código escrito agora **será jogado fora** ou entrará em produção errado. Bloqueia a fase em que está | Impede o avanço de fase. É o que o critério de saída mede |
| 🟡 **Amarelo** | Bloqueia uma entrega específica, não a fase. Dá para avançar em paralelo e absorver depois | Não impede o avanço de fase; impede o *merge* da entrega que depende dela |
| 🟢 **Verde** | Precisa de resposta antes do go-live, mas não altera desenho. Preenchimento, parâmetro, conteúdo | Nenhum |

**Regra de classificação:** é vermelha se a resposta errada obriga a **reescrever schema, policy ou contrato de integração**. É amarela se obriga a reescrever uma tela, um relatório ou um cálculo. É verde se só muda um valor.

Uma questão sem dono nomeado é automaticamente vermelha, por não ter caminho de resolução.

---

## 2. Placar por fase

| Fase | 🔴 | 🟡 | 🟢 | Situação |
|---|--:|--:|--:|---|
| **F0** | **3** | 1 | 0 | **aberta** — ver §3 |
| **F1** | **0** | 12 | 2 | **em execução** — as duas vermelhas fecharam em 26/07: F-02 na §9, F-03 por decisão normativa. Em 27/07: `Q-SPEC001-08`, `MT-09` e `Q-CLAUDE11-01` abertas; `MT-06`, `Q-RBAC-01`, `Q-AUDIT-01` e `Q-DISTRIB-01` resolvidas (§9). **O conector foi construído em 27/07** (`src/crm/`, 19 verificações) mas **não rodou contra o CRM real**: falta `CRM_DATABASE_URL`. E a `Q-VIEWS-01` abriu **vermelha** — as views existem e não cumprem as duas condições que a `SPEC-002` pressupõe. A `SPEC-002` segue em rascunho e a fase do conector é a `Q-FASE-01`. **Atualizado em 27/07, fim da sessão 8:** o conector rodou contra o CRM real, a `Q-VIEWS-01` fechou no mesmo dia e a `Q-LOTE-01` — a única vermelha remanescente — fechou com a R13 implementada em lotes (§9). **Atualizado de novo em 27/07, depois do passo 5:** o ciclo rodou **valendo, duas vezes**, contra o CRM real — 41 clientes espelhados, 2ª passada `criados: 0, atualizados: 0`. **Os três critérios formais de saída da F1 estão cumpridos.** Mas a reconciliação da `SPEC-002` (v1.3) expôs a **`Q-ESCOPO-01`, vermelha**: o conector entrega 1 das 4 entidades que a própria spec declara. Ela não invalida os critérios do `PRD` §10 — invalida a entrega nomeada *"conector CRM read-only"*, e é o bloqueio real da F2. A `Q-FASE-01` foi **resolvida** (§9): o `PRD` vence, o conector é F1. E a medição das views do CRM feita em seguida abriu uma segunda vermelha, a **`F-01 (medida)`**: a carteira legada e os ganhos do funil são conjuntos disjuntos por `lead_id` e se cruzam por nome em 24 pessoas, o que torna o espelho de `unidade_consumidora` impossível sem decisão de operação |
| F2 | **7** | **13** | **3** | **21/08, fim do dia — A `Q-CICLO-01` FOI RESOLVIDA PELO DONO E CONSTRUÍDA NO MESMO DIA.** *"vamos com o caminho da fatura unificada"* — e a construção saiu barata por um achado medido: **o motor de split já tinha a forma exata da conta unificada**, e nenhuma linha dele mudou. Entrou a migration 34 (a coluna que liga as duas metades), a triagem pura com 42 verificações, o `INSERT ... SELECT` que copia os centavos de dentro do banco, duas rotas e o botão na tela. A `Q-CICLO-02` caiu para 🟡: (a) e (b) foram derivadas e construídas, e o que sobra é o **aval fiscal** da `Q-DOCG3-11`. Ganho de operação medido: a camada `vencimento`, **46 de 46 vazia**, deixa de bloquear quando a conta traz a data. **21/08 — A VERIFICAÇÃO PONTA A PONTA DO CICLO DO CLIENTE ABRIU CINCO, e a primeira governa as outras quatro.** As 12 fases do processo do cliente foram medidas contra produção (`PLANO-ciclo-do-cliente-2026-08-21.md`): **nenhuma está quebrada por defeito de código** — onze estão paradas por insumo que ninguém digitou, e a décima segunda por uma bifurcação de projeto que nunca foi decidida. A bifurcação é a **`Q-CICLO-01` 🔴**: existem DOIS caminhos de fatura, o contratual (`fatura`, em lote, 5 faixas, gera split) e o unificado (`registro_de_fatura_unificada`, um por vez, 7 faixas, **não gera nada**), e o documento que o cliente recebe é o único dos dois que não consegue pagar o dono da usina. Dela saem a **`Q-CICLO-02` 🔴** (como o unificado viraria `fatura`) e, do lado do insumo, a **`Q-GERACAO-USINA-01` 🔴** — duas das quatro usinas nunca tiveram geração medida, medido nas duas pontas, o que tira 12 das 46 UCs de qualquer competência. Mais duas amarelas do caminho da leitura: **`Q-CONTA-ORIG-01`** (a conta lida não é arquivada) e **`Q-CONTA-LOTE-01`** (29 contas por mês, uma por vez). **14/08, madrugada — A LEVA QUE FECHOU A TARIFA, O CADASTRO E A ECONOMIA ACUMULADA.** Fecharam **quatro**: `Q-TARIFA-FIN-01` (a tarifa virou coluna da UC, migration 30, e a aba SAIU — decisao do dono), `Q-TARIFA-CRM-01` (a granularidade e por CLIENTE, medida em 41 de 41, e o campo do CRM e `consumo_reais / consumo_kwh`), `Q-TARIFA-CRM-02` (a fonte entrou ANTES de a aba sair, na ordem que a propria questao pedia) e `Q-DOCG3-04` (a serie por UC virou `registro_de_fatura_unificada`, migration 29 — o "Voce ja economizou" imprimia o desconto DESTA fatura e dizia "Primeira fatura", para toda fatura). **E uma REABRIU, com correcao de registro: `Q-DOCG3-07`** — o numero que a fechou (*"4,02:1 contra branco"*) estava errado; remedido, e **3,08:1**, e o Orange como TEXTO da 2,69:1 e nunca tinha sido medido. As duas tintas do papel foram trocadas pelos derivados que o tema ja tinha, e a **T7** passou a medir cada uma. Entrou o **Cadastro de Fatura** (migration 28: contato do emissor, `modelo_de_fatura` como TEMPLATE e `campo_personalizado_da_fatura`) — cinco textos que a folha imprimia eram literais no codigo, e dois deles nomeavam uma cooperativa bancaria com o codigo dela. `npm test` **EXIT=0, 1908 verificacoes** (eram 1786). **14/08, noite — A ABA TARIFAS FOI MEDIDA E ELA NAO PUXA DE LUGAR NENHUM.** Abriu a **`Q-TARIFA-FIN-01` 🔴**: a tarifa vive numa tabela so (`distribuidora` + vigencia), nao ha coluna dela em UC nem em contrato, e a unica linha em producao diz **R$ 1,130000** contra os **R$ 1,185396** que a planilha mediu. Passou despercebido porque **zero faturas foram emitidas**. E a **`Q-TARIFA-CRM-01` caiu para 🟡**: a granularidade esta respondida — **por cliente**, e derivavel HOJE por `consumo_reais / consumo_kwh` com **41/41 de cobertura**, em tres valores (1,13 · 1,16 · 1,18). O que a segura agora nao e falta de dado: e o dado ser **redondo em duas casas nos 45 cards**, assinatura de digitacao e nao de medicao. **14/08, fim do dia — O EDITOR DE LAYOUT SAIU, e o custo era ZERO quando saiu.** **1.914 linhas** removidas: `layout-visual.ts`, `layout-editor.tsx` e `tests/layout-visual.ts` inteiros, duas rotas, dois modelos do Prisma, cinco enums e as duas tabelas da migration 23 (migration 27). O que autorizou foi medicao e nao estimativa: o layout posicionado tinha **cinco** tipos de bloco e a folha G3 cobria quatro — a tabela de valores entrou e fechou o quinto. **E a medicao contra producao fecha o argumento:** `layout_do_documento` **0 linhas**, `bloco_do_documento` **0**, `campo_do_documento` **15**. As duas superficies pareciam gemeas e nao eram — a de conteudo foi usada, a de posicao nunca recebeu um registro em onze dias. `campo_do_documento` **fica inteira**: o modelo fixo substituiu a POSICAO, nao o CONTEUDO. Registro em `REFERENCIA-fatura-unificada-2026-08-13.md` §16. **14/08, noite — A FOLHA 1 DO MODELO G3 ENTROU, e e a primeira coisa VISIVEL deste ciclo.** Cabecalho, bloco do cliente com CPF mascarado, barra do total, aviso *"Nao pague a conta da Equatorial"*, faixa de pagamento e rodape — composta no servidor (`src/dominio/folha-g3.ts`, 23 verificacoes), em centavos, e **padrao na tela**. Fotografada contra a SPA real: **PDF de 1 pagina**. Faltam **duas** das sete faixas (os tres cartoes e o detalhamento dos repasses), e a folha **diz isso** — `faixas_ausentes` carrega faixa, motivo e questao. Fecharam quatro na recomendacao do dono: **`Q-DOCG3-08`** (migration 26: `razao_social` e `cnpj` na identidade), **`Q-DOCG3-07`** (o cinza vale na folha, nao na tela), **`Q-DOCG3-12`** (semantica nova dos cartoes, decidida e nao construida) e **`Q-DOCG3-06`** virou so o desenho do codigo de barras — a linha passou a sair em GRUPOS. **14/08, fim do dia — a tarifa passa a vir do CARD DO CRM (decisao do dono) e o campo NAO ESTA EM VIEW NENHUMA.** Medido como `financeiro_ro`: zero colunas "tarifa" em qualquer schema visivel, zero colunas de `public` — a regra 4 fecha o caminho, e o unico destravamento e o dev do CRM expor. `PROMPT-dev-crm-rodada8-2026-08-14.md` aberto. Abriu a **`Q-TARIFA-CRM-01` 🔴** (a granularidade: por distribuidora, como hoje, ou por cliente? muda modelo, nao so fonte) e a **`Q-TARIFA-CRM-02` 🟡** (a tela nao sai antes da fonte entrar, ou a R26 derruba a composicao). **E a `Q-DOCG3-13` FECHOU no mesmo dia: o dono decidiu RECUSAR a emissao** quando o boleto nao confere, e foi construido — `BoletoNaoConfere` (409), com o adaptador falso reescrito para produzir linha estruturalmente real em vez de virar excecao dentro da regra de recusa. Medido antes: **zero boletos em producao**, entao a recusa nao tornou nenhum documento existente inimprimivel. **14/08 — os dois primeiros passos do §13.4 da referencia FORAM CONSTRUIDOS, e a suite fechou `EXIT=0`.** Os quatro **digitos verificadores da linha digitavel** entraram (`src/dominio/linha-digitavel.ts`, 30 verificacoes) e a `Q-DOCG3-06` perdeu a metade que importava — sobrou so o desenho do codigo de barras. As **tres `emReais` viraram duas**, e a que morreu era a que imprimia dinheiro no papel do cliente **sem validar** o que recebia. Abriu a `Q-DOCG3-13` 🟡 (o que fazer quando a conferencia do boleto diverge — hoje o comportamento e "so conta", por omissao) e a `Q-DOCG3-14` 🟢 (a era do fator de vencimento). **13/08, noite — a referência `g3_fatura_unificada` foi remedida no commit `36e964e`, quatro commits à frente do que o plano de 12/08 conhecia, e abriu três.** `Q-DOCG3-11` 🔴 — a decomposição do repasse da referência (não compensado + iluminação + bandeira + demais) **não bate** com a do `PRD` §5.1 (fio B + iluminação + encargos), e é a base do split. `Q-DOCG3-12` 🟡 — os três cartões da folha 1 trocaram de significado e passaram a comparar **energia contra energia**. `Q-REF-SEGREDO-01` 🔴 — o `api/ler-fatura.js` **publica metadado da `ANTHROPIC_API_KEY` e é proxy aberto**, medido no ar hoje. Em compensação a referência **responde** de onde vem a tarifa cheia (`Q-DOCG3-02`) e reformula a `Q-DOCG3-06`: o que falta não é o codificador de barras, é conferir os quatro dígitos verificadores. Tudo em `REFERENCIA-fatura-unificada-2026-08-13.md`. **13/08, tarde — o gerente do Sicoob respondeu, e a resposta foi um MANUAL DE PORTAL: fecha 3 das 7 perguntas e nenhuma das quatro que travavam.** As três do certificado saem confirmadas por **fonte primária** — A1 ICP-Brasil, e *"somente a chave pública"* em `.PEM`, `.CRT` ou `.CER` —, o que **de-arrisca a única compra pendente** (o A1) e **confirma o `ADR-0005` sem emendá-lo**. Sandbox, os três campos do contrato, a chave Pix e a autenticação do webhook **continuam sem resposta**, e a lição é de destinatário: três delas nunca foram de gerente de conta. **Nenhuma linha de código passou a ser escrevível.** Abriu a **`Q-SICOOB-AUTORIZA-01` 🟢** (+1 verde): o manual diz que *"os **responsáveis** pela conta"* autorizam o aplicativo PJ, no plural, e quantos são não está medido — conferência de dois minutos que precisa vir **antes** da compra do A1. Registro em `RESPOSTA-gerente-sicoob-2026-08-13.md`. **03/08, sessao 20 — a `Q-VIEWSCRED-01` FECHOU e foi construida no mesmo dia, e a construcao abriu uma amarela.** O conector le as **dez** views (era 8), confere o credito congelado contra o originador digitado e **nao escreve nada** — `SPEC-002` v1.6, R26 e invariante 14. Medido antes de construir: **39 das 39 UCs espelhadas tem credito vigente**, entao o eixo deixou de sair de documento. As cinco condicoes de sinal foram medidas contra o CRM real ANTES de escritas e **as cinco estao mudas hoje** — o criterio de ruido da R25 aplicado por construcao. **A causa raiz nao era o conector:** as views existiam desde 01/08 e **nada deste lado comparava o que o CRM expoe contra a nossa lista fechada**, entao `conferirRoleDeLeitura` passou a devolver `viewsNovasNoCrm` e `viewsAusentes`. Abriu a **`Q-SITUACAO-01` 🟡**: a coluna de situacao que a `RESPOSTA-rodada5` §4 pediu chegou, e ela diz que **11 das 39 UCs espelhadas estao `nao_ativado`** (7 em troca de titularidade) — recusar, marcar ou so contar sao tres caminhos, e a decisao custa menos com `contrato` em zero. **03/08, tarde — o dev do CRM respondeu, e a resposta destravou uma e abriu outra:** a **`Q-UCMUDOU-01` cai de 🔴 para 🟡** (foi correção de digitação, adjudicada pelo CRM; construído o `npm run destravar-uc`, com quatro guardas e ensaio provado nos dois sentidos — falta só o `--valendo`), e abriu a **`Q-VIEWSCRED-01` 🔴**: o CRM tem **10** views e o conector conhece **8**, e as duas que faltam são justamente as que respondem *quem vendeu* e *qual a situação*. Enquanto o conector não as ler, toda digitação sai de documento — a lição da `Q-CRMCODIGO-01`, desta vez com o conserto do nosso lado. **30/07, tarde — a revisão por vertente acrescentou a `Q-INADIMPLENCIA-01` 🟡:** o `PRD` §4.3 define inadimplência como *"visão derivada **+ registro de tratativas, acordos e histórico de contato**"* e o §9 pede a tela *"com tratativas"*. Medido em 30/07: existe a **visão derivada** (`marcarVencidas()` e o KPI `vencidas_em_aberto`) e **não existe nada do registro** — *"quem não pagou"* o sistema responde, *"o que já se fez a respeito"* não tem onde ser gravado. **29/07, noite — o caminho de cobrança deixou de existir só no backend.** A pergunta *"como gero boleto e relatório?"* foi medida em vez de respondida: as rotas existiam todas e **a SPA parava em "compor rascunho"** — emitir, boleto, baixa, repasses e comissões não tinham tela, e o número saía por `curl` ou não saía. Construídas as três telas e a exportação CSV; **35 verificações novas** (461 → 496). Abriu a **`Q-DOCFATURA-01` 🟡** com a decisão do documento (fatura nossa + faixa pagável do Sicoob) e as cinco coisas que ela não resolve — onde mora a logo, quais campos, PDF, entrega e o A1. **29/07, noite — o eixo do originador foi decidido e o insumo encolheu:** *"o originador vai ser o `vendedor_origem` até segunda ordem"*. A lista deixou de ser aberta e fechou em **três nomes**, medidos contra o CRM. Abriu a **`Q-ORIGVEND-01` 🟡** com o que a decisão **não** resolve: os três CPF/CNPJ (que nenhuma view entrega), a natureza, a confirmação do `tipo`, e **12 das 41 atribuições que existem no CRM e são invisíveis às 8 views** — só em cards do funil `Rateio` em etapa `normal`. Mapa em `ATRIBUICAO-originador-2026-07-29.md`. **29/07, fim do dia:** a `Q-ORIGINADOR-01` **fechou** na opção (a) — as UCs levam originador e nenhuma comissão foi paga ainda, então o contador em 0 é o valor certo (§9). O campo virou obrigatório na tela e a prontidão ganhou a camada que acusa contrato ativo sem originador. **Fica pendente o insumo, não a decisão:** a lista de originadores. **29/07, manhã:** abriu a `Q-ORIGINADOR-01` 🔴 — produção tem **zero** originadores e o contrato não tem caminho de edição, então os 39 que a operação vai digitar nascem sem comissão possível, em silêncio. Achada percorrendo a tela que a fila manda usar, não auditando spec. **28/07, no deploy:** abriram `Q-WEBHOOK-01` 🔴 e `Q-AGENDA-01` 🟡, as duas achadas ao ler o caminho da Sicoob ponta a ponta para o deploy — não por auditoria de spec. A primeira é o buraco no contrato de integração do webhook; a segunda é a ausência de qualquer processo periódico. **28/07, depois da PAUTA:** a `Q-021` fechou (§9) — a base de faturamento é a geração medida —, e a `RATEIO-USO-01` caiu de 🔴 para 🟡: o overbooking de crédito some por construção com essa escolha. Abriram `Q-FATCHEIA-01` 🔴 e `Q-SICOOB-01` 🔴, e as três amarelas novas vieram do que a carteira encostou | <br>**03/08:** a `Q-SPEC001-02` **fechou** — o dono decidiu que o vencimento varia por UC, e o importador existe. Mas a medicao que respondeu a pergunta abriu **duas vermelhas que ninguem tinha nomeado**: a `Q-CLIENTEDUP-01` (84 linhas de `cliente` para **41 pessoas** — preencher CPF colide com o indice unico no meio da digitacao) e a `Q-PAGADOR-01` (o boleto sairia com pagador **sem CPF e sem endereco**, e `boleto.ts:164` manda `?? ''` em vez de recusar). E a `Q-CONTAPAGAR-01` 🟢 entrou como confirmacao |
| F3 | **2** | **3** | 0 | **03/08, tarde — a `Q-EIXO-FUNCIONARIO-01` FECHOU, e o eixo não era nenhuma das duas colunas que medíamos.** É o **crédito congelado no momento do ganho**, regra da G3 desde 01/08. Consequência medida no dia: o mapa de 30/07 está errado em **12 das 41 UCs**, o Out Sales sai de **24,2% para 4,6%** e **5.858,8 kWh/mês — 19,6% da carteira** — trocam de dono. Mapa refeito: `ATRIBUICAO-originador-2026-08-03.md`. **E a correção do próprio dev abriu duas:** a **`Q-PARCERIA-01` 🔴** — o crédito traz vendedor **e** parceiro na mesma venda, `contrato` guarda um só, e a escolha muda a 2ª parcela de 25% para **zero** — e a **`Q-COMISPCT-01` 🟡**, o `comissao_pct` do CRM contra a nossa `regra_comissao`, que concordam hoje e não têm dono para quando divergirem. **30/07, tarde:** abriu a **`Q-EIXO-FUNCIONARIO-01` 🔴** — a descrição do dono nomeou os beneficiários da comissão como *"responsáveis dos leads"*, e o eixo **decidido** em 29/07 é o `vendedor_origem`. São colunas diferentes do CRM, medidas lado a lado, que **divergem em 43 dos 80 ganhos**. Vermelha por prazo, não por dúvida: a R20-b congela o tipo no rascunho e os 39 contratos estão prestes a ser digitados. **29/07, fim do dia:** a `Q-PRONTIDAO-COMIS-01` **fechou** (§9) — a décima camada, `originador_do_contrato`, separa os dois estados que eram o mesmo `?`. **29/07, manhã:** abriu a `Q-PRONTIDAO-COMIS-01` 🟡 — a camada `regra_de_comissao` lê `nao_medido` tanto sem contratos quanto com 39 contratos sem originador, e as duas coisas aparecem como o mesmo `?`. **28/07:** as quatro questões fiscais que bloqueavam a F3 foram respondidas (§9). Sobra a `Q-022` — como o contrato chega ao originador |
| **F4** | **0** | 1 | 1 | **Esta linha não existia até 30/07 — e a ausência é o achado.** Não é que a F4 estivesse atrasada: o **módulo corporativo do `PRD` §4.4 nunca entrou neste registro**. Medido em 30/07 contra produção (`REVISAO-VERTENTES-2026-07-30.md`): **0 de 13 entidades** (`conta_pagar`, `conta_receber`, `pagamento`, `conta_bancaria`, `categoria`, `centro_custo`, `movimento_caixa`, `extrato_importado`, `conciliacao`, `cartao_credito`, `fatura_cartao`, `fornecedor`, `compra`), **0 de 85 rotas**, **0 de 12 telas**, e nenhuma das dez camadas da tela Pendências mede nada da empresa. A vermelha (`Q-PAGAMENTO-01`) **não é "falta construir a F4"** — é uma decisão com janela: enquanto `split_item` tiver 0 linhas, onde mora o estado de pagamento ainda é escolha; depois da primeira liquidação vira migration em tabela com dinheiro gravado. Ver §7 | <br>**03/08: a F4 deixou de ser zero.** A `Q-PAGAMENTO-01` **fechou** com `conta_pagar`, `pagamento`, `categoria` e `centro_custo` — **4 das 13** entidades do `PRD` §4.4, 12 rotas e 1 tela. As quatro sao as que QUITAM; as nove que faltam sao fluxo de caixa, conciliacao e cartao, e seguem na `Q-CORPORATIVO-01`. **Nao e a F4 aberta** — e a fatia dela que tinha prazo, feita enquanto `split_item` ainda tinha 0 linhas |
| F5 | 0 | 1 | 0 | compras, fornecedores, cartão de crédito e folha importada — **não iniciada**, sem entidade em produção e sem questão própria além da `Q-CORPORATIVO-01`. Também sem linha neste placar até 30/07 |
| F6 | 0 | 1 | 1 | — |

---

## 3. F0 — o que falta para fechar

Entregas da F0 conforme `PRD-v2.2` §10:

| Entrega | Situação |
|---|---|
| Auditoria do CRM | ✅ concluída (P0–P6), reverificada em 24/07 (P8), sem deriva |
| Spike Prisma + RLS | ✅ **fechado.** `ADR-0003` **r2**, 21 + 12 testes. Só o PgBouncer segue sem cobertura, e está fora do escopo da F1 |
| Decisões de comissão | ✅ **fechadas em 24/07** — eixo único, PADRAO 50%, tabela desenhada na `SPEC-001` §3.3 |
| Decisões fiscais | 🔴 **risco aceito, não resolvido.** Rebaixadas de bloqueio de F0 para bloqueio de F2/F3. A reunião com o contador não ocorreu |
| Provisionamento de infra | ✅ **fechado em 24/07** — `ADR-0004` |

| ID | Nível | Pergunta | Quem responde |
|---|:--:|---|---|
| **Q-011** | 🔴 | Retenção sobre comissão a PF — incide, e como? | contador |
| **Q-002 C** | 🔴 | Escrituração sem emissão de documento fiscal | contador |
| **Q-003 C** | 🔴 | Crédito de IBS/CBS na operação | contador |
| **AUD-05b** | 🟡 | De onde vem o `30%` — 3 leads apenas. Sem categoria correspondente na tabela de taxas | Vinicius |
| **Item 10** | 🔴 | Comissão a sócia é despesa dedutível ou distribuição de lucro? Renata concentra 39 de 48 ganhos (83%) | contador |

**Quatro vermelhas restantes, todas do contador.** As de engenharia e as de decisão fecharam em 24–25/07.

**A assimetria do fechamento da F0, registrada e não escondida:** a comissão foi **resolvida**; o fiscal foi **aceito como risco**. As quatro questões acima não foram respondidas — foram rebaixadas para bloqueio de F2/F3. A F1 não toca nenhuma delas e corre inteira. Mas no dia em que a F2 começar, as quatro voltam a ser vermelhas e a reunião vira pré-requisito outra vez, agora sem folga de calendário. **Marcar o contador durante a F1 é o caminho de menor dor.**

---

## 4. F1 — fundação

| ID | Nível | Pergunta | Quem |
|---|:--:|---|---|
| ~~**F-02**~~ | — | ~~Quais funis contam como conversão final?~~ **Resolvida — ver §9.** Estava contada em duplicidade no placar até 26/07 |
| ~~**F-03**~~ | — | ~~Quem mantém `INADIMPLENTES`?~~ **Resolvida por decisão normativa — ver §9** |
| MT-01 | 🟡 | Usuário pode pertencer a mais de um tenant? Custo de errar: uma constraint | Vinicius |
| MT-07 | 🟡 | Quem informa o `crm_tenant_id` ao ativar um conector, e como se valida | Vinicius |
| AUD-09 | 🟡 | CPF/CNPJ: CRM exige ou o financeiro coleta? | Vinicius |
| Q-SPEC001-03 | 🟢 | Endereço da UC: coleta local obrigatória ou opcional? | Vinicius |
| MT-09 | 🟡 | **O Supabase habilita RLS sozinho, e habilita do jeito que a regra 3 chama de falha.** O projeto traz o event trigger `ensure_rls` (`ddl_command_end`) e a funcao `SECURITY DEFINER` `public.rls_auto_enable`, que roda `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` em toda tabela nova de `public`. Ela **nao cria policy e nao poe FORCE** — exatamente o estado das 82 de 151 tabelas do CRM, que nega tudo em silencio. Consequencia: uma tabela futura que esqueca a RLS nao fica *sem* RLS, fica com **RLS sem policy**, por default da plataforma e nao por desenho nosso. Entrou na lista branca do invariante 19 em 27/07 porque nao controlamos nem podemos remover a funcao. **ATUALIZADO em 27/07, e a atualizacao muda a pergunta: isto deixou de ser risco previsto e virou fato medido no nosso schema** — ver `Q-DISTRIB-01` na §9. O event trigger habilitou RLS em `distribuidora`, sem policy e sem FORCE, e a role de runtime passou a ler **zero** linhas dela. A mitigacao que esta entrada declarava — *"coberto pelo CAT-3"* — **nao existia**: o CAT-3 filtra por `ter tenant_id`, e o comentario dele nomeava `distribuidora` como exemplo do que ignorar. Coberto agora pelo **CAT-8**, que acusa qualquer tabela com RLS e zero policies e roda tambem contra producao. **O que resta decidir e menor, mas segue aberto:** tratar no provisionamento (`ADR-0004`) — desabilitar ou neutralizar o event trigger para o nosso schema —, ou conviver com ele e confiar no CAT-8 como rede. Note que o CAT-8 e *deteccao*, nao prevencao: ele acusa depois que a tabela nasceu errada | Vinicius |
| Q-SPEC001-08 | — | **RESOLVIDA em 30/07/2026.** As duas linhas da §9 que diziam "as **nove** FKs" passaram a dizer **dez**, batendo com a tabela nominal da §3.4. As mencoes a "nove" que sobraram — o rodape de revisao da v2.2 e a nota sobre o `ADR-0003` — sao **registro datado e ficam intactas**, pela decisao do `PATCH-citacoes-2026-07-24.md`. A §3.4 ganhou uma nota nova: a lista e **da F1**, e o banco hoje tem **26** FKs compostas (medido em 30/07 contra as 21 migrations). Ela **nao** foi reaberta de proposito — recontar a cada migration a transformaria num numero que envelhece, que e o defeito que o `CAT-7` foi escrito para nao ter. Corpo original abaixo: **A `SPEC-001` se contradiz na contagem de FKs compostas.** A §3.4 linha 320 registra que *"**A décima** entrou na v2.9 com `regra_repasse`"*, mas duas linhas adiante o documento continua na contagem antiga: linha 536 — *"As **nove** FKs da §3.4 são compostas, e cada uma rejeita a referência cross-tenant com `23503`"* — e linha 565 — *"`test_fk_composta_rejeita_cross_tenant` \ | §3.4 · Inv. 2 — as **nove** FKs, uma a uma"*. Medido em 27/07 contra o banco com as 12 migrations: são **dez**, e dez é o correto. A décima é `regra_repasse_usina_fk`. Consequência pela regra 8: o checklist e a tabela de testes cobrem nove, então a décima é invariante sem teste nomeado. **Correção é do autor da SPEC — não editar** | Vinicius |
| Q-CLAUDE11-01 | 🟡 | **A opcao (b) foi EXECUTADA em 30/07/2026; (a) e (c) seguem abertas e sao do autor do `CLAUDE.md`.** O furo continua real — o `findUnique` por chave parcial compila —, mas deixou de ser invisivel, e agora ha invariante dos **dois** lados. **Banco:** `CAT-9` em `tests/catalogo.sql` exige que todo indice unico parcial tenha predicado `IS NOT NULL`. O criterio nao e "ser parcial" e sim o PREDICADO: com `IS NOT NULL`, para todo valor nao-nulo existe no maximo uma linha e `findUnique` nao tem como devolver arbitraria; com `WHERE status = 'ativo'` — o `contrato_ativo_unico_por_uc` que originou a regra — varias linhas compartilham a chave. Os tres parciais de hoje sao todos `IS NOT NULL`. **Codigo:** `tests/regra11.ts` (8 verificacoes) proibe navegar por chave parcial, e a lista sai do proprio `schema.prisma` — lista escrita a mao envelheceria em silencio. O criterio e o PAR **(modelo, chave)**, e isso importa: `tenant_id_documento` e parcial em `cliente` e **cheia** em `originador` e `dono_usina`, que a usam em `findUnique` legitimamente. Um teste por nome de chave acusaria os dois e treinaria o time a ignora-lo. Provado nos dois sentidos: a violacao plantada acusa e some ao ser removida. **Corrigido junto:** o comentario de `src/repos/cliente.ts` repetia a protecao inexistente e agora diz o que foi medido. **O que continua a decidir:** (a) reescrever o mecanismo declarado na regra 11 — **nao editei o `CLAUDE.md`** — e (c) se `partialIndexes` deve continuar ligado; a verificacao `R3b` cai no dia em que for desligado, e e o sinal de que a decisao foi aplicada. Corpo original abaixo:  **O mecanismo que a regra 11 declara como automático deixou de valer, e ninguém foi avisado porque nada falhou.** A regra 11 afirma: *"O Prisma já exclui parcial das chaves de `findUnique` — verificado no DMMF"*, e `src/repos/cliente.ts` repete a afirmação em comentário. Medido em 27/07 nos tipos gerados: **é falso hoje**. Com `previewFeatures = ["partialIndexes"]` ligado no generator (`prisma/schema.prisma` linha 4), o `db pull` passou a emitir `where: raw(...)` nos `@@unique` parciais, e as chaves **aparecem** em `findUnique`. Conferido: `clienteWhereUniqueInput` expõe `tenant_id_documento` (índice `cliente_documento_unico`, parcial `WHERE documento IS NOT NULL`) e `unidade_consumidoraWhereUniqueInput` expõe `tenant_id_crm_usina_cliente_id` (`uc_crm_unico`, parcial). **Consequência:** o `findUnique` por chave parcial agora *compila*, e para um predicado que não seja `IS NOT NULL` — o caso do `contrato_ativo_unico_por_uc` que originou a regra — várias linhas podem compartilhar a chave e o Prisma devolve uma arbitrária. É exatamente o modo de falha dos R$ 111,00 contra R$ 789,00, com a proteção que a regra supunha existir removida. **O `CAT-1` não cobre isto:** ele acusa índice parcial que cobre exatamente as colunas de uma **FK**, que é outro caso. Não há invariante para "parcial virou chave de `findUnique`". Os repositórios escritos em 27/07 usam `findFirst` com predicado explícito e não dependem da proteção automática — o furo é para quem escrever o próximo. **Decidir:** (a) reescrever o mecanismo declarado na regra 11 e a fonte do comentário em `cliente.ts`; (b) acrescentar `CAT-8` que compare os índices parciais do catálogo com as chaves do DMMF; (c) avaliar se `partialIndexes` deve continuar ligado. **Alteração da regra é do autor do `CLAUDE.md` — não editar** | Vinicius |
| **Q-PRISMA11B-01** | 🟢 | **O `db pull` produziu um schema que o `prisma generate` recusa — e a regra 11 acertou onde consertar.** Medido em 30/07, aplicando a migration 19 em produção: `logo_de_cobranca` tem `tenant_id UNIQUE` (uma logo por tenant) e a FK **composta** `(tenant_id, identidade_id)` que o `CAT-7` exige. O `db pull` inferiu daí uma relação **to-one** — inferência correta — e o `generate` falhou com **`P1012`**: *"a one-to-one relation must use unique fields on the defining side… add `@@unique([tenant_id, identidade_id])`"*. Ou seja, o Prisma 7.9 exige que os campos **da relação** sejam únicos e não aceita que a unicidade venha de um subconjunto deles, mesmo quando o banco garante **mais** do que ele pede: `tenant_id` único implica `(tenant_id, identidade_id)` único. **Dois fatos operacionais que valem registro, porque nenhum estava escrito:** (a) o `db pull` **valida o `schema.prisma` existente antes de introspectar**, então com o arquivo inválido ele não roda — a saída é restaurar o último arquivo válido e puxar de novo, e não editar o inválido; (b) o `generate` falhar deixa o client **anterior** intacto em `src/generated/`, e o `tsc --noEmit` passa em cima dele: **typecheck verde não prova que o client corresponde ao schema.** **Consertado onde a regra 11 manda — no banco:** migration 20 acrescenta `UNIQUE (tenant_id, identidade_id)`, redundante para o Postgres e necessária para o gerador. `@@unique` escrito à mão no arquivo funcionaria hoje e **desapareceria no próximo `db pull`**, com o sintoma aparecendo num dia em que ninguém lembraria por quê — é o mesmo argumento que a regra 11 fez sobre o índice parcial, numa direção nova. Há precedente para a redundância: a regra 2 manda `UNIQUE (tenant_id, id)` em toda tabela referenciada, redundante com a PK, para a garantia ser **expressável** pela FK composta. **Verde e não amarela porque está resolvida e o mecanismo ficou no banco**, não em memória: `db pull` + `generate` reproduzem, e as 20 migrations mais o catálogo 8/8 rodaram contra produção depois | — |



| Q-PROV-01 | 🟡 | **O `RESUMO-SESSAO-7` afirma que "daí em diante o próprio app cria tenant e vínculo, com auditoria" — e esse caminho não existe.** Medido em 27/07: não há repositório nem rota que crie `tenant`, e o caminho não é trivial de abrir. A policy `tenant_por_tier` exige `app.current_tier() = 'plataforma_admin'` no `WITH CHECK`, e o único emissor de contexto com tier é `abrirComoPlataforma()`, que recebe um `tenantId` **já existente** para atravessar. Criar o **primeiro** tenant não tem tenant onde se apoiar — é o mesmo ovo-e-galinha do primeiro `plataforma_admin`. **Não foi improvisado:** inventar contexto para uma linha que ainda não existe abriria uma segunda porta de emissão de contexto, e a `SPEC-001` §3.2 existe para não haver uma segunda. O provisionamento ficou em `scripts/provisionar-tenant.sql`, na mesma faixa do bootstrap e da role de runtime, com `modo=ensaio\|valendo` e três guardas verificadas. **Decidir:** (a) aceitar que criar tenant é provisionamento por `psql`, e corrigir a frase do `RESUMO-SESSAO-7`; (b) abrir um caminho de aplicação — exigiria repositório, rota e uma forma de emitir contexto de plataforma **sem** tenant alvo, que é desenho novo e provavelmente ADR; (c) permitir só a partir do segundo tenant, com o primeiro nascendo por script. A (c) é o meio-termo honesto: o primeiro de qualquer coisa é sempre provisionamento | Vinicius |

| Q-VALOR-01 | 🟡 | **A `SPEC-002` R14 afirma que "os funis de venda têm zero ganhos sem valor". Medido no primeiro ciclo real, 27/07: é falso.** Contagem na origem: `Vendas - Assinatura` 40 ganhos, **0** com `valor_venda`, **0** com `valor_posicao`, **40** com `consumo_kwh`; `Vendas - Integração` 1 ganho com valor; `Parceiros` 7 sem nada. O conector recusou os 40 — fez o certo, recusar em vez de adivinhar — e a **contagem de recusas foi o que trouxe o problema à tona**, que é exatamente o papel da invariante 8. **Leitura do negócio, e já estava na própria spec:** assinatura de crédito de energia não tem "valor da venda", tem consumo mensal, e a R10 manda faturar por `consumo_kwh × tarifa` tratando valor do CRM como semente. Exigir `valor_venda` num funil de assinatura é cobrar o campo errado. **Decisão do dono, 27/07:** `consumo_kwh` conta como valor; a recusa passa a exigir ausência dos três. Implementado, com testes `N5b`/`N5c` nos dois sentidos. **Fica pendente:** (a) atualizar a redação de R9 e R14 na `SPEC-002` — texto de spec é do autor; (b) a R10 também manda gravar `consumo_referencia_centavos` como semente a partir de `consumo_reais`, e **não foi implementado** porque converter reais em centavos exige decidir arredondamento e a R23 proíbe `round()` em intermediário | Vinicius |
| F-01 (medida) | 🔴 | **A carteira legada e os ganhos do funil são conjuntos DISJUNTOS por `lead_id`, e se cruzam por nome em 24 pessoas.** A `F-01` existe no `PRD-v2.2` §11 como pergunta de planejamento — *"a carteira legada (36 clientes) é migrada para o funil de clientes ativos antes do go-live?"* — e em 27/07 ela ganhou número, medido contra o CRM real: dos **36** `lead_id` de `financeiro.rateio_creditos`, **zero** aparecem em `financeiro.vendas_ganhas`; por nome normalizado, os dois conjuntos coincidem em **42 pares / 24 pessoas distintas**. Ou seja: a mesma pessoa é **dois leads** no CRM, um na carteira de rateio e outro no funil de vendas. **Consequência direta e não óbvia:** o espelho de `unidade_consumidora` (`SPEC-002` §2) é impossível sem resolver isto — a UC pendura em `cliente`, e criar cliente a partir do rateio produziria 36 linhas das quais 24 duplicam gente já espelhada. O dedup da R4 **não pega**, porque ele é por `crm_lead_id` e os ids são genuinamente diferentes. **Decidir:** (a) a operação unifica os leads no CRM antes do go-live — é a intenção original da F-01, e resolve na origem; (b) o financeiro passa a casar por documento (CPF/CNPJ), o que exige a `AUD-09` respondida antes e ainda não resolve quem não tem documento; (c) espelhar UC sem cliente, com vínculo posterior manual — cria trabalho permanente e contraria o desenho. **DECIDIDA em 28/07 pelo dono: espelho fiel** (a opção que não estava na lista original e que a resposta do dev tornou possível). Os 36 leads de rateio são espelhados pelo próprio `crm_lead_id`, como já se faz com `vendas_ganhas`. **A duplicidade passa a ser fiel:** ela existe no CRM, e o conector espelha em vez de consertar a origem. Quando o dedup do CRM — que o dev confirmou estar em desenvolvimento — mesclar os pares, `lead_merges` avisa e `fundirEspelho` consolida sozinho, com a maquinaria já testada nos `N32`–`N34`. **Não há risco de faturar duas vezes:** a cobrança segue UC e contrato, e só o lead de rateio tem UC. Medido em produção no mesmo dia: **35 clientes criados** (36 menos a UC duplicada), 76 espelhados no total, e a segunda passada saiu `criados: 0, atualizados: 0`. **A (a) da lista original — operação unifica os leads — segue sendo o desfecho desejável**, mas deixou de ser pré-requisito.
~~**A (a) é a única que não move o problema de lugar.**~~
**Precisão de 27/07, do dono, e ela muda o tom sem mudar o bloqueio:** a ausência de `vendas_ganhas` para os 36 do rateio **não é defeito nem dado faltando** — a carteira legada nunca passou pelo funil de vendas como ganho, e era esperado que fosse assim. O que existe é um **vínculo que ainda não foi criado**, e o dono nomeou onde ele vai nascer: *"quando dar ganho em rateio vai para clientes ativos"*. Ou seja, o caminho de ligação não é casar rateio com `vendas_ganhas` — é o funil `Clientes ativos`, que estava vazio por configuração do CRM em 27/07 e **não está mais: 29 cards medidos em 29/07** (ver **`Q-ATIVOS-01`**). **O bloqueio do espelho de `unidade_consumidora` continua de pé** — as 24 pessoas duplicadas entre os conjuntos seguem reais —, mas a saída provável deixou de ser "operação unifica leads à mão" e passou a ser "corrigir a automação do Rateio e ler de `Clientes ativos`" | Vinicius + operação |
| Q-ATIVOS-01 | 🟡 | **A `ATIVO-01` foi encerrada por um sintoma, e o dono corrigiu a leitura em 27/07.** Em 26/07 a `ATIVO-01` fechou "por fato": o funil `Clientes ativos - Assinatura` estava **vazio**, logo a fonte de estado ativo trocaria para `financeiro.rateio_clientes`. **A medição estava certa e a conclusão, não.** O funil está vazio porque a etapa-fonte (`Rateio Concluido`) tem `stage_type='normal'` e **não dispara a automação** — isso é *configuração do CRM*, não desenho. **Esclarecimento do dono, 27/07:** *"quando dar ganho em rateio vai para clientes ativos"* — o funil `Clientes ativos` **é** o destino projetado do ganho de rateio, e é de lá que o financeiro deve puxar cliente ativo. Encerrar a questão pelo estado vazio foi tomar a ausência de dado como resposta de desenho. **A consequência é imediata e está no código:** a §4.3 classifica `Clientes ativos - Assinatura` como *cópia derivada* — "não desativa e não conta" —, com base na resposta do dev em 26/07 de que aqueles cards são cópias apagadas rotineiramente pelo sync da G3. Se o funil passa a ser **fonte de verdade do cliente ativo**, esse tratamento fica invertido: o conector ignoraria exatamente a população que deveria ler. **As duas coisas podem ser verdadeiras ao mesmo tempo** — população mantida por sync *e* fonte de estado — e é justamente por isso que não dá para decidir por dedução. **Nada foi alterado no código:** o funil está vazio hoje, então mudar o comportamento agora seria implementar contra um estado futuro e desfazer, sem teste possível, a classificação que a medição do dev justificou. **Decidir, e a primeira parte é do dev do CRM:** (a) corrigir o `stage_type` da etapa de ganho do Rateio para a automação disparar, e então medir o funil populado antes de mexer na §4.3; (b) definir se `Clientes ativos` substitui ou complementa `rateio_clientes` como fonte — se complementa, qual vence em divergência; (c) só depois, reescrever a classificação da §4.3 e o `FUNIL_COPIA_DERIVADA`. **Reclassifica a `ATIVO-01`, que está na §9 como resolvida — a resolução fica no registro, datada, com este ponteiro.**

> **MEDIDO EM 29/07, e muda o estado de fato desta questão: o funil não está mais vazio.** Snapshot do CRM às 17:31Z — `Clientes ativos - Assinatura` / **ATIVOS: 29 cards, 29 leads únicos, 0 parados >30d**. Em 27/07 a contagem era zero, e é sobre esse zero que os dois parágrafos acima estão escritos.
>
> **A condição que a própria questão nomeou como pré-requisito aconteceu.** A alternativa (a) pedia *"corrigir o `stage_type` da etapa de ganho do Rateio para a automação disparar, e então **medir o funil populado** antes de mexer na §4.3"* — o funil está populado. O argumento de *"nada foi alterado no código porque mudar agora seria implementar contra um estado futuro"* **caducou**: o estado deixou de ser futuro.
>
> **O que isto NÃO resolve, e por isso a questão continua aberta:** medir 29 cards não diz se eles são *fonte de verdade do cliente ativo* ou a *cópia derivada* que o dev do CRM descreveu em 26/07 como apagada rotineiramente pelo sync — e a §4.3 do conector trata essa população como cópia, isto é, **ignora exatamente esses 29**. As duas leituras seguem possíveis e a escolha entre elas continua sendo a decisão (b), com dono. O que mudou é que agora ela pode ser decidida **contra dado**, e não contra ausência de dado.
>
> Registrado em 29/07 pela regra 10, ao medir o CRM por outro motivo — a `Q-ORIGINADOR-01`. Nada foi alterado no código do conector.
**RESPONDIDA na metade técnica em 28/07, e o dev corrigiu a própria premissa dele:** o funil Rateio tem **duas** etapas coexistindo desde 29/06 — `Rateio Concluído` (`normal`, **28 cards**, posição 7) e `Desconto Ativo` (`won`, **0 cards**, posição 8). **A automação "Rateio → Clientes Ativos" está HABILITADA** e dispara na entrada em `Desconto Ativo`. O histórico de eventos registra **zero entradas em `Desconto Ativo` na vida do funil**. Ou seja: ninguém parou a automação — **ela está armada e nunca disparou**, porque a operação trata `Rateio Concluído` como etapa terminal e nunca move o card adiante. O desenho (etapa `won` separada + automação ligada apontando para ela) **diz que a expectativa era avançar**; a prática nunca acompanhou. Classificação do dev, e é a honesta: **drift operacional/de processo, não decisão.** Isso confirma o esclarecimento do dono e fecha a parte que era do CRM. **O que resta é decisão do dono, e são duas:** (a) a operação passa a mover os cards para `Desconto Ativo`, ou (b) a automação passa a escutar `Rateio Concluído`. Enquanto nenhuma acontecer, o funil segue vazio e a §4.3 **não deve ser mexida** — medir o funil populado vem antes.
**DECIDIDA em 28/07 pelo dono: não mover os cards.** *"No momento não há nenhum card com desconto ativo, então não moverei."* **E a decisão é sustentável porque o espelho de UC mudou o problema de lugar:** desde 28/07 o financeiro obtém estado de cliente ativo de `financeiro.rateio_clientes` × `rateio_creditos`, e `cliente_estado_crm.tem_rateio_ativo` é preenchido por esse caminho (R24) — **35 clientes marcados em produção**. O funil `Clientes ativos` deixou de ser necessário como fonte. **Reclassificada de 🔴 para 🟡:** não bloqueia mais nada. Fica registrada porque, no dia em que a operação passar a mover os cards, a §4.3 trata `Clientes ativos - Assinatura` como *cópia derivada* — "não desativa e não conta" — e esse tratamento passa a estar **invertido**. O aviso está na própria constante `FUNIL_COPIA_DERIVADA` em `sincronizacao.ts` | Vinicius |
| UC-DUP-01 | 🟡 | **A UC repetida é provável erro de digitação, e o dev conferiu o modelo antes de opinar.** `000041446801282` aparece em dois contratos: `G3-0141` (Fernando Albino — Carteira Lourival) e `G3-0312` (Nilton Rodrigues/Mineiro), cada um com **exatamente um** contrato, **mesmo percentual (5,5%)**, criados com **39 minutos de diferença em 14/07** — o dia em que **28 dos 36** contratos foram digitados na carga manual da carteira, pelo **mesmo usuário**. O dev descartou as duas hipóteses alternativas **por leitura do modelo, não por opinião**: não é troca de titularidade (não há status nem data-fim em `usina_clientes`; as duas linhas estão vivas) e não é "dois beneficiários por UC" (nada no modelo nem na validação suporta isso; a unicidade lá é `(tenant_id, lead_id)`, e a UC não é única de propósito nenhum). **Leitura mais provável: um dos dois números de UC foi digitado errado.** Exige conferência contra o rateio oficial da Equatorial. **O dev confirmou explicitamente que o nosso modelo de UC única por tenant está certo** — então isto não muda o schema; muda o dado, do lado do CRM. <br>**MEDIDO EM 29/07, e o estado de fato mudou: a duplicidade acabou, e o número certo continua faltando.** `000041446801282` aparece **uma vez só** hoje em `financeiro.rateio_clientes`, na `G3-0312` — e a `G3-0141` (Fernando Albino) está com a coluna `uc` **vazia**. Alguém do lado do CRM esvaziou o campo; **o motivo não é medível daqui**, e não afirmo que foi correção desta questão. Consequência para o financeiro: a `G3-0141` é **recusa em cascata** — `unidade_consumidora.numero_uc` não aceita vazio —, e é uma das duas linhas do rateio que **não estão** entre as 39 UCs espelhadas. Ou seja, a hipótese *"um dos dois números foi digitado errado"* segue sem resposta: o número duplicado sobreviveu na `G3-0312` e a `G3-0141` ficou sem nenhum. Registrado em 29/07 pela regra 10, ao medir o CRM pela `Q-ORIGVEND-01` | Vinicius + operação |
| GERACAO-01 | 🟡 | **Crédito prometido sobre geração não lançada, e o cruzamento é do dev.** A série de `usina_geracao_mensal` é **digitação manual da fatura**, sem ingestão automática e **sem sinal de "competência completa"** — o sistema deles não distingue *"não gerou"* de *"não foi lançado"*. Evidência de que é lançamento e não operação: a `0002` foi criada em 08/07 e tem série desde **jan/2026** (backfill manual); a `0001`, criada em 03/07, só teve **junho** lançado; a `0003`, **nada**. **E o cruzamento que importa para a F2:** a `0003` é justamente a usina com **um cliente a 100% do rateio** (Ulisses José Barbosa Ramos) — há crédito prometido sobre geração nenhuma lançada. Se a `0003` nunca gerou ou se a `0001` operava antes de junho, só a operação sabe. **Consequência de desenho para a F2:** a base de conferência de fatura não pode tratar ausência de série como zero — é o mesmo modo de falha da R9 (valor nulo não é zero) numa entidade nova | Vinicius + operação |
| RATEIO-USO-01 | 🟡 | **A usina tem DUAS medidas e o sistema só controla uma.** <br>**28/07, REBAIXADA de 🔴 para 🟡 pela resposta 9a da PAUTA, e a razão é aritmética e não administrativa.** A base de faturamento passou a ser a **geração efetivamente medida** rateada pelo percentual contratado (`SPEC-003` R30). Então a soma dos kWh faturados de uma usina numa competência é `geracao × Σ percentuais / 100`, e a R11 já garante que Σ percentuais ≤ 100 — logo **o faturado não passa da geração por construção**, e não por vigilância. O overbooking de crédito, que era o medo declarado, some com a escolha. A view `uso_da_usina_por_competencia` torna isso *olhável*: se `saldo_kwh` ficar negativo, a composição foi contornada por outro caminho. **O que sobra, e por isso a questão não fecha:** decidir o que fazer quando a geração **não foi lançada** — hoje é recusa contada (`sem_geracao_lancada`), o que é seguro mas para o faturamento daquela UC, e o dev confirmou no `GERACAO-01` que o CRM não distingue "não lançada" de "zero". <br><br>**Texto original de 28/07, preservado:** Esclarecimento do dono em 28/07: *"existem dois fatores diferentes: o quanto ela será usada e o quanto ela já foi usada, funcionando para evitar overbooking"*. Traduzindo para o que existe: <br>• **quanto SERÁ usada** = Σ `percentual_rateio` das UCs contratadas. **Controlado** — a `SPEC-001` R11 rejeita acima de 100 % por trigger deferido, e `rateio_por_usina` classifica. <br>• **quanto JÁ FOI usada** = crédito efetivamente consumido contra a geração da competência. **NÃO EXISTE CONTROLE NENHUM.** `usina_geracao` guarda a geração (8 competências espelhadas em 28/07) e nada a confronta com consumo. <br>**As duas juntas é que evitam overbooking; hoje só a primeira existe.** E a mensagem de erro da R11 no `repos/rateio.ts` já confunde as duas — ela diz *"isso alocaria credito que a usina nao gera"*, misturando alocação com geração, que é justamente a distinção que o dono acaba de separar. <br>**O caso concreto já está no banco:** a usina `0003` tem **1 UC com 100 % alocado** e **zero geração lançada**. Pela R11 ela está `completo` e passa; não há invariante que note que não há nada atrás. <br>**Decidir:** (a) segundo invariante — crédito consumido por competência ≤ geração lançada da competência —, o que exige decidir o que fazer quando a geração não foi lançada *versus* quando foi zero (o dev confirmou no `GERACAO-01` que o CRM **não distingue** os dois); (b) só alertar, sem bloquear, como a R11 faz abaixo de 100 %; (c) tratar no faturamento em vez de no cadastro, faturando pelo gerado e não pelo alocado — o que é a `Q-021` do `PRD` §11 e tem dimensão fiscal (ver `PAUTA-contador.md` §10). **Bloqueia a F2:** sem essa decisão a base de faturamento é ambígua entre contrato e medição | Vinicius + contador |
| RATEIO-TETO-01 | 🟢 | **A política de teto de rateio deles e a nossa R11 divergem, e hoje isso não colide.** Do lado do CRM o `percentual_rateio` é **digitado manualmente** — nada calcula nem arredonda —, então os **99,78 %** (usina `0001`, 21 UCs) e os **91,20 %** (`0002`, 14 UCs) são transcrição da carteira/registro na distribuidora, **não resíduo gerado pelo sistema**. E o CRM **deliberadamente não força fechar 100**: há política anti-overbooking que bloqueia vínculo novo acima de `100 − margem`, com margem default de 5 %. Do nosso lado a R11 é teto **duro** em 100. **Hoje não há colisão** — as três somas medidas são 100,00 / 99,78 / 91,20. **Fica registrado porque a margem deles pode, em tese, admitir soma que a nossa constraint recusaria**, e aí o sintoma seria uma escrita rejeitada sem causa aparente. **Se a sobra é capacidade a vender ou erro de transcrição, o dado não registra** — é intenção, e só a operação responde. **Recomendação do dev, adotada:** a F2 trata a sobra como *"não alocado"* e **não a classifica** até essa resposta existir | Vinicius |
| Q-UC-DISTRIB-01 | 🟡 | **A UC herda a distribuidora da usina vinculada, e isso precisa de confirmação.** O CRM não expõe distribuidora em `rateio_clientes` (as colunas são outras), e do nosso lado `unidade_consumidora.distribuidora` é `NOT NULL` com FK — o mesmo obstáculo da usina. Duas saídas existiam: recusar toda UC, o que entregaria nada, ou derivar. **Derivei da usina vinculada**, e a diferença para um default é que o conector **não escolhe valor nenhum** — ele propaga o que o usuário já cadastrou na usina. Se a usina não estiver espelhada, a UC é recusa contada, em cascata. **A base para acreditar que a herança vale:** geração compartilhada compensa crédito dentro da mesma área de concessão, então UC e usina do mesmo rateio estariam na mesma distribuidora. **Não afirmo isso como fato verificado — é o motivo desta questão.** Se houver caso em que não vale (UC atendida por outra concessionária, rateio interestadual), a UC passa a exigir cadastro local como a usina. Teste `N46`, e o plantio da distribuidora fixa acusa. <br><br>**28/07 — a questão continua aberta, mas parou de depender de alguém lembrar dela.** A pergunta normativa segue sendo do Vinicius. O que mudou é que a suposição virou **sinal** em vez de ficar esperando resposta (`SPEC-002` R21-b, invariante 13). Havia um risco **independente da norma**: a herança da R21 acontece só no `INSERT`, e no `UPDATE` a `distribuidora` fica de fora porque é campo local e a R5 diz que o usuário vence — então alguém podia editar a UC amanhã, pôr outra concessionária, e **nada notaria** até um relatório vir errado. Agora divergiu, aparece em `conector_execucao.detalhe` e na saída do `npm run ciclo`. **Não recusa** (a linha é válida) e **não sobrescreve** (seria a R5 ao contrário) — é a mesma categoria de `garantia_de_tenant_degradada`: registra sem mudar o `status`. Testes `N51`–`N54`, verificados nos dois sentidos por plantio. **Medido contra produção em 28/07, em ensaio: zero divergências nas 35 UCs** — as três usinas são `Equatorial` e as UCs herdaram dela, então o sinal nasce silencioso, que é o estado correto. **O sinal não substitui a resposta, e é importante ver por quê:** a conferência roda só no `UPDATE`. A UC que **nasce** pela R21 herda o valor da usina por construção e não pode divergir de si mesma — se a herança estiver errada de origem, ela nasce silenciosa e assim permanece. E não é conferível internamente: o CRM não expõe distribuidora em `rateio_clientes`, então não há segunda fonte contra a qual comparar. **Corolário da medição:** as zero divergências de 28/07 dizem que ninguém editou o campo, e **não** que a herança está certa — seriam zero do mesmo jeito se as 35 estivessem todas erradas. Se a resposta for *"pode haver UC de outra concessionária"*, a UC passa a exigir cadastro local como a usina, e este sinal acusa cada caso **à medida que a operação digitar o valor real** — ele não acha as erradas sozinho | Vinicius |
| ~~Q-ESCOPO-01~~ | ✅ | **RESOLVIDA em 28/07: as quatro entidades da §2 estão implementadas** — `cliente`, `usina`, `usina_geracao` e `unidade_consumidora`, e a `PortaDeLeitura` passou de 3 para 7 das 8 views (a que falta é `parceiros`, que alimenta `originador`, F3). O texto original fica abaixo, datado. **O que sobrou não é escopo, é dado:** hoje as 3 usinas não estão cadastradas localmente, e por isso as 35 UCs são recusa em cascata. Um cadastro de 3 linhas destrava. <br><br>*Texto original de 27/07:* O conector entrega uma das quatro entidades que a `SPEC-002` §2 declara.** A §2 "Entra" diz *"upsert idempotente em `cliente`, `unidade_consumidora`, `usina`, `usina_geracao`"*. Medido em 27/07 por leitura do motor: espelha **só `cliente`** (mais `cliente_estado_crm`, que é dele por definição). Das 8 views que `src/crm/leitura.ts` sabe ler, `executarCiclo` chama **3** — `vendas_ganhas`, `leads_arquivados`, `lead_merges`. As outras cinco (`usinas`, `rateio_clientes`, `rateio_creditos`, `geracao_mensal`, `parceiros`) existem, são testadas na leitura e **nunca são consultadas pelo ciclo**. **Não é dívida cosmética, e ela se propaga:** (a) a R16 — originador vem de `partner_id`, nunca da tag — não tem onde acontecer, porque o conector não cria `contrato`; o `test_atribuicao_por_partner_id` da §9 não é teste faltando, é funcionalidade faltando, e por isso não foi escrito em 27/07; (b) `unidade_consumidora` e `usina` hoje só nascem por cadastro local, quando a `SPEC-002` promete espelho; (c) **é o bloqueio real da F2** — a base de faturamento é `consumo_kwh × tarifa` por UC, e sem espelho de usina e de geração não há o que faturar nem contra o que conferir. **Por que não foi improvisado agora:** completar o escopo é decidir o que é campo espelho e o que é campo local em três entidades novas (`SPEC-001` §3.3), e a `SPEC-002` §3 não desce a esse nível para elas. **Decidir:** (a) completar o escopo declarado antes de abrir a F2, com a §3 da spec estendida às três entidades; (b) reduzir a §2 ao que existe e mover o resto para uma `SPEC-004` própria, declarando o custo; (c) completar só `unidade_consumidora` e `usina` agora, deixando `usina_geracao` para a F2, onde ela é consumida. A (a) é a que não deixa a `SPEC-002` mentindo.
**DECIDIDA em 27/07 pelo dono: opção (a) — completar o escopo declarado.** E a medição feita em seguida, contra o CRM real, mostrou que as três entidades estão bloqueadas por motivos **diferentes**, nenhum deles previsto:
<br>• **`usina` — a distribuidora vem VAZIA.** As 3 usinas têm `distribuidora = ''` (string vazia, `length = 0`), e `usina.distribuidora` é `NOT NULL` com FK para a tabela de referência, que hoje tem **uma** linha (`Equatorial`). Assumir `Equatorial` porque é a única seria exatamente o improviso que a regra 10 proíbe — e é o mesmo formato do erro que a `Q-VALOR-01` pegou. Também medido: `potencia_kwp` **0 de 3** e `data_instalacao` **0 de 3** (a §7 já previa), e `dono_lead_codigo`/`dono_lead_nome` **0 de 3**, o que confirma a `C1-crm` e impede ligar `usina.dono_usina_id`.
<br>• **`usina_geracao` — pronta, mas depende de `usina`.** 8 linhas, 2 das 3 usinas, junção por `codigo_geradora`, e o enum `origem_geracao` já tem o valor `crm`. Nada a decidir aqui: destrava junto com a usina.
<br>• **`unidade_consumidora` — o bloqueio duro, e é a `F-01` com número.** A UC pendura em `cliente`, e **não há vínculo entre a carteira de rateio e os ganhos espelhados**: dos 36 `lead_id` de `rateio_creditos`, **ZERO** aparecem em `vendas_ganhas`. Os dois conjuntos são disjuntos por `lead_id`. Pior: por **nome** eles se cruzam em **42 pares, 24 pessoas distintas** — a mesma pessoa existe como dois leads diferentes, um no funil de vendas e outro na carteira legada. Espelhar UC a partir de `rateio_clientes` criaria 36 clientes, dos quais **24 são duplicatas de gente já espelhada** — e o dedup por `crm_lead_id` (R4) não pega, porque os `lead_id` são genuinamente diferentes. Medido também: `data_vencimento` **0 de 36** (a §7 previa) e **uma UC repetida** — `000041446801282` em dois contratos (`G3-0141` e `G3-0312`), mesma usina.
<br>**O que isto muda:** a opção (a) segue sendo a decisão certa, mas **não é executável hoje para `unidade_consumidora`**. `usina` e `usina_geracao` são executáveis assim que a distribuidora for decidida. A F-01 deixou de ser pergunta de planejamento e virou **pré-requisito medido** do espelho de UC | Vinicius |
| Q-CICLO-ORFAO-01 | 🟡 | **Com a R13 implementada, um ciclo morto por `kill` deixa `conector_execucao` em `em_andamento` para sempre — e o `EXCLUDE` trava o conector.** Antes da correção da `Q-LOTE-01` o ciclo inteiro era uma transação, então qualquer morte revertia a linha de abertura e o problema não existia; ele nasce da R13, que **exige** que a abertura commite antes dos lotes. O caminho normal está coberto e foi escrito junto: exceção em qualquer fase fecha o registro em `parcial` (§7) ou `erro`, na transação seguinte, e o erro original é relançado. **O que não está coberto é a morte que não passa por `catch`** — `SIGKILL`, OOM, queda do processo entre dois lotes. A `CHECK execucao_fim_coerente` da migration 14 impede que a linha fique meio-fechada, e o comentário dela já nomeava o risco: *"sem isto um ciclo que morre no meio fica 'em_andamento' para sempre e o 'segundo ciclo não inicia' da §7 trava o conector permanentemente"*. **Não improvisei um prazo de expiração.** Escolher "em andamento há mais de N minutos pode ser tomado" é decidir, no código, quanto tempo um ciclo legítimo pode demorar — e errar para menos faz dois ciclos processarem o mesmo lote em paralelo, que é exatamente o que a §7 proíbe. Enquanto não houver decisão, o desbloqueio é operacional e explícito: `UPDATE conector_execucao SET terminado_em = now(), status = 'erro' WHERE status = 'em_andamento'`, com a `CHECK` garantindo coerência. **Decidir:** (a) conviver — hoje o ciclo é disparado à mão e quem o matou sabe destravar; (b) prazo de expiração declarado, com o `iniciado_em` como âncora e o número saindo de medição, não de estimativa; (c) `heartbeat` — o ciclo carimba uma coluna a cada lote e a tomada exige silêncio maior que o intervalo de lote, que é o único critério que não depende de adivinhar a duração total. A (c) é a que sobrevive ao crescimento, e custa uma coluna | Vinicius |

> ## MEDIDO EM 14/08/2026 — a `Q-VALOR-01` (b) deixou de ser dívida de redação e virou **o** bloqueio da primeira fatura
>
> A `RETOMADA-2026-08-15` §7.1 afirma, em linha de comando pronta para copiar, que
> *"o conector semeia a tarifa a partir do card no próximo `npm run ciclo`"*.
> **Rodado valendo em 14/08 às 12:0x, contra produção: `unidade_consumidora.tarifa_reais_por_kwh`
> ficou em 0 de 41.** Não é falha do ciclo — ele saiu `status ok`, 108 lidos, 0 recusados.
>
> **A corrente, e onde ela arrebenta:**
>
> | Elo | Estado medido |
> |---|---|
> | `financeiro.vendas_ganhas` no CRM tem `consumo_kwh` **e** `consumo_reais` | **45 de 55 cards** — o dado existe |
> | `src/crm/leitura.ts:87` **lê** `consumo_reais` | sim, e o tipo o declara (`leitura.ts:228`) |
> | `sincronizacao.ts` grava no espelho do cliente | **cinco colunas: `nome`, `telefone`, `email`, `origem`, `consumo_kwh`.** `consumo_reais` é lido e **descartado** |
> | `cliente.consumo_referencia_centavos` | **0 de 88**, e o ciclo de 14/08 não mudou isso |
> | `tarifaDoCliente(consumo_kwh, consumo_referencia_centavos)` | devolve `null` na primeira linha: `centavos == null` |
> | `unidade_consumidora.tarifa_reais_por_kwh` | **0 de 41** |
>
> **Isto não é defeito novo — é a `Q-VALOR-01` (b) exatamente como ela foi escrita em 27/07**,
> e o comentário em `sincronizacao.ts:338-343` a cita pelo nome. O que mudou é a
> **consequência**: em 27/07 não gravar a semente era uma coluna vazia sem
> consumidor; desde a migration 30, que moveu a tarifa para a UC e apagou a tabela
> `tarifa`, essa coluna vazia é o **único** insumo da tarifa, e sem tarifa a
> composição do lote levanta pela R26. A dívida de 27/07 virou o caminho crítico.
>
> **Não improvisei o arredondamento, e é o mesmo motivo de 27/07:** converter
> `consumo_reais` em centavos exige decidir o que fazer com o que passa de duas
> casas, e a R23 proíbe `round()` em intermediário. Regra 10.
>
> **DECIDIDO em 14/08 pelo dono: o conector lê o campo de tarifa DIRETO, e não
> deriva.** Nem arredondamento de centavos, nem divisão — a origem é o campo que a
> operação digita no card.
>
> **E isso não é executável hoje: o campo não está no contrato.** Medido em 14/08
> no catálogo do CRM, como `financeiro_ro` — `column_name ilike '%tarifa%'` em
> **todos** os schemas visíveis devolve **zero colunas**, e a role só enxerga
> `financeiro` (as 10 views), `extensions`, `net`, `information_schema` e
> `pg_catalog`. As três views candidatas expõem:
>
> | view | o que tem |
> |---|---|
> | `vendas_ganhas` | `consumo_kwh`, `consumo_reais` — e mais nada de tarifa |
> | `rateio_clientes` | `percentual_rateio`, `uc`, `data_vencimento` — nenhuma tarifa |
> | `rateio_creditos` | `geracao_nominal_kwh`, `creditos_kwh_mes` — nenhuma tarifa |
>
> Pela regra 4 o financeiro não lê tabela base em nenhuma circunstância, então o
> campo **não existe para nós** enquanto não estiver numa view. **Pedido: a
> `PROMPT-dev-crm-rodada8-2026-08-14.md` já o fazia desde as 00:49 do mesmo dia, e
> segue sem resposta.** A `PROMPT-dev-crm-rodada9-2026-08-14.md` não repete o
> pedido — ela corrige a urgência, responde a pergunta 1 da rodada 8 e traz a
> prova da linha abaixo.
>
> **A PROVA NOVA, e ela decide a favor de ler em vez de derivar.** Derivando
> `consumo_reais / consumo_kwh` nos 45 cards: `1,130000` em **37**, `1,180000` em
> **4**, e o grupo do `1,16` em **4 — que não são iguais entre si**:
>
> ```
> 1,159997     1,160000     1,160001     1,160008
> ```
>
> Quatro cards, quatro números, para a mesma tarifa. Ninguém digita `consumo_reais`
> de modo a produzir `1,159997` por acaso quatro vezes: **alguém digitou `1,16`**, o
> `consumo_reais` foi gravado como `consumo_kwh × 1,16` **arredondado em duas
> casas**, e a divisão de volta devolve o arredondamento em vez do número. Ou seja
> — **o campo digitado existe, e derivar é uma volta a mais numa conta que já perdeu
> casas.** O erro é de 8 na sexta casa, é invisível (não é defeito, não alarma, não
> loga) e multiplica todo kWh de toda fatura.
>
> Isto também **corrige a leitura da `RETOMADA` §3.3**, que dizia *"redondos em duas
> casas nos 45 cards, sem exceção"*: 41 dos 45 são redondos, 4 não são, e são
> justamente os 4 que provam a existência da fonte digitada.
>
> **Enquanto a coluna não vem, o desbloqueio é digitar as 41 na aba Unidades** — o
> campo, o filtro de pendência e o alerta entraram nesta leva. E a `Q-VALOR-01` (b)
> **sai do caminho crítico da tarifa** por esta decisão, mas continua aberta pelo
> mérito dela: `consumo_reais` segue lido e descartado, e `consumo_referencia_centavos`
> segue em 0 de 88.
>
> **Enquanto não houver decisão, o estado é honesto e não silencioso:** a R26
> levanta em vez de tratar base nula como zero, que é exatamente o que a
> `Q-TARIFA-CRM-02` mandou preservar quando a aba Tarifas saiu.

## 5. F2 — faturamento

| ID | Nível | Pergunta | Quem |
|---|:--:|---|---|
| **POP-01** | 🟡 | **Tres populacoes, tres numeros, e o faturamento precisa de um denominador.** 29 leads em `Rateio Concluido` · 36 vinculos em `usina_clientes` · 28 de 36 homologadas (sessao 3). Qual e a base de cobranca? | Vinicius + operacao |
| **F-01b** | 🔴 | **Sucessora do F-01.** Nenhuma etapa do funil marca o cliente pagante — o card sai do `won` à mão, e o estado "desconto na fatura" vive fora do CRM. O gatilho real é a 1ª fatura com desconto da distribuidora. Faturar no `won` do Rateio fatura cedo demais | Vinicius + operação |
| ~~**Q-021 / AUD-03**~~ | — | ~~Faturar pela geração nominal ou pela série real?~~ **RESOLVIDA em 28/07 — ver §9** |
| **Q-FATCHEIA-01** | 🔴 | **O que é "fatura cheia"?** O `PRD` §5.4 diz que *"fatura não-cheia não avança o contador nem gera comissão"* e **em nenhum lugar define o termo** — não está no PRD, na `SPEC-001` nem no `GLOSSARIO`. Varredura feita em 28/07: a palavra aparece 4 vezes, sempre como premissa. **A consequência é imediata e é dinheiro:** a resposta define em que mês começa a pagar a comissão de todo contrato novo, e com captador sênior são 60% do consumo repartidos nas duas primeiras. A `SPEC-003` R35 declara a única regra derivável do dado que existe — cheia é a competência que o contrato cobre do primeiro ao último dia —, e a coluna `fatura.flag_fatura_cheia` é `NOT NULL` **sem default** de propósito: quem compõe tem de dizer, e o dia em que a regra for outra muda-se num lugar só. **Decidir:** (a) confirmar a regra da R35; (b) cheia = qualquer fatura acima de um piso de valor; (c) cheia = toda fatura, e o escalonamento passa a contar meses corridos | Vinicius |
| **Q-SICOOB-01** | 🔴 | **13/08/2026, tarde — O GERENTE RESPONDEU COM UM MANUAL DO PRÓPRIO SICOOB, E ELE RESPONDE 3 DAS 7 PERGUNTAS.** *"Manual para criação de aplicativo no portal Developers"*, Brasília/DF, **22/11/2024** — fonte **primária**, e de 21 meses atrás. Registrado em `RESPOSTA-gerente-sicoob-2026-08-13.md`; a folha do portal ganhou a marca de proveniência **📄**, que fica **entre** o 👁 e o 📹. **O QUE ELE FECHA — as três do certificado:** *"certificado digital válido do **tipo A1** emitido pela **ICP Brasil** para o CNPJ/CPF do cooperado"* (nomeia A1 e só A1, nunca menciona A3 — confirmação por nomeação exclusiva, não negativa explícita), qualquer AC do ICP-Brasil, e a linha que valia a pergunta inteira: *"insira **somente a chave pública** do certificado no formato **`.PEM`, `.CRT` ou `.CER`**"*. **O risco que motivou a pergunta NÃO existe:** o formulário não pede o `.pfx`, e a chave privada não sobe em campo web. **O `ADR-0005` sai confirmado, não emendado**, e a compra do A1 — decidida em 13/08 de manhã com base em vídeo de terceiro — deixa de ter pergunta em aberto. **CORREÇÃO DE REGISTRO:** esta questão e a folha afirmaram que *"o que sobe é `.PEM` em vez de `.CER`"*, tratando a suposição original como corrigida. **As três extensões servem** — a suposição original estava certa e o vídeo mostrou uma das três. Não muda procedimento; muda o que se acreditava ter descoberto. **O QUE ELE NÃO RESPONDE, e são as quatro que travam:** (a) **sandbox** — a palavra não aparece, há **um** fluxo de criação e nele o certificado é exigido, então **a premissa do paralelismo continua caída** e nada a restaura (ausência de menção não é prova de ausência de sandbox); (b) os **três campos** do contrato de cobrança — o `B4` continua aberto; (c) a **chave Pix aleatória** (`Q-SICOOB-PIXCHAVE-01`); (d) a **autenticação do webhook** (`Q-WEBHOOK-01`). **TRÊS ACHADOS QUE NINGUÉM TINHA PERGUNTADO**, e nenhum tinha ocorrência no corpus: **(1) há um TERCEIRO login** — o passo 3 exige, dentro do fluxo, **cooperativa + conta corrente (ou chave de acesso) e a senha do App Sicoob Mobile Banking**, que não é a senha do portal nem o token da API; a folha tinha aviso para não confundir **dois** endereços e agora são **três** coisas chamadas login; **(2) o segundo fator trava a criação** e o próprio manual traz o contorno (ativar, ou desativar e reativar as notificações do App Sicoob) — manual que traz o próprio contorno está dizendo que a falha é frequente; **(3) a ativação do PJ tem caminho exato** — App Sicoob → pesquisa → *"Transações Pendentes / Detalhamento"* → *"Autorização para Uso de APIs"* → senha —, **mais um segundo caminho que o vídeo não mostrou: o Sicoobnet Empresarial**. E o manual diz *"os **responsáveis** pela conta"*, no plural: se a conta PJ da G3 exigir mais de um autorizador, a ativação depende de mais de uma pessoa — **`Q-SICOOB-AUTORIZA-01`**, aberta por isto. **O `B2` GANHOU CORROBORAÇÃO E NÃO FECHOU:** o passo 11 diz *"a credencial **(client id)**"*, no singular, e nenhum passo menciona segredo — mas o manual descreve o que fazer, não enumera o que o painel devolve. **São duas fontes independentes e nenhuma delas é o painel.** ⚠️ **O `ADR-0005` §38 ainda descreve a resolvedora devolvendo TRÊS coisas** (*"o certificado A1, o `client_id` e o `client_secret`"*). **O ADR não foi editado de propósito:** é decisão registrada, e emenda de decisão é `r2` com medição 👁 — não com corroboração documental. A emenda sai no dia em que o painel do aplicativo criado mostrar, ou não, um segundo campo. **A instrução oficial de exportação do certificado NÃO foi lida** — o manual aponta `portal/documentacao` → Segurança → Certificado Digital, e o portal é SPA que não devolve conteúdo sem navegação autenticada. É ponteiro, não medição. **CONTINUA VERMELHA, e pela mesma razão de sempre:** o critério de saída da F2 é boleto liquidado, e nada disto foi exercido contra a API real. **Nenhuma linha de código passou a ser escrevível** — `src/sicoob/http.ts` segue bloqueado pelo `B4` e pela credencial. Corpo anterior: **13/08/2026 — O PORTAL FOI ABERTO, E SOBROU UM ITEM SO.** Folha preenchida em `SICOOB-portal-2026-08-06.md`, com **proveniencia marcada linha a linha** (visto por nos · video de terceiro · em branco) — a folha tinha sido escrita a partir de documentacao, e agora metade dela e navegacao. **(1) A CONTA PJ JA EXISTE e o portal a reconhece:** a aba *Informacoes* da criacao de aplicativo confirma **numero da conta e nome do cooperado**, e ela veio preenchida. A metade cara do `B5` estava resolvida sem ninguem ter medido. **(2) O `B2` FOI RESPONDIDO — NAO HA `client_secret`.** O fluxo copia **so o `client_id`** e o scope e vazio, entao **o certificado E a credencial** e a resolvedora do `ADR-0005` devolve **dois** campos e nao tres. E a forma que o `P2` previa como possivel, e ela muda o que se escreve, nao so o que se guarda. **(3) A PREMISSA DO PARALELISMO CAIU.** `PROXIMOS-PASSOS-2026-08-09` §B.1 (`B5`) e §B.5 afirmavam que a conta PJ e o A1 *"correm em paralelo e nao bloqueiam o sandbox"*: **falso** — nao ha aplicativo sem conta confirmada **e** certificado subido na aba *Seguranca*. Custou pouco porque a conta ja existia, mas a afirmacao estava escrita em dois documentos e foi repetida em conversa. **(4) O QUE SOBE NO PORTAL E O `.PEM`**, a parte **publica** derivada do `.pfx` por OpenSSL — e isso acrescenta ao `ADR-0005` uma distincao que nao estava escrita: existe um artefato **publico**, subido a mao, que **nao e segredo e nao entra no cofre**. O procedimento de terceiro gera tambem um `.key` com `-nodes`, ou seja **chave privada sem senha**, que e exatamente o que o `ADR-0005` §110 descartou — por isso ele **nao e gerado aqui**. **O UNICO ITEM EXTERNO RESTANTE E O A1 e-CNPJ:** A1 e **nao A3**, arquivo `.pfx`/`.p12` com senha, CNPJ `66714022000121`, emitido por **AC ICP-Brasil e nao pelo Sicoob** — logo nao depende de mais ninguem. Depois dele: criar o aplicativo, **confirmar no app do banco** (sem isso ele fica `pendente` e o `client_id` nao serve) e ai o `src/sicoob/http.ts` passa a ser escrivivel. **Continua vermelha** porque o criterio de saida da F2 e boleto liquidado, e nada disto foi exercido contra a API real. Corpo anterior: **05/08, fim do dia: a DOCUMENTACAO foi lida, e o lado de SAIDA ficou sabido.** Producao `https://api.sicoob.com.br/cobranca-bancaria/v3`, sandbox `https://sandbox.sicoob.com.br/sicoob/sandbox/cobranca-bancaria/v3`, OAuth2 em **Keycloak** (`auth.sicoob.com.br/auth/realms/cooperado/protocol/openid-connect/token`), `Authorization: Bearer` MAIS `client_id` em toda chamada, e certificado **ICP Brasil emitido para o CNPJ do cooperado**. Isso confirma o `ADR-0005` sem muda-lo e levanta um detalhe novo: **o `client_secret` pode nem existir** - em Keycloak com mTLS o certificado E a credencial, e qual das duas formas a Sicoob usa muda o que a resolvedora do cofre devolve. **O objeto `pagador` tambem ficou medido:** `numeroCpfCnpj · nome · endereco · bairro · cidade · cep · uf · email`, com `endereco` em UMA string (concatena o adaptador), `cep` sem mascara e `uf` de duas letras - exatamente o que `npm run enderecos` ja normaliza. **`email` esta no payload deles e NAO esta no nosso tipo `Pagador`**: 3 de 29 clientes faturaveis tem e-mail, 29 de 29 tem telefone. **O que continua vermelho e o que sempre foi externo:** o A1 e a credencial de sandbox, mais o proprio `http.ts`, que ninguem escreveu. Corpo anterior: **05/08: um dos dois bloqueios internos CAIU.** O `ADR-0005` foi **decidido** (Opcao A, Supabase Vault + funcao resolvedora `SECURITY DEFINER` amarrada ao tenant, com trilha na mesma transacao), entao **`src/sicoob/http.ts` passou a poder ser escrito** - antes nao se sabia de onde ele leria o A1. E a `Q-WEBHOOK-01` ganhou desenho (`ADR-0006`, Proposta). **O que continua vermelho e o que sempre foi externo:** o certificado A1 e a credencial de sandbox. Sobra tambem o proprio `http.ts`, que ninguem escreveu ainda. Corpo original abaixo:  **Certificado A1 e credencial de sandbox da Sicoob não existem, e sem eles o critério de saída da F2 não fecha.** O `PRD` §10 mede a F2 por *"boleto liquidado no sandbox baixa a fatura automaticamente"*. Em 28/07 o ciclo inteiro está provado **contra o adaptador falso** (`tests/repos-carteira.ts`, 31 verificações): compor, emitir, registrar, o banco pagar, baixar e repartir. O que falta é exatamente a metade que não dá para simular. **Do nosso lado está pronto:** a `PortaDeCobranca` é injetada, `conector_cobranca` guarda a referência do segredo por tenant (regra 5) e o composition root liga `COBRANCA_NAO_CONFIGURADA` por padrão — que recusa com 503 nomeado em vez de fingir. **Falta:** obter o A1, a credencial de sandbox, e escrever `src/sicoob/http.ts` contra a Cobrança v3 | Vinicius |
| **Q-SICOOB-PIXCHAVE-01** | 🟡 | **13/08/2026, tarde — PERGUNTADA AO GERENTE E NÃO RESPONDIDA.** O `PROMPT-gerente-sicoob-2026-08-13.md` §1.3 perguntou o procedimento para solicitar a chave aleatória vinculada ao contrato. **A resposta foi um manual de criação de aplicativo no portal, e ele não menciona Pix em passo nenhum.** **Isto continua sendo pergunta de gerente** — é ato dele, não tela do portal, e das quatro que ficaram sem resposta é a **única** que estava no destinatário certo. **Vale insistir**, e insistir só nela: as outras três (sandbox, os três campos do contrato, autenticação do webhook) têm dono diferente, e reperguntar tudo ao gerente gasta boa vontade sem trazer resposta — repartição por destinatário em `RESPOSTA-gerente-sicoob-2026-08-13.md` §6. **A questão não mudou de conteúdo nem de nível**, só ganhou a informação de que já foi perguntada uma vez. Corpo anterior: **13/08/2026 — O PIX DO BOLETO EXIGE UMA CHAVE QUE A G3 NAO TEM, E ELA NAO SE CADASTRA NO PORTAL.** Medido no video de terceiro que descreve a configuracao da Cobranca v3 (`SICOOB-portal-2026-08-06`, item 3): *"e necessario que o cliente crie uma chave Pix **aleatoria** e mande para o **gerente vincular ao contrato** dele. Depois que essa chave Pix aleatoria e vinculada ao contrato, a gente pode marcar o indicador de Pix"*. **A chave de producao da G3 e tipo `cnpj`** (`66714022000121`) e serve ao **Pix estatico** do documento — que e o meio de pagamento no ar hoje. **Sao duas chaves, dois propositos**, e a segunda depende de **ato humano fora do portal**. **A consequencia esta numa afirmacao ja escrita:** `PROXIMOS-PASSOS-2026-08-09` §B.1 argumenta que *"os dois"* e barato porque o `POST /boletos` ja traz o BR Code com `"codigoCadastrarPIX": 1` — verdade, e **com um pre-requisito que nao estava nomeado**. Do nosso lado o custo e baixo: o banco de chaves existe desde a migration 17 e cadastrar a segunda e um comando (`npm run identidade`). O que nao e nosso e o vinculo. **Cruza com o `B3`** (boleto, Pix `cob`, ou os dois), que continua sem ID proprio e sem decisao | Vinicius + cooperativa |
| **Q-SICOOB-PARCEIRA-01** | 🟢 | **13/08/2026, tarde — O MANUAL DO SICOOB ACRESCENTA UM DADO E NÃO RESPONDE A PERGUNTA PRINCIPAL.** 📄 **O que ele acrescenta:** *"nos casos de integração com empresa parceira, ficarão disponíveis (em cores) apenas as APIs que a empresa oferece"* — **o catálogo da parceira LIMITA o do cooperado**, quem integra por terceiro só alcança o que o terceiro já oferece. E confirma a bifurcação: *Sim* escolhe a parceira numa lista, *Não* é o ramo em que o certificado A1 é exigido — logo **quem marca "sim" não sobe certificado nenhum**, e quem responde pela identidade é a parceira. **O que ele NÃO responde é justamente o que a questão pergunta:** se a parceira precisa ser homologada antes, se o interruptor muda o contrato da API, e sobretudo **se um certificado de parceira atende vários cooperados** — a de desenho, que colide com `conector_cobranca.credencial_ref` **por tenant**. **Continua verde**, e a nova informação reforça por quê: o dia em que a G3 virar parceira, o catálogo de APIs que ela oferecer passa a limitar os cooperados dela. Corpo anterior: **13/08/2026 — O INTERRUPTOR "EMPRESA PARCEIRA", E O QUE ELE SIGNIFICA NO DIA EM QUE A G3 FOR A PARCEIRA.** A aba *Seguranca* da criacao de aplicativo pergunta se a integracao e feita por **empresa parceira**, e o certificado subido responde por essa identidade. **Para a G3 hoje a resposta e trivial e foi dada em 13/08:** marcar **nao**, com o A1 da propria G3 — quem integra e o titular da conta, e nao ha integradora. **O que fica aberto e o outro lado, e nada disto esta medido:** se a parceira precisa ser homologada antes, se o interruptor muda o contrato da API, e sobretudo **se um certificado de parceira atende varios cooperados**. **A ultima e de desenho, nao de operacao:** o financeiro e multi-tenant, e no dia em que a G3 rodar o sistema para outra empresa solar **a G3 vira a empresa parceira** — um certificado para N cooperados e o oposto de `conector_cobranca.credencial_ref` **por tenant**, que e o que o `ADR-0005` decidiu. **O que ja esta certo e nao depende de medir:** o certificado carrega um CNPJ e quem tem a chave privada autentica como aquele CNPJ; com certificado de terceiro a G3 **nao revoga sozinha**; a renovacao anual passa a depender do calendario do terceiro; e o cofre da G3 guardaria chave privada de outra empresa — custodia sem controle. **Verde** porque nao bloqueia nada hoje e a decisao de hoje e clara. Entra na lista para **nao ser redescoberta com um cliente na mao** | Vinicius |
| **Q-SICOOB-AUTORIZA-01** | 🟢 | **13/08/2026 — QUANTAS PESSOAS PRECISAM AUTORIZAR O APLICATIVO, E A PERGUNTA NASCEU DE UM PLURAL.** 📄 O manual do Sicoob (22/11/2024, entregue pelo gerente) diz que o aplicativo de conta **PJ** nasce `pendente` e que *"será necessário que **os responsáveis pela conta** autorizem"* — **no plural** —, por App Sicoob (pesquisa → *"Transações Pendentes / Detalhamento"* → *"Autorização para Uso de APIs"* → senha) **ou** pelo **Sicoobnet Empresarial**. **Não está medido quantos responsáveis a conta PJ da G3 tem, nem se a autorização de API exige assinatura conjunta.** **Verde pela taxonomia da §1** — não reescreve schema, policy, contrato de integração, tela nem cálculo; é pré-condição operacional. **E está no caminho crítico mesmo assim:** o A1 é o único item externo restante, e o pior momento de descobrir que falta um segundo autorizador é com o certificado **já comprado** e o aplicativo parado em `pendente`, porque aí o custo não é a espera — é a espera depois do desembolso. **Conferência de dois minutos, e ela vem antes da compra**, junto com a outra que o mesmo manual entregou: as **notificações do App Sicoob precisam estar ativas**, porque é por elas que chega o segundo fator do passo 4 da criação — e o manual traz o contorno (desativar e reativar), o que indica falha frequente. Os dois viraram os passos **0a** e **0b** de *"o que falta, em ordem"* na folha `SICOOB-portal-2026-08-06.md`. **Fecha sozinha** no momento em que o dono olhar a conta; não depende de ninguém do Sicoob responder | Vinicius |
| **Q-WEBHOOK-01** | 🟡 | **13/08/2026, tarde — PERGUNTADA AO GERENTE, NÃO RESPONDIDA, E O SILÊNCIO CONFIRMA O DIAGNÓSTICO DE 05/08.** O `PROMPT-gerente-sicoob-2026-08-13.md` §1.4 perguntou as quatro formas possíveis (mTLS, faixa de IP, cabeçalho combinado, assinatura do corpo) e a marcava, na nota interna, como *"a mais valiosa e a menos provável de ser respondida por ele"*, encaminhável de propósito para não travar as outras. **Foi exatamente o que aconteceu:** a resposta foi um manual de criação de aplicativo, que não toca a direção de ENTRADA em passo nenhum. **Isto não é resposta negativa, é destinatário errado** — e agora está medido que é errado, não suposto. A pergunta vai para o **suporte técnico / canal de desenvolvedor**, não para o gerente de conta. **O `ADR-0006` NÃO MUDA, e é importante dizer o que NÃO aconteceu:** nenhuma opção voltou à mesa (isso exigiria o portal oferecer cabeçalho), e **o plano B do §3 NÃO foi acionado** — acioná-lo exige *saber* que não há mTLS nem faixa de IP, e continuamos sem saber. O ADR foi desenhado para acomodar as três formas justamente porque esta resposta não vinha; ele segue esperando, e esperar é o comportamento previsto. **Aberta desde 28/07 — 16 dias**, e continua sem emergência pela mesma razão de sempre: `GET /boletos/situacao` é a consulta ativa diária do `PRD` §6, o dinheiro chega no dia seguinte e a baixa manual funciona hoje. Corpo anterior: **05/08, fim do dia: a documentacao foi lida e a metade que esta questao precisa NAO E PUBLICA.** O material publico da Cobranca v3 descreve o cadastro da URL no portal e o escopo de webhook da aplicacao, e **nao descreve cabecalho, assinatura, certificado de cliente nem faixa de IP na direcao de ENTRADA** - ou seja, como a Sicoob se autentica ao chamar o NOSSO endpoint. **Isso nao e "nao tem", e "nao e publico"**, e a diferenca e de destinatario: o pre-requisito deixou de ser *"ler a documentacao"* e passou a ser **perguntar ao suporte da Sicoob** ou abrir a aplicacao no portal e ver o que a configuracao de webhook oferece. **A recomendacao do `ADR-0006` nao muda por causa disto** - ela ja estava desenhada para acomodar as tres formas, e e para este resultado que foi desenhada assim. Corpo anterior: **05/08: DESENHADA — `adr/ADR-0006-autenticacao-do-webhook.md`, Proposta, aguarda decisao.** Baixou de vermelha para amarela porque o caminho deixou de nao existir: ha desenho, com opcoes medidas e recomendacao. **E as tres decisoes acopladas eram QUATRO.** A quarta apareceu percorrendo o caminho de ESCRITA em vez de reler a rota: **webhook nao tem usuario**. `src/db/contexto.ts:79` exige `usuarioId` em forma de UUID, `src/repos/liquidacao.ts:96` exige o papel `escrever_carteira`, e a regra 9 exige *quem* - e a Sicoob nao e nenhuma das tres coisas. Reusar o `auth_user_id` de uma pessoa (que e o que os scripts fazem hoje com `--auth-user`) faria a trilha dizer que o Vinicius baixou uma fatura as 3h da manha: **trilha que mente e pior que trilha ausente**. A proposta e usuario de SERVICO por tenant, sem caminho de login e com o papel minimo. **O que governa o ADR inteiro e o que NAO esta medido:** o que a Sicoob suporta. HMAC so existe se eles assinarem, mTLS depende de o TLS chegar ao Node, cabecalho depende de a configuracao do webhook aceitar um - metade das opcoes nao e exercivel. E a licao da `Q-VIEWSCRED-01` aplicada ANTES de custar, entao o ADR escolhe a FORMA que acomoda as tres e nomeia a leitura da documentacao da Cobranca v3 como pre-requisito. **E nada disto e emergencia:** `GET /boletos/situacao` e a consulta ativa diaria do `PRD` §6 e existe justamente para capturar liquidacao cujo webhook falhou - sem webhook o dinheiro nao se perde, chega no dia seguinte, e a baixa manual funciona hoje. Corpo original abaixo:  **Como a Sicoob se autentica no webhook de liquidação, e como o tenant é resolvido?** Medido em 28/07: `POST /api/liquidacoes/webhook-sicoob` passa por `emTenant` (`src/http/rotas.ts:553`), e em `src/http/servidor.ts:233` **toda rota exceto `GET /publico/config` exige Bearer resolvido para `auth_user_id`** — mais o cabeçalho `x-tenant-id` para escolher o tenant. A Sicoob não emite JWT do Supabase nem conhece o nosso `tenant_id`: **do jeito que está, o webhook real não entra.** O caminho não está desenhado em lugar nenhum — não está no `PRD` §6, não está na `SPEC-003` §177, não estava aqui. Três decisões acopladas: (a) o que autentica — assinatura HMAC do corpo, mTLS do lado deles, ou token opaco por tenant; (b) de onde sai o tenant — do `nossoNumero`/`referencia` do boleto, ou de um caminho por tenant na URL; (c) se a rota sai de trás do autenticador ou ganha um autenticador próprio. É vermelha pela regra da §1: a resposta errada reescreve contrato de integração, e possivelmente `conector_cobranca`. **Não bloqueia hoje** — o adaptador falso e a baixa manual não passam por aqui —, mas bloqueia o critério de saída da F2 do `PRD` §10, que é *"boleto liquidado no sandbox baixa a fatura automaticamente"*. Mitigação parcial já prevista: a consulta ativa diária do `PRD` §6 captura a liquidação mesmo sem webhook, e **ela também não tem agendador** — ver `Q-AGENDA-01` | Vinicius |
| **Q-AGENDA-01** | — | **RESOLVIDA em 30/07/2026.** Os dois processos do `PRD` §6 existem. **Fila de emissao com retry exponencial:** `src/dominio/agenda.ts` (puro) decide quando retentar, `src/cobranca/agenda.ts` executa, e a migration 21 acrescentou `boleto.ultima_tentativa_em` e `proxima_tentativa_em` — sem carimbo, "esperar 2^n" so poderia ser contado do nascimento da linha, e um boleto que falhou ontem seria retentado junto com um que falhou agora. **Consulta ativa diaria:** le a situacao no banco e aplica a baixa por `repos/liquidacao.baixar()`, que continua sendo o unico gatilho do split. **A decisao que nao e arbitraria: a fila NAO desiste sozinha.** Nao ha teto de tentativas, e o teto e do INTERVALO — um boleto que sai da fila por contagem para de ser cobrado sem que ninguem tenha decidido parar, e do ponto de vista do sistema a fila fica limpa. **`agenda_execucao` existe porque o modo de falha de um processo periodico e a AUSENCIA de execucao**, que nao produz erro, nem log, nem linha: sem a tabela, "a consulta ativa parou no dia 3" nao e uma pergunta que alguem consiga fazer. **NAO ha agendador no codigo**, e e regra 10: o `PRD` §3 deixou a escolha do host em aberto, entao o script roda uma vez e sai. **55 verificacoes** (24 puras + 31 com banco). Os quatro numeros que o PRD nao da viraram **Q-AGENDA-02**. Corpo original abaixo: **Não existe processo periódico no sistema, e o `PRD` §6 pede dois.** Verificado em 28/07 por varredura de `src/` e `scripts/`: nenhum `cron`, `setInterval` ou agendador. Faltam a **fila de emissão com retry exponencial** (as colunas existem — `boleto.tentativas` e `ultimo_erro`, R45 — mas nada as consome) e a **consulta ativa diária** dos boletos em aberto (a rota `GET /api/boletos/situacao` existe e ninguém a chama). A segunda não é conveniência: webhook perdido sem consulta ativa é dinheiro recebido que o sistema nunca baixa, e a inadimplência passa a acusar quem pagou. Amarela e não vermelha porque não reescreve schema nem contrato — é entrega que falta, e o lugar dela já está decidido (processo persistente no VPS, `systemd timer` ao lado do `financeiro.service`) | Vinicius |
| ~~**Q-ORIGINADOR-01**~~ | — | ~~Os 39 contratos que a operação vai digitar levam originador?~~ **RESOLVIDA em 29/07 na opção (a) — ver §9.** Corpo original preservado abaixo, porque é ele que registra o que foi medido: | Vinicius + operação |
| | | **Os 39 contratos que a operação vai digitar levam originador — e ele não existe no banco.** Medido em 29/07 contra produção: a tabela `originador` tem **0 linhas**, então o select *Originador (comissão)* da tela de Contratos oferece uma lista vazia e quem digitar hoje grava `originador_id` nulo nas 39. **A escolha não tem caminho de volta pelo uso normal:** `src/repos/contrato.ts` expõe `rascunhar`, `ativar`, `suspender`, `encerrar` e `renovar`, e **nenhuma edição** — `originador_id` e `originador_tipo_no_fechamento` só se escrevem no `rascunhar`, porque a R20-b congela o tier no fechamento. Corrigir depois é `encerrar` + `renovar`: abre linha nova, zera `faturas_cheias_pagas` e deixa na trilha uma renovação que não houve comercialmente. **A consequência é dinheiro e é silenciosa:** `src/repos/split.ts:164` só monta item de comissão quando há `originador_id` **e** tier congelado — sem os dois o split roda, fecha e não paga comissão, sem erro, sem log e sem recusa. **Decidir, antes de a digitação começar:** (a) cadastrar os originadores primeiro e exigir o campo na tela; (b) declarar que a carteira legada não tem comissão — aí o nulo é a resposta certa e precisa estar escrito, não implícito; (c) digitar agora e reconciliar depois, assumindo o custo do `encerrar`+`renovar`. Vermelha pela segunda cláusula da §1 — *entrará em produção errado* —, e o custo de responder depois não é o mesmo de responder antes | Vinicius + operação |
| **Q-DOCFATURA-01** | 🟡 | **12/08/2026 — TERCEIRA VOLTA, E ELA REVERTE A DECISAO 2: o layout deixa de ser configuravel e passa a ser FIXO.** O dono pediu o design de `github.com/lealvbl-stack/g3_fatura_unificada`, que e uma folha A4 em fluxo — e o editor de blocos sai. Decisao, corpo e as seis coisas que ela nao resolve estao na **`Q-DOCG3-01`** e em `PLANO-documento-modelo-g3-2026-08-12.md`. **A virada espera o leitor da Equatorial**, entao em 12/08 nada foi apagado e nada foi construido. Corpo da segunda volta abaixo, intacto: **03/08, SEGUNDA VOLTA DA DECISAO 2 — o layout deixou de ser lista e passou a ser POSICAO.** O dono pediu: *"construir com base no tamanho de folha que eu escolher e colocar os elementos onde eu desejar, nao apenas assinalar se o elemento aparece ou deixa de aparecer"*. A decisao 2 de 30/07 entregou `campo_do_documento` — quais campos, em que ordem, com que rotulo —, e isso e uma lista linear: **sem coordenada, sem largura e sem coluna, entao duas informacoes nunca podiam ficar lado a lado**. E a moldura (logo, titulo, faixa de pagamento) nem configuravel era: estava em JSX fixo. **Construido em 03/08: migration 23** (`layout_do_documento` + `bloco_do_documento`, seis tipos de bloco, geometria em milimetro `numeric(6,1)`), `src/dominio/layout-visual.ts` puro, `PUT /cobranca/layout` e o editor de arrastar na aba Documento. **Nenhuma configuracao existente foi migrada ou perdida:** `campo_do_documento` continua sendo o CONTEUDO da tabela de valores e o bloco `tabela_de_campos` a pinta; layout vazio cai no padrao em codigo, que reproduz o documento de sempre. **Tres defeitos foram consertados junto, e os tres eram silenciosos:** (a) `@page` nao declarava `size`, entao **o papel era o padrao do sistema operacional de quem imprime** — a mesma fatura saindo com geometria diferente em duas maquinas, sem nada registrar qual foi usada; (b) a previa era `max-width: 800px` em pixel e o papel era `width: 100%` em milimetro — **duas geometrias**, entao ela conferia conteudo e nao forma; (c) o negrito do `valor_total_centavos` vivia em duas linhas do `.tsx` e **nao viajava no payload**, entao o CRM — que consome a mesma rota e nao roda React — receberia a fatura sem ele. Agora estilo e DADO do bloco. Plano completo em `PLANO-layout-visual-2026-08-03.md`; **74 verificacoes** (29 puras + 31 do editor + 14 com banco). **O que a segunda volta NAO resolveu, e fica declarado:** nao ha gerador de PDF no servidor (segue `window.print()`, decisao 3 intacta — o que mudou e que o papel agora e declarado), nao ha paginacao automatica (uma folha; o que nao couber e sinal), nao ha layout por tipo de documento e a fonte segue sendo a de `web/src/tema.ts`. Corpo original abaixo: **O documento que o cliente recebe é NOSSO, decidido em 29/07 pelo dono — e a decisão abre quatro coisas que ninguém respondeu ainda.** A pergunta apareceu ao percorrer o caminho de cobrança pela tela: *"preciso gerar boletos com logo e divisão personalizada de campos"*. **Medido antes de propor:** varredura de `src/`, `web/`, `prisma/` e `scripts/` — **zero** ocorrência de PDF, logo ou renderização; `tenant` tem 7 colunas e nenhuma de marca; e o único gancho de personalização que existe é `PedidoDeBoleto.mensagens`, *"linhas livres impressas no boleto"*, que pressupõe que **quem imprime é o banco**. **Decisão (a) entre três:** a G3 emite uma **fatura de energia própria** — logo, campos e ordem escolhidos por ela, com o detalhamento do crédito —, e a **parte pagável** (linha digitável, código de barras e QR Pix) vem do Sicoob e entra como faixa nesse documento. **O que a decisão resolve:** não renderizamos boleto FEBRABAN, então não assumimos a responsabilidade de imprimir linha digitável errada — o cliente pagaria certo um valor errado —, e a personalização não fica limitada ao que o convênio permitir. **O que ela NÃO resolve, e é o motivo desta entrada:** (a) **onde mora o binário da logo.** Não há coluna, e as três saídas têm custos diferentes: `bytea` na tabela (entra no backup e na RLS junto com o resto, e infla o dump), armazenamento de objeto com URL na coluna (mais barato, mas é dependência nova e a URL vaza se não for assinada), ou arquivo no VPS (simples e **quebra o multi-tenant**, porque o disco não tem `tenant_id`). Qualquer uma exige migration com `tenant_id`, RLS, policy e gatilho de auditoria (regras 2, 3 e 9) — **não escolhi**, porque é decisão com consequência de operação e não detalhe de implementação; (b) **quais campos são configuráveis e em que ordem** — isso é insumo do dono: o que o cliente precisa ver na fatura dele; (c) **o PDF exige dependência nova.** O projeto tem **três** dependências de runtime no servidor (`@prisma/adapter-pg`, `@prisma/client`, `pg`) e três no `web/`, e gerar PDF significa um gerador ou HTML-para-impressão pelo browser — a segunda é zero dependência e imprime pelo diálogo do sistema, a primeira é anexável em e-mail; (d) **nada entrega o documento ao cliente.** Não há e-mail nem WhatsApp no financeiro, e a regra 4 impede usar os do CRM — o `PRD` §7.8 diz que a direção certa é *endpoint exposto pelo financeiro e consumido pelo CRM*. **E a faixa pagável continua dependendo do A1** (`Q-SICOOB-01`): até ele existir, o documento sai como **demonstrativo sem faixa**, e decidir se isso já serve para enviar ao cliente é a quinta pergunta. **Construído em 29/07, e nada disto dependia do certificado:** a aba **Cobrança** (o conector, pela `credencial_ref` — com trava que recusa segredo colado, regra 5), o caminho **fatura → emitir → boleto → baixa** na tela, e as três telas de **relatório com CSV**. Ver `web/src/cobranca-regras.ts` e as 35 verificações novas. <br>**AS CINCO FORAM DECIDIDAS EM 30/07 pelo dono, e duas contra a minha recomendação — o que fica registrado é a decisão, não a recomendação:** (1) **logo em `bytea` em tabela nossa** — recomendada, e com a restrição de **PNG/JPEG, nunca SVG**, porque a logo é embutida no HTML do documento e SVG executa script; (2) **layout configurável por tenant, na aba** — **contra a minha recomendação** de layout fixo. O custo que eu tinha nomeado (schema + tela + validação, para uma empresa só) foi aceito, e a mitigação está no schema: a lista de campos é **fechada por enum** (`campo_de_fatura`, 16 valores), então *"campo inexistente vira fatura errada"* deixou de ser possível — é o banco recusando, não código que alguém tem de lembrar de rodar; (3) **HTML agora, gerador de PDF depois** — recomendada: o mesmo template serve aos dois, e a decisão de dependência só vira obrigatória quando houver envio automático; (4) **entrega manual agora, com a rota do CRM já preparada** — o dono pediu as duas: baixar/imprimir hoje, e o caminho do `PRD` §7.8 (*endpoint exposto pelo financeiro, consumido pelo CRM*) estruturado desde já. **Consequência de desenho, e ela é a razão de a rota vir antes da tela:** a composição do documento **não pode nascer dentro do `.tsx`** — se nascer, publicá-la para o CRM depois é reescrever. Medido em 30/07 e é o que fecha a escolha do canal: **84 clientes têm telefone, 10 têm e-mail** (12%), e o WhatsApp já é operado no CRM; (5) **documento sai sem a faixa pagável, com QR Pix estático** — **contra a minha recomendação** de chave Pix em texto. Aceito o custo: implementar **BR Code** (EMV TLV + CRC16) e renderizar o QR. **O que não muda com a escolha:** sem `txid` por fatura o dinheiro chega sem dizer de quem é, então a conciliação continua **manual**, pela baixa da tela de Faturas — e é por isso que a baixa manual foi construída antes. **Construído em 30/07:** a **migration 19** (`identidade_de_cobranca`, `logo_de_cobranca`, `campo_do_documento`), aplicada em banco limpo, **e ainda não em produção**. **Dois invariantes do próprio repositório recusaram o meu primeiro desenho, e nas duas vezes o resultado ficou melhor:** o **inv. 17** recusou a logo sem gatilho de auditoria — virou **propagação**, um gatilho que carimba o `sha256` na tabela auditada, o que é mais forte do que eu queria, porque o hash passa a ser **derivado pelo banco** e o metadado não pode divergir do conteúdo; e o **`CAT-7`** recusou a FK simples que eu justifiquei como *"essa obviamente não atravessa tenant"* — virou **composta**, como as outras dez. Medição que decidiu a divisão em duas tabelas: **`to_jsonb` sobre `bytea` ocupa exatamente 2,00×**, então auditar a linha inteira poria 1,2 MB na trilha por troca de uma logo de 300 KB. **Aplicado em produção em 30/07:** a 19 e a **20** (esta última pela `Q-PRISMA11B-01`), `db pull` + `generate` refeitos, catálogo **8/8 contra produção** e `npm test` verde. **CONSTRUIDO em 30/07, na ordem que a decisão pediu:** `src/dominio/brcode.ts` (BR Code do Pix estático — EMV TLV + CRC16/CCITT-FALSE, **33 verificações**), `src/dominio/layout-do-documento.ts` (**22**), `src/repos/documento.ts`, **8 rotas novas** (78 → 86) e a aba **Documento** com a prévia imprimível. **A prévia É o documento:** ela pinta o retorno de `GET /faturas/:id/documento`, que é a mesma rota que o CRM vai consumir — se as duas divergissem, a prévia deixaria de ser conferência. `npm test` verde, 571 verificações. <br>**AS TRES FORAM FECHADAS EM 30/07, e as tres verificacoes que as fecham pegaram defeito real — o que segue e o registro disso, nao um aviso de conclusao.** (a) **O DESENHO DO QR EXISTE.** `src/dominio/qrcode.ts`: modo byte, Reed-Solomon sobre GF(256), oito mascaras com as quatro regras de penalidade do padrao, versoes 1 a 12 nos quatro niveis. O SVG e montado no **servidor** e entra no payload de `GET /faturas/:id/documento` — decisao 4 valendo para o desenho tambem: o CRM consome a mesma rota e nao roda React, e um QR desenhado no `.tsx` obrigaria o CRM a portar o codificador. **45 verificacoes, e nenhuma compara a saida com constante minha** — seria a tautologia que a sessao 14 pegou no teste do CRC. Sao: sindrome nula (usa a tabela do corpo, NAO a divisao polinomial que gerou a paridade), divisibilidade do BCH conferida por rotina de bits escrita de outro jeito, distancia de Hamming minima do codigo (formato 7, versao 8), total de codewords **derivado da geometria** contra as ancoras publicadas (v1=26, v2=44, v7=196), a paridade publicada do exemplo do ISO/IEC 18004 (`"01234567"` v1-M) com os codewords de dado **derivados a mao no comentario**, e ida-e-volta por decodificador separado que redescobre os modulos de funcao por **predicado geometrico** em vez de repetir os lacos de marcacao. **Dois defeitos que essas verificacoes pegaram, e nenhum apareceria em revisao de PR:** a informacao de formato repartia **8+7** em vez de **7+8**, e o bit 7 caia sobre o **MODULO ESCURO FIXO** em `(n-8, 8)`, apagando-o em metade das mascaras — sintoma: leitor que nao acha o codigo, porque aquele modulo e uma das ancoras que ele procura; e — este e o mais interessante — **a sindrome nula NAO identifica a contagem de paridade, so a limita por baixo**, porque o gerador de grau 18 e multiplo do de grau 11. Pior: o fluxo **inteiro** de um codigo de dois blocos tambem e codeword (`S(a^i) = 0` para `i <= 8`), entao `blocos=1, par=9` passava num codigo de `blocos=2, par=18`, cortava os dados no lugar errado e devolvia **texto errado com sindrome nula**. Era defeito da suite, corrigido escolhendo o `par` maximo — e e o mesmo modo de falha que este projeto persegue no resto: silencioso e plausivel. **O teto de versao 12 e nomeado e LEVANTA**: o pior BR Code que o `brcode.ts` consegue montar tem **243 bytes** e cabe na versao 11; truncar daria um QR legivel apontando para um Pix incompleto. O QR do Pix **do boleto** tambem vem desenhado — ele tem `txid` e concilia sozinho, entao desenhar so o pior dos dois seria estranho. **O que NAO muda com o desenho, e a tela continua dizendo:** Pix estatico nao carrega `txid` por fatura, e a conciliacao segue manual. (b) **O TESTE DE INTEGRACAO DO REPO EXISTE.** `tests/repos-documento.ts`, **46 verificacoes** no harness `tests/repos.sh`, pela role **sem `BYPASSRLS`**. Cobre o que so existe no BANCO e passaria verde quebrado num teste puro: o mime derivado da **assinatura do arquivo** (SVG e GIF recusados pelos bytes, e o SVG importa porque a logo e embutida no HTML), o `sha256` derivado pelo **gatilho** e conferido contra `node:crypto` — a regra 9 verificada e nao declarada —, a lista de campos fechada pelo **enum**, e o isolamento das seis leituras de `paraFatura` (fatura do tenant B e **404**). **Dois erros meus que a propria suite pegou durante a escrita:** `prisma.$queryRaw` dentro de `withTenantEm` roda **fora** da transacao, logo sem `SET LOCAL`, logo a RLS devolveu **zero** com a vigencia existindo — o modo de falha que o `ADR-0003` nomeia, cometido por mim no teste dele; e a fatura achada por **indice posicional** apontava para a de outra suite, porque `repos.sh` compartilha banco e tenant. A suite passou a afirmar a **relacao** (o documento repete a coluna gerada; o campo 54 do BR Code e esse total por TEXTO) em vez de um total constante: fixar 85.000 seria fixar uma dependencia de ordem de execucao com cara de conta conferida. (c) **A LOGO VAI NO PAYLOAD.** `GET /faturas/:id/documento?embutir_logo=1` devolve `logo.data_uri`. **OPT-IN de proposito:** base64 custa 33% a mais e a tela ja busca o binario por rota propria com o Bearer dela; quem precisa do embutido e o consumidor que **nao pode** fazer a segunda chamada autenticada, e esse e o CRM. **O que continua em aberto e nao e disto:** a autenticacao do CRM, que e a `Q-WEBHOOK-01`. <br>**O que ficou de fora em 30/07, e e o mesmo de sempre:** **nenhuma tela foi aberta** — nao ha browser neste ambiente. `tsc --noEmit`, `npm test` (**EXIT=0**) e as suites puras passam; o QR **nao foi lido por camera de celular**, e isso e teste de campo. As 46 + 45 verificacoes provam que a matriz e um QR valido pelo padrao e que o texto volta inteiro; nao provam que o aplicativo do banco o aceita. <br>**Registro historico — as tres pendencias, como estavam:** **Três coisas que ficaram de fora, e nenhuma é esquecimento:** (a) **o DESENHO do QR não é gerado.** O BR Code está completo, válido e paga por copia-e-cola; renderizar o quadrado exige um codificador (Reed-Solomon e máscaras) que não foi escrito, e desenhar um placeholder que parecesse um QR seria pior que dizer que falta. A tela diz; (b) **`src/repos/documento.ts` não tem teste de integração.** Os módulos puros têm 55 verificações novas, o repositório tem zero — as suítes de repo do projeto rodam por harness próprio (`tests/repos.sh`) e este não entrou nela. É dívida nomeada, não coberta; (c) **o documento servido ao CRM não carrega a logo embutida** — a tela a busca por `GET /cobranca/logo` com o Bearer, e um consumidor externo precisaria ou da mesma credencial ou de a logo vir em base64 no payload. Decidir junto com a autenticação do CRM, que é a mesma pergunta em aberto da `Q-WEBHOOK-01`. <br>**Medição que corrigiu o código durante a construção:** `valor_total_centavos` é `GENERATED ALWAYS` e **aceita nulo** — eu havia escrito, em comentário, que todas as colunas de dinheiro eram `NOT NULL` e que "zero ali é zero de verdade". Falso. Um total desconhecido imprimia "R$ 0,00", que é vazio virando número; agora sai "—", e a faixa de pagamento **recusa** montar QR sem valor em vez de gerar um em que o cliente digita a quantia | Vinicius |
| **Q-CRMCODIGO-01** | 🔴 | **`lead.codigo` do CRM NAO E ESTAVEL, e o mapa de atribuicao de originador ficou errado em duas linhas — descoberto ao reconferir o CRM em 30/07 antes de a operacao digitar os 39 contratos.** Medido pela role `financeiro_ro` e pelo conector de analise, 30/07 03:25–03:40Z, contra a medicao de 29/07 22:53Z. **O que aconteceu:** `financeiro.lead_merges` registra **76 merges em 30/07** (origem `merge_leads`; mais 2 em 29/07 e 1 de backfill em 10/07). Um merge escolhe um lead sobrevivente, e o `codigo` acompanha o sobrevivente. **Resultado medido:** de 7 codigos conferidos por amostra do mapa de 29/07, **5 nao existem mais** (`G3-0139`, `G3-0141`, `G3-0155`, `G3-0301`, `G3-0412`); das 41 linhas do rateio, **39 tem codigo diferente** com a MESMA UC e o MESMO cliente. E `financeiro.vendas_ganhas` caiu de **80 para 51** linhas, porque o `DISTINCT ON (l.id)` da view conta leads distintos e os merges colapsaram duplicatas. <br>**AS TRES CONSEQUENCIAS QUE MOVEM DINHEIRO, e sao o motivo desta entrada ser vermelha:** (a) **a lista de originadores caiu de tres nomes para DOIS** — `Jezielly Vieira` tem hoje **zero** cards como responsavel e **zero** como `vendedor_origem` no CRM inteiro; (b) **duas atribuicoes trocaram de dono**, e uma e grande: a UC `000406456101252` (4.60%, 496,8 kWh) passou de Jezielly para **Out Sales**, e a UC `000407359701237` (**18.40%, 1.987,2 kWh**) passou de **Renata** para **Out Sales**. Digitar contrato pelo mapa de 29/07 pagaria comissao a pessoa errada, **sem erro e sem log**, e nao ha caminho de conserto: a R20-b congela o tier no `rascunhar` e `contrato` nao tem edicao; (c) **o peso mudou** — Renata 80,8% -> **74,2%** (25 -> 24 UCs), Out Sales 15,9% -> **24,2%** (14 -> 16). O total da carteira NAO mudou: 29.896,2 kWh/mes nas duas medicoes, entao a carteira e a mesma e o que se moveu foi de quem ela e. <br>**O QUE FOI CONFERIDO E ESTA BEM, e importa dizer:** a sincronizacao do financeiro **nao usa `codigo` como chave** — `src/crm/sincronizacao.ts` casa `rateio_clientes` com `rateio_creditos` por **`contrato_id`** e espelha cliente por **`crm_lead_id`**, os dois UUID; `lead_codigo` aparece so na mensagem de uma recusa. Um `npm run ciclo` depois do merge nao duplica nem perde nada. **E o achado tem um lado bom, medido:** as **12** atribuicoes que a `Q-ORIGVEND-01` dizia serem invisiveis as 8 views ficaram **legiveis** (40 das 41 hoje), porque a linha do rateio passou a apontar para um lead sobrevivente COM posicao `won` — e **as 12 batem uma a uma** com o que o conector de analise havia dito em 29/07. Duas medicoes por caminhos diferentes, quatro dias e um merge de 76 leads no meio, concordando. As duas que mudaram eram de fonte `view`, nao `card`: **nao foi o metodo de 29/07 que errou, foi o dado que se moveu.** <br>**O que fica aberto, e tem dono:** (1) **o CRM vai renumerar de novo?** Se merge e rotina de operacao, todo documento que referencie `lead.codigo` tem prazo de validade, e ha varios no repositorio; (2) o desempate do dedup da R4 em `sincronizacao.ts` usa `codigo` (*"empate resolve por `codigo`"*) — so decide qual de N posicoes do MESMO `lead_id` vence quando `ganho_em` empata exatamente, e e deterministico dentro de uma execucao, mas **muda entre execucoes** se os codigos mudarem. Nao afeta cadastro; fica registrado para nao ficar por descobrir; (3) **cadastrar `Jezielly Vieira` mesmo com zero UCs?** Cadastrar nao paga nada — originador sem contrato nao entra em split. **Nao escolhi** (regra 10). <br>**Mitigacao ja aplicada:** o mapa novo e **`ATRIBUICAO-originador-2026-07-30.md`**, ordenado por **UC** (a chave estavel) e nao por codigo de lead; o de 29/07 recebeu cabecalho de SUPERADO com o de-para, corpo intacto. **A consulta que reproduz o mapa**, para reconferir imediatamente antes de digitar: `select coalesce(g.vendedor_origem,'(?)'), r.uc, r.cliente, r.percentual_rateio from financeiro.rateio_clientes r left join financeiro.vendas_ganhas g on g.codigo = r.lead_codigo order by 1, r.cliente;` pela `financeiro_ro`. **O mapa e uma foto de um CRM que se moveu duas vezes em quatro dias** | Vinicius + operação |
| **Q-ORIGVEND-01** | 🟡 | **21/08, fim do dia — O `tipo` FOI CONFIRMADO PELO DONO e a lista encolheu para DOIS.** Verbatim: *"Renata e Out Sales são tipo próprio"* — logo **`vendedor_g3`** para os dois, que paga **25% na 1ª e 25% na 2ª** fatura cheia. As dez regras de comissão já estão cadastradas e vigentes; nenhuma precisa nascer. **E a lista voltou de três para DOIS nomes**, medido no mesmo dia: restringindo às **29 UCs que o financeiro fatura**, o crédito vigente dá **Renata 26** e **Out Sales 3**; **Kallina Tandara tem 4 créditos e nenhum ativado**, então ela sai do caminho crítico sem sair do mapa. **O que continua aberto, e o dono declarou DEMANDA DE SEQUÊNCIA (não bloqueia a leitura da conta nem a geração de cobrança, só o contrato):** os documentos e a natureza. E um deles tem uma pergunta ANTES do número — **"Out Sales" é equipe, e equipe não tem CPF**: ou há um CNPJ por trás (natureza `pj`), ou há uma pessoa que responde pelos 3 contratos. Sem isso, **os 3 do Out Sales não nascem e os 26 da Renata nascem**. **Fora do cadastro e dentro do fiscal:** Renata concentra 26 das 29 e é sócia — o `Item 10` da §3 (*comissão a sócia é despesa dedutível ou distribuição de lucro?*) deixa de ser hipotético e passa a valer sobre 90% da carteira. Corpo anterior: **03/08: A "SEGUNDA ORDEM" CHEGOU, e o eixo deixou de ser o `vendedor_origem`.** A `Q-EIXO-FUNCIONARIO-01` fechou com a resposta do dev do CRM: o eixo e o **credito congelado** (`financeiro.vendas_creditadas`), e por ele o mapa de 30/07 esta errado em **12 das 41 UCs** — o Out Sales sai de **24,2% para 4,6%**, e **5.858,8 kWh/mes (19,6%)** trocam de dono. **Tres consequencias diretas para esta entrada:** (a) **a lista volta a TRES nomes** — Renata (33 UCs), Out Sales (7), **Kallina Tandara** (1) —, entao sao **tres** CPF/CNPJ a pedir e nao dois; (b) **o item (d) fecha**: a `000009997201253` (lead `G3-0154` arquivado), que nenhum caminho de leitura entregava, e da **Renata** pelo credito; (c) **o item da R16 esta DESMENTIDO** — esta entrada dizia que os dois eixos *"nunca discordam sobre o dado de hoje"* porque **zero** linhas do rateio tinham `partner_id`, e desde o recredito de 03/08 **tres tem**. Ver **`Q-PARCERIA-01`** 🔴. **O que continua aberto e e o mesmo de antes:** os documentos, a natureza `pf`/`pj` e a confirmacao do `tipo` — nenhuma das **10** views expoe documento de pessoa. **O que deixou de estar aberto:** a atribuicao por UC, que agora e legivel por view em **41 de 41** (`Q-VIEWSCRED-01`). **Mapa vigente: `ATRIBUICAO-originador-2026-08-03.md`.** Corpo original abaixo: **O eixo do originador é o `vendedor_origem`, decidido em 29/07 pelo dono — *"até segunda ordem"*. O que fica aberto é o insumo humano que nenhuma consulta entrega, e uma atribuição que o financeiro não consegue ler.** A decisão fecha a lista de originadores em **três nomes**, e não em três prováveis: `vendedor_origem` é `users.name` alcançado por `leads.vendedor_origem_user_id`, com FK — não é o custom field *"Nome do vendedor"* em texto livre que o `PRD` §7 registra como semente frágil. Medido em 29/07 22:50Z pela role `financeiro_ro`: dos 80 ganhos, **Renata 49, Out Sales 29, Jezielly Vieira 1, um nulo**. **A escolha do eixo move dinheiro, e a medição sustenta a do dono contra uma recomendação anterior:** o `RESUMO-SESSAO-3` §121 havia recomendado *"`responsavel` paga, `vendedor_origem` só registra"* — os dois eixos **divergem em 43 dos 80 ganhos**, e por `responsavel_atual` a Jezielly salta de 1 para 28. O motivo aparece nos **15 cards do funil `Rateio`**: o `responsavel` é `Jezielly Vieira` em **15 de 15**, com o `vendedor_origem` variando — ali `responsavel` é dono operacional do card, não quem vendeu. Pagar por aquele eixo pagaria 28 vendas a quem processou o cadastro. **A recomendação da sessão 3 fica superada por medição, não por preferência.** **A `SPEC-002` R16 foi conferida e não colide hoje:** ela manda a atribuição vir de `leads.partner_id`, e das 28 linhas do rateio que casam com um ganho **zero** têm `partner_id`; nos 80 ganhos **um** tem, é card de `Parceiros` (que a R14 já exclui) e **não tem `vendedor_origem`**. Os dois eixos nunca discordam sobre o dado de hoje. **O que fica aberto, e é o motivo desta entrada:** (a) **CPF/CNPJ dos três** — `documento` é `NOT NULL` e o dígito é conferido, e **não há de onde derivar**: nenhuma das 8 views expõe documento de usuário, e `financeiro.parceiros` tem 9 linhas sem coluna de documento, nenhuma delas sendo um dos três; (b) **natureza `pf`/`pj`** — o script recusa o par errado, e *"Out Sales tem cara de PJ"* é inferência minha, não medição; (c) **confirmação do `tipo`**, que a semente do CRM sugere como `vendedor_g3` para a Renata (`proprio`) e `terceirizado` para as outras duas — os dois totalizam 50% repartidos 25+25, então a escolha **não altera valor hoje**, mas **congela no contrato** pela R20-b; (d) **quem originou a G3-0154**, cujo lead está **arquivado** e por isso sai de toda consulta de card: nem a view nem o conector de análise entregam o `vendedor_origem` dela. **E a atribuição por UC não é derivável por código, o que é a parte contraintuitiva:** das 41 linhas do rateio, **28** têm a origem legível em `financeiro.vendas_ganhas`, **12** só existem em cards do funil `Rateio` em etapa `normal` — e a view expõe apenas `won`. A origem **existe no CRM** e é **invisível às 8 views**. Um derivador automático acertaria 28, erraria 12 em silêncio e pareceria completo, que é o modo de falha que a sessão 12 corrigiu na tela de Contratos. **O mapa das 41 está em `ATRIBUICAO-originador-2026-07-30.md`** — o de 29/07 foi **SUPERADO**, ver `Q-CRMCODIGO-01`: 76 merges de lead em 30/07 tiraram a `Jezielly Vieira` da lista (zero cards hoje), moveram duas atribuições para o Out Sales e renumeraram 39 dos 41 códigos de lead. **A lista de originadores é de DOIS nomes**, e as 12 atribuições que esta entrada dizia invisíveis ficaram legíveis pela view — e as 12 bateram. O mapa antigo — ele é o único portador das 12. **Peso, para dimensionar:** Renata **80,8%** do crédito kWh/mês (25 UCs), Out Sales 15,9% (14), Jezielly 1,7% (1), sem origem 1,7% (1). Renata é **sócia**, e a pergunta fiscal já tem resposta — `PAUTA` 7: a comissão dela **é comissão**, dedutível como as demais. **Nada foi escrito:** `originador` e `contrato` seguem em 0 | Vinicius + operação |
| **Q-DEPLOY-GENERATE-01** | ✅ | **SEGUNDO ACHADO, no proprio conserto:** `npx prisma generate` no VPS respondeu `PrismaConfigEnvError: Cannot resolve environment variable: DIRECT_URL`. O `.env` do servidor tem `DATABASE_URL` e nao tem `DIRECT_URL` — e **nunca precisou ter**, porque as migrations deste projeto sempre foram aplicadas de fora. O `prisma.config.ts` chamava `env('DIRECT_URL')` do `prisma/config`, que **levanta na carga do arquivo**, antes de o Prisma saber qual comando foi pedido. **Medido antes de consertar:** `generate` roda em 711 ms com `DIRECT_URL` apontando para uma porta morta — ele le o `schema.prisma` e escreve arquivos, nao disca. Corrigido: a lista de comandos que discam e por **inclusao** (`migrate`, `db`, `studio`, `debug`), entao subcomando novo do Prisma cai no lado seguro; `generate` recebe uma URL propositalmente ridicula (`invalido.local:1`) para que, se algum dia alguem tentar discar, a falha nomeie este arquivo em vez de parecer indisponibilidade do banco. **Cair para `DATABASE_URL` foi descartado e a razao importa:** ela aponta para a role de runtime, sem DDL — `migrate deploy` morreria no meio com "permission denied" numa tabela qualquer, que e pior que o erro original. `tests/prisma-config.ts` roda o **CLI de verdade** com um `.env` sem a variavel: importar o modulo daqui o executaria com o `.env` desta maquina, que e exatamente a condicao que nao reproduz o servidor. | **O `prisma generate` não estava no caminho de deploy, e isso quebrou a aba Documento EM PRODUÇÃO — achado pelo dono ao abrir o sistema pela primeira vez, em 30/07.** O sintoma foi `Cannot read properties of undefined (reading 'findFirst')`. A cadeia: (1) `src/generated/` está no `.gitignore`, então `git pull` não o traz; (2) **nada** no deploy rodava `prisma generate` — só o CI e as suítes, e `tests/repos.sh` só gerava quando o diretório *não existia*, nunca atualizando um obsoleto; (3) as migrations 19 e 20 entraram em produção trazendo `identidade_de_cobranca`, `logo_de_cobranca` e `campo_do_documento`; (4) o client do servidor continuou o de 28/07. `dbt().identidade_de_cobranca` era `undefined`. **A lição de método é maior que o defeito:** o deploy foi conferido dos dois lados e deu verde porque as rotas responderam **`401 TokenInvalido`** — e **401 prova que a rota existe e recusa credencial, não que ela funciona**; ela nem chega ao banco. As três rotas estavam quebradas atrás daquele 401. **Resolvida com guarda de arranque, não com passo no procedimento:** `iniciar()` compara as tabelas de `public` com os modelos do client e **recusa subir**, nomeando as que faltam e imprimindo o comando que conserta — mesma forma da guarda que já recusa role com `BYPASSRLS`. Um passo a mais dependeria de alguém lembrar, e a regra 11 já disse o que acha disso. `tests/app.ts` `A9a`–`A9c`, com plantio que prova que morde; `tests/repos.sh` passou a gerar sempre; o `README` ganhou o passo e o quadro | — |
| **Q-AGENDA-02** | 🟢 | **Os quatro números da agenda que o `PRD` não dá, e é confirmação, não decisão de projeto.** O `PRD` §6 pede "retry exponencial" e "consulta ativa **diária**" e não diz base, teto, prazo de aviso do A1 nem teto de itens por rodada. Pela regra 10 isso é lacuna, e lacuna não vira default escolhido por quem implementa sem dono nomeado — então os quatro estão num objeto só (`POLITICA`, em `src/dominio/agenda.ts`), com o raciocínio de cada um escrito ao lado, e **nenhum teste depende deles**: as suítes usam política artificial de propósito, o que torna trocá-los uma edição de quatro números. Escolhidos: **base 300 s** (falha de transporte que se resolve sozinha se resolve em minutos; menos que isso é martelar o banco durante uma indisponibilidade dele), **teto 21.600 s** (quatro tentativas por dia no regime lento, mesma ordem de grandeza da consulta diária), **aviso do A1 em 30 dias** (renovar A1 tem processo e assinatura, não é um clique) e **200 itens por rodada** (para uma rodada não virar janela de horas — e o que sobra é **contado e impresso**, nunca truncado em silêncio). **O que NÃO é escolha e não deve mudar sem conversa:** a fila **não desiste sozinha**. Não há teto de tentativas; o teto é do *intervalo*. Um boleto que sai da fila por contagem para de ser cobrado sem que ninguém tenha decidido parar, e do ponto de vista do sistema a fila fica limpa — que é a falha silenciosa que a agenda existe para fechar, cometida pela agenda | Vinicius |
| **Q-TARIFA-CONC-01** | 🟡 | **07/08/2026 — A PERGUNTA (b) GANHOU CAMINHO, e a (a) continua intacta.** A (b) era *"quem produz a planilha da Equatorial e até quando ela chega?"*, e a resposta desde 30/07 é **ninguém**: são 29 leituras manuais por mês, que é o tipo de trabalho que ninguém faz na segunda vez. A frente de **leitura automática da fatura** (`PLANO-leitura-fatura-equatorial-2026-08-07.md`) é a resposta desenhada para ela — porta de coleta, porta de leitura, domínio puro e adaptadores falsos **já construídos e provados** (42 verificações), com o coletor real faseado atrás de uma medição do portal que é do dono. **Ela NÃO responde a (a)** — *se* a competência leva tarifa da concessionária ou sai só com o crédito continua sendo decisão sua, e nenhuma automação a substitui. **E a frente trouxe uma vermelha nova que é da mesma família desta:** a `Q-EQTL-CAMPOS-01` — a coluna é **fio B + iluminação pública + encargos**, e o **total a pagar** da fatura da Equatorial é outro número, na mesma página, igualmente plausível. Corpo de 06/08 abaixo: **A AUSENCIA DEIXOU DE SER MUDA, e so isso: as duas perguntas continuam abertas e continuam sendo da operacao.** A tela de Faturas e o `npm run faturar -- --valendo` agora CONTAM quantos rascunhos sairiam sem tarifa da concessionaria, nomeiam as UCs e dizem a ordem dos tres passos (`web/src/cobranca-regras.ts` -> `conferirTarifas`, 7 verificacoes `B10`). **Isto conta e NAO decide** (regra 10): zero pode ser o valor certo da competencia, e essa e a pergunta (a). Nao ha trava, nao ha recusa, e `podeEmitirFatura` continua olhando so o status — a verificacao `B10f` prende isso de proposito. O que mudou e que o silencio virou numero, que e o que a `prontidao` faz com as onze camadas. Corpo de 06/08 abaixo: **REABERTA NA PARTE OPERACIONAL, e o que apareceu nao e um dado que falta: e uma ORDEM que nenhum documento diz.** Achado percorrendo o caminho da primeira fatura ponta a ponta. `valor_total_centavos` e coluna GERADA (`schema.prisma:485`): `valor_consumo + valor_tarifas_concessionaria + valor_juros_multa`. O segundo tem **default 0**, e `comporLote` usa zero quando ninguem informa (`src/repos/fatura.ts:174`) — entao o lote de 28 faturas **compoe e emite sem erro nenhum** cobrando **so o credito**, sem as tarifas da distribuidora. **E `lancarTarifasPorUC` so aceita RASCUNHO**, o que fixa a sequencia em tres passos e nao dois: `compor` → `npm run tarifas` → `emitir`. Emitir antes obriga a **cancelar a fatura** para corrigir, e cancelamento exige motivo e fica na trilha. **Nem a fila de `PENDENCIAS-2026-08-05` nem o "caminho mais curto" da `RESUMO-SESSAO-21` §7 tem esse passo** — os dois vao de "compor o lote" direto para a competencia. **O que precisa de resposta, e nao e codigo:** (a) as faturas de **2026-06** levam tarifa de concessionaria ou saem so com o credito? (b) se levam, quem produz a planilha da Equatorial e ate quando ela chega? O item (b) e o que a propria entrada de 30/07 ja dizia estar aberto — o que 06/08 acrescenta e que **a ausencia dele nao aparece como recusa**, aparece como fatura de valor menor. Corpo de 30/07 abaixo: **RESOLVIDA em 30/07/2026 na parte que era codigo.** Havia coluna e havia rota, e a rota pede o **id da fatura** — que ninguem tem na mao. "Por planilha" (`PRD` §5.1) significava, na pratica, abrir uma fatura por vez na tela. Agora: `src/dominio/planilha-tarifas.ts` (puro) le o CSV, `fatura.lancarTarifasPorUC` casa por **`numero_uc`** e `npm run tarifas` compoe, com `--ensaio`/`--valendo`. **O modo de falha perseguido nao e o arquivo ilegivel** — esse aparece — **e o arquivo que le CERTO o numero ERRADO**: "1.234" e R$ 1.234,00 ou R$ 1,23, e as duas leituras produzem fatura plausivel. Por isso o ensaio imprime a INTERPRETACAO ao lado do texto original. O leitor nao adivinha: nao pula linha que nao entende, nao soma UC repetida e nao assume zero para celula vazia — **ausente nao e zero**, e a fatura que a planilha nao cobriu aparece numa lista em vez de ser zerada. **36 verificacoes.** **O que NAO fecha com isto:** quem produz a planilha e com que periodicidade continua sendo processo humano sem dono nomeado. Corpo original abaixo: **Quem lança `valor_tarifas_concessionaria_centavos`, por competência, e em que formato?** O `PRD` §5.1 diz que na v1 os dados da Equatorial entram *"por planilha ou lançamento manual"* e que não existe API pública da distribuidora. A coluna existe e a rota também (`PUT /faturas/:id/tarifas-concessionaria`, só em rascunho), mas **não há importador**: hoje o valor entra fatura a fatura. Com 35 UCs isso é 35 lançamentos por mês. **Decidir:** o formato do arquivo, quem o produz e se a conferência é por UC ou por total do lote | operação |
| **Q-ESTORNO-01** | 🟡 | **Não existe caminho para reverter uma liquidação, e a ausência é deliberada.** A `SPEC-003` R46 recusa cancelar fatura já liquidada: desfazer o documento sem desfazer o caixa deixaria dinheiro sem título. Mas reverter a entrada — devolução, pagamento em duplicidade, baixa manual errada — é ato real e não tem implementação. **Depende da PAUTA 1, e agora com resposta:** competência governa a receita, caixa governa o split. Então estornar toca três coisas ao mesmo tempo — a receita da competência, o caixa e os `split_item` já pagos ao dono da usina e ao originador. **Decidir:** (a) estorno como execução de split NEGATIVA, que preserva o histórico e faz a soma continuar fechando; (b) linha de reversão em tabela própria; (c) só por correção manual com trilha, enquanto o volume for baixo | Vinicius + contador |
| F-04 | 🟡 | Conector lê participação no funil ou etapa dentro dele? | Vinicius |
| AUD-04 | 🟡 | Como o financeiro sabe que a competência está fechada? | Vinicius + operação |
| **Q-GERACAO-01** | 🟡 | **Tres das quatro usinas nao tem geracao lancada na competencia corrente, e isso limita o primeiro lote a menos da metade da carteira — medido em 30/07, e ninguem tinha nomeado.** Por usina, sem o join que multiplicava: `0001` tem **20 UCs** e geracao so ate **2026-06**; `0002` tem **14 UCs** e geracao ate **2026-07**; `0003` (1 UC) e `04` (4 UCs) **nao tem geracao nenhuma**. Consequencia: **em 2026-07 apenas 14 das 39 UCs teriam geracao**, e as outras 25 seriam recusa contada `sem_geracao_lancada`. **A competencia mais completa e 2026-06, com 34.** Isso NAO e defeito: a base de faturamento e a geracao MEDIDA (`PAUTA-contador` 9a), entao faturar sem ela seria emitir receita sobre energia que ninguem registrou ter sido gerada — e o `0003` e o caso que a `EXPLICACAO` do proprio codigo cita, com 100% alocado e zero geracao. **O que decidir:** quem lanca a geracao e com que periodicidade. Enquanto nao houver, escolher a competencia do primeiro lote e escolher quantas UCs entram | operacao |
| **Q-PECA-NAO-PLUGADA-01** | 🟢 | **06/08/2026 — QUARTO CASO, e ele estava no caminho da primeira fatura.** `PUT /cobranca/chaves-pix/:id` existe desde a migration 25 e tinha **zero chamadores**: a aba Documento so CADASTRA chave, e nenhum script alcancava a rota. A `RETOMADA-2026-08-06` §2 dizia que alinhar o nome do recebedor ao do DICT *"custa um comando"* — **e o comando nao existia**. Reexecutar o cadastro tambem nao serve: `chave_pix_chave_unica` recusa a mesma chave, entao a unica saida seria `UPDATE` a mao, por fora do contexto de tenant e da conferencia de chave. A varredura barata que a entrada original prescreveu **funcionou** — foi assim que este apareceu, procurando o chamador de uma rota citada como pronta. Plugado em `npm run identidade -- --editar`, com `--ensaio`, diff campo a campo e o BR Code regerado; `editarChavePix` tambem **nao tinha teste** e ganhou quatro (`W1j`-`W1m`), entre eles o sentido perigoso: editar sem devolver `titular_*` e `observacao` **apaga os tres**, porque o contrato do repositorio e "a linha inteira". **O que ainda NAO esta plugado:** a aba Documento continua sem formulario de edicao — corrigir digitacao pela tela e impossivel, e o caminho e o script. Corpo original abaixo: **Padrao a procurar, e nao uma questao a decidir: peca pronta que ninguem plugou.** Tres casos na mesma tarde de 30/07, todos com o mesmo formato — o comentario descreve a intencao, o codigo a implementa pela metade, e **nada falha**: (a) `ErroDaApi.ehDeSessao`, getter escrito com o comentario *"401 e 403 pedem acao diferente de 422"* e **nenhuma chamada** — o 401 virava texto vermelho em cada painel; (b) o `motivo` do `TokenInvalido`, montado em `jwt.ts` com o comentario *"para o log"* e **nunca logado**, porque 401 tem `status` e `ehInesperado` devolve false; (c) `boleto.tentativas`, incrementada desde a migration 16 e **nunca consumida**, ate a agenda existir. Os tres foram consertados. Fica registrado porque a varredura barata — procurar comentario que promete comportamento e conferir se alguem chama — provavelmente encontra mais | — |
| **Q-SPEC001-02** | ✅ | **24/08/2026 — A RESPOSTA CONTINUA VALENDO E O PESO DELA CAIU, e isto e consequencia da `Q-CICLO-01`.** O vencimento do caminho oficial vem **impresso na conta da distribuidora**; o dia do cadastro, que esta questao decidiu, virou a **SEGUNDA** fonte - vale quando a conta vier sem data. A prontidao foi remedida para a mesma regra de `vencimentoEscolhido` e a camada passou de `pendente 29 de 29` para `nao medido 0 de 0`: sem conta lida a fonte principal nao chegou, e dizer `pendente` ali mandaria preencher o que a conta traz de graca. **O que NAO mudou:** continua sem default e vai continuar - escolher um dia aqui seria o improviso que a regra 10 proibe, e o importador `npm run vencimentos` continua sendo o caminho de lote quando ele for preciso. Provado em `npm run ensaio-prontidao` (`P6` e `P7`), que apaga a data da conta e preenche o dia do cadastro dentro de uma transacao com ROLLBACK. Corpo original abaixo: **RESOLVIDA em 03/08/2026 pelo dono: o vencimento VARIA por cliente/UC, e quem preenche e a operacao do financeiro.** A pergunta registrada era *"por UC ou por contrato?"*, e a medicao de 30/07 ja tinha mostrado que essa escolha nao existia — o campo so existe na UC e so o DIA e usado. A resposta do dono fecha a outra metade: **nao ha um dia unico**, entao nao e um `UPDATE`, e sim uma planilha. Construido em 03/08: `src/dominio/planilha-vencimentos.ts` (puro), `unidade_consumidora.lancarVencimentosPorUC` e `npm run vencimentos`, com `--ensaio` imprimindo a interpretacao de cada dia ao lado do texto original. <br>**E a decisao expos um defeito que estava em producao e nao tinha nada a ver com a pergunta.** Ao procurar onde o valor seria gravado, apareceu quem o APAGA: `src/crm/sincronizacao.ts` levava `data_vencimento` dentro do objeto `espelho` ate o `updateMany`, e o CRM tem a coluna **vazia em 41 de 41 linhas** de `financeiro.rateio_clientes` (medido em 03/08). **Preencher as 39 UCs e rodar `npm run ciclo` apagaria as 39** — sem erro, sem log e sem recusa, e o sintoma chegaria um passo adiante apontando para o lugar errado: a composicao do lote recusaria tudo por `sem_vencimento`, e o motivo pareceria *"a operacao nao preencheu"*. Corrigido pela **R25 da `SPEC-002`**: campo local, o usuario vence (R5), e divergencia vira sinal em `conector_execucao.detalhe` como na R21-b. Testes `N55` e `N56`, e **o `N55` falha contra o codigo anterior** — e o registro executavel do defeito. `SPEC-001` §3.3 reclassificou o campo. **O que esta entrada NAO fecha:** a ordem das recusas continua valendo, e preencher o vencimento **antes** dos contratos e o que evita a segunda passada nas 39. Corpo original abaixo: **MEDIDA EM 30/07 E MENOR DO QUE PARECIA — subiu para vermelha porque e o bloqueio DESTRAVAVEL do faturamento.** A pergunta registrada era *"quem preenche `data_vencimento`, por UC ou por contrato?"*, o que soa como decisao de modelagem. **Nao e.** Lendo `src/dominio/faturamento.ts`: a triagem recusa com `sem_vencimento` se o campo for nulo, e o valor entra em `vencimentoDaFatura(competencia, data_vencimento.getUTCDate())` — ou seja, **so o DIA DO MES e usado**, e o campo existe **apenas na UC**: nao ha equivalente em `contrato`. Nao ha duas opcoes a escolher. O que falta e um dado: **em que dia do mes cada UC vence**. Se for o mesmo dia para todas, e um UPDATE; se variar, e uma coluna de planilha. **A ORDEM IMPORTA e esta medida:** a triagem recusa na ordem `sem_contrato_vigente` -> `ja_faturada` -> `sem_rateio` -> `sem_geracao_lancada` -> `sem_vencimento`, e mostra so o PRIMEIRO motivo. Com 0 contratos, preencher o vencimento hoje **nao muda uma linha** do resultado; e digitar os contratos SEM a data faz as 39 cairem na quinta recusa, e o trabalho volta para as 39 UCs. Preencher **antes** dos contratos e o que evita a segunda passada. Estado em producao, 30/07: **0 de 39** UCs com data. | Vinicius + operacao |
| **Q-UCMUDOU-01** | 🟡 | **RESPONDIDA em 03/08 pelo dev do CRM na opcao (a): foi CORRECAO DE DIGITACAO, uma coisa so, e nao dois fatos.** Prova documental do lado deles: em 14/07 dois contratos nasceram com a mesma UC `000041446801282`; o certo e o da **Renata Lucy** (`G3-0311`, contrato `74bb7b2d-…`, gravado 14/07 15:23 e nunca alterado), e o do **Fernando Albino** recebeu a UC dela por engano. Corrigido em **29/07 14:27:30**, com backup do estado anterior em `backup_uc_dedup_fix_20260729` (contexto `uc_trocada`). **Nossa linha se corrige, e nao vira duas.** Conferido daqui nos dois sentidos em 03/08 pela `financeiro_ro`, e a leitura bate: o contrato `524a4866` serve hoje a `000307301401201` e a UC `000041446801282` e servida pelo `74bb7b2d`. **E apareceu um detalhe que o dev nao mencionou:** o cliente da nossa linha e o lead `251ec351-…`, que e **vitima de merge** — `financeiro.lead_merges` mostra que foi absorvido pelo `3478ec41-…` (G3-0080) em 30/07. **CAI PARA AMARELA porque deixou de ser pergunta e virou execucao pendente.** Construido em 03/08: **`npm run destravar-uc`** (`scripts/destravar-uc.ts`), que apaga **uma coluna so** — `crm_usina_cliente_id` da UC nomeada — e deixa o conector escrever o vinculo novo no ciclo seguinte, pelo caminho normal, com contagem e trilha. **A R23 nao muda e nao foi afrouxada:** o conector continua recusando mover vinculo, porque a informacao que desempata e externa (adjudicacao de quem opera o CRM). O script **le o CRM antes de escrever** e tem **quatro guardas** — o ponteiro existe; o contrato se moveu; ha substituto para a UC; o substituto nao esta preso a outra UC nossa —, e recusa se qualquer uma falhar. Provado nos dois sentidos em 03/08: ensaio na `000041446801282` passa e nomeia as duas pontas; ensaio na `000059133001226` (sadia) **recusa**. **Falta so o `--valendo` e um ciclo** — e a mesma rodada traz a `000307301401201` e a `000295713501257`. Nada digitado: `contrato` e `originador` seguem em 0. Corpo original abaixo: **Uma atribuicao do nosso espelho esta ERRADA agora, e o conector nao a corrige de proposito.** Achado em 03/08 ao responder o dev do CRM, e medido nos dois lados. O contrato de rateio `524a4866-be99-4803-a203-58691d506654` **mudou de UC** no CRM: espelhamos em 28/07 apontando para `000041446801282` (FERNANDO ALBINO - CARTEIRA LOURIVAL) e hoje ele aponta para `000307301401201` (mesmo cliente). E a UC `000041446801282` passou a pertencer a **outro** contrato — `74bb7b2d-…`, lead `G3-0311`, **Renata Lucy Nogueira Drumond Teles Leaonilton**. **Consequencia hoje:** o espelho tem `000041446801282` atribuida ao **Fernando Albino** e o CRM diz que ela e da **Renata Lucy**. Digitar contrato por essa linha creditaria comissao a pessoa errada, **sem erro e sem log** — e a R20-b congela o tier no `rascunhar`, sem edicao. **O conector NAO conserta, e e decisao registrada (R23 da `SPEC-002`):** mover vinculo de contrato entre UCs seria escolher qual das duas leituras vale. Vira **recusa contada** — provado por `npm run ciclo --ensaio` em 03/08: `criados 3, atualizados 66, recusados 1`, e a recusa nomeia as duas UCs. **Efeito colateral medido:** e por isso que a UC `000307301401201` nao entra no espelho nem depois de um ciclo novo — ela e uma das duas linhas da diferenca 39 → 41, e a outra (`000295713501257`, Marlon Estevam de Sousa) e so atraso e entra sozinha. **Decidir, e a pergunta esta no `RESPOSTA-dev-crm-rodada5-2026-08-03.md` §3:** foi (a) **correcao de digitacao** — a UC do Fernando estava errada e o CRM consertou, e entao a nossa linha antiga se corrige —, ou (b) **dois fatos distintos** — o Fernando mudou de UC E a `000041446801282` foi para a Renata, e entao a nossa linha vira duas? **Nao ha caminho automatico para nenhuma das duas**, e nada foi tocado: `contrato` e `originador` seguem em 0 | Vinicius + dev do CRM |
| ~~**Q-VIEWSCRED-01**~~ | ✅ | **FECHADA EM 03/08/2026, e construida no mesmo dia.** As duas views entraram na lista fechada de `src/crm/conexao.ts` (que passa de **oito para dez**), ganharam leitor nomeado em `leitura.ts` com as colunas MEDIDAS - a entrada original listava 11 colunas de `vendas_creditadas` e a view tem **27**, entre elas `uc`, que e a ponte com o espelho, e `vendedor_user_id` e `parceiro_id`, que sao chave e nao nome -, e o ciclo passou a **conferir** o eixo do originador pela **R26** da `SPEC-002` v1.6. **Medido antes de construir: 39 das nossas 39 UCs espelhadas tem credito vigente**, e as 5 que sobram das 44 sao as que o destrave e o atraso de ciclo explicam - o eixo e legivel por view em 39 de 39 hoje e em 41 de 41 depois do destrave. **O conector NAO escreve originador, e isso virou o invariante 14**: `contrato.originador_id` e campo local (R5) e a R20-b congela o tipo no `rascunhar` sem edicao, entao divergencia vira SINAL em `conector_execucao.detalhe` - cinco condicoes, **todas medidas contra o CRM real antes de escritas e todas mudas contra o estado de hoje**, que e o criterio de ruido que a R25 fixou. **A causa raiz nao era o conector, e ela ficou consertada junto:** as views existiam desde 01/08 e **nada deste lado comparava o que o CRM expoe contra a nossa lista fechada** - `scripts/ciclo-crm.ts` imprimia "views legiveis: 10" numa linha que ninguem confronta com o 8. `conferirRoleDeLeitura` passa a devolver `viewsNovasNoCrm` e `viewsAusentes`, e o script grita as duas: contagem que ninguem confere nao e medicao. **Abriu a `Q-SITUACAO-01` 🟡.** 27 verificacoes puras (`tests/credito-originador.ts`) mais `N57`-`N62` com banco. Corpo original abaixo: **O CRM tem 10 views e o conector conhece 8. As duas que faltam sao exatamente as que respondem "quem vendeu" e "qual a situacao" — e o dev disse que elas nao existiam.** Medido em 03/08 pela `financeiro_ro`: `information_schema.columns` lista **10** views em `financeiro`, e `VIEWS_DO_CRM` em `src/crm/conexao.ts` lista **8**. As duas novas sao **`financeiro.vendas_creditadas`** (48 linhas — 45 vigentes, 3 revogadas; traz `vendedor`, `responsavel_origem`, `parceiro_nome`, `parceria_tipo`, `comissao_pct`, `vendedor_nome_ficha`, `divergencia_ficha`, `ganho_em`, `origem_carimbo`, `vigente`, `revogado_em`, `revogado_motivo`) e **`financeiro.rateio_situacao`** (41 linhas — `etapa_rateio`, `stage_type`, `situacao`, `em_troca_titularidade`, `na_etapa_desde`). **A resposta Q4b do dev — *"nao esta em view nenhuma… mesmo com o nome, leriam 0 linhas"* — esta vencida**, e o mecanismo pelo qual a leitura funciona e o da **regra 3**: por padrao a RLS das tabelas base e avaliada contra o **dono da view**, nao contra quem consulta, e nenhuma das 10 declara `security_invoker` (conferido em `pg_class.reloptions`: as 10 vem sem opcoes). Para nos esta seguro — as 10 filtram por `crm_tenant_id` e **as 3 conferidas devolvem 1 tenant so** —, mas e o mesmo mecanismo que a regra 3 mede como anulador de `FORCE ROW LEVEL SECURITY`, e esta avisado na Parte 1 da `RESPOSTA-dev-crm-rodada6`. **A consequencia para nos, e e o motivo desta entrada ser vermelha:** o eixo do originador agora e **legivel por view em 41 de 41**, e o conector **nao le**. Enquanto nao ler, toda digitacao sai de documento, e documento envelhece — que e literalmente a licao da `Q-CRMCODIGO-01`, so que desta vez o conserto e nosso e nao depende de terceiro. **O que construir:** as duas views entram em `VIEWS_DO_CRM`, o ciclo passa a ler o credito por UC, e **divergencia entre o originador digitado e o credito vigente vira sinal** em `conector_execucao.detalhe` — o desenho da R21-b/R25, que nao recusa e nao sobrescreve. Isso cobre de graca o aviso de revogacao que o dev ofereceu construir: `vigente` e `revogado_em` **ja estao na view**. **Nao construido nesta sessao** — e regra de `SPEC-002` nova, com teste obrigatorio nomeado (regra 8), e a sessao acabou no mapa | Vinicius |
| ~~**Q-SITUACAO-01**~~ | ✅ | **FECHADA EM 04/08/2026 pelo dono, e construida no mesmo dia: o financeiro fatura as ATIVADAS.** Das tres opcoes, a escolhida foi a (c) na forma de recusa nomeada: a UC continua espelhada e visivel nos cadastros - some-la encolheria a carteira sem ninguem ter cancelado nada -, e o LOTE a recusa por **`rateio_nao_ativado`**, na mesma lista dos outros cinco motivos. **Migration 24**: `rateio_situacao`, `rateio_em_troca_titularidade` e `rateio_situacao_lida_em` em `unidade_consumidora`, campo ESPELHO que so o conector escreve. **A opcao (a) e a (b) que eu apresentei como alternativas nao eram** - a triagem e pura sobre os nossos dados e a situacao vive no CRM, entao QUALQUER das duas precisava da coluna; o que mudava era o comportamento, nao o armazenamento. A imprecisao foi minha e esta registrada. **A posicao na ordem da triagem foi escolhida:** depois de `sem_contrato_vigente` e `ja_faturada`, antes de `sem_rateio` - dizer "falta geracao" numa UC que nao e para faturar mandaria a operacao trabalhar a toa, e a R20-b congela o tier no `rascunhar` sem volta. **`NULL` nao e "nao ativado"**: significa que o conector nao leu, e cai na mesma recusa porque o lado seguro para dinheiro e nao cobrar o que nao se confirmou - o que separa as duas e a EXPLICACAO, porque uma pede rodar o ciclo e a outra pede falar com o CRM. A coluna e `text` e nao enum: o vocabulario e DELES, e um estado novo quebraria um enum no meio do ciclo. **E a regra so alcanca UC ESPELHADA**, o que impediu um defeito de entrar: `POST /unidades-consumidoras` cria UC a mao, e sem essa condicao ela ficaria nao faturavel PARA SEMPRE, sem erro e sem log - regra de fonte externa aplicada a quem nao vem dela. O buraco apareceu porque QUATRO suites quebraram de uma vez, todas criando UC pelo caminho da tela. `SPEC-003` R32, testes `F4h`-`F4m` e `K2d`. Corpo original abaixo: **O rateio tem situacao desde 03/08, e 11 das nossas 39 UCs nao estao ativadas - o conector espelha as 39 como se fossem iguais.** Ate 03/08 nao havia coluna nenhuma que dissesse a situacao do contrato de rateio, e por isso **toda linha de `financeiro.rateio_clientes` era lida como valida** - foi exatamente o pedido da §4 da `RESPOSTA-dev-crm-rodada5`, e ele veio atendido em `financeiro.rateio_situacao`. **Medido no dia, pela `financeiro_ro`, sobre as UCs que JA espelhamos: 28 `ativado`, 11 `nao_ativado` - e 7 destas 11 estao `em_troca_titularidade`.** (Sobre as 41 linhas da view: 29 ativado/`won`, 12 nao_ativado/`normal`.) **A pergunta:** o conector deve (a) continuar espelhando tudo e apenas CONTAR - o que foi construido em 03/08, `situacao_do_rateio` no `conector_execucao.detalhe` -, (b) recusar linha nao ativada como recusa contada, ou (c) espelhar e marcar a UC com a situacao, deixando a triagem de faturamento decidir? **Por que nao foi decidido agora:** a (b) tiraria 11 UCs da carteira espelhada e a (c) e coluna nova com migration - as duas sao decisao de negocio com dono, e a regra 10 manda registrar e parar. **O que ja existe e nao espera a decisao:** a **R26** emite sinal quando um contrato **ativo** cobre um rateio nao ativado ou em troca de titularidade. Hoje isso dispara **zero** vezes, porque `contrato` esta em 0 - **e a decisao vale mais barata agora do que depois de 39 contratos digitados**. **O que a troca de titularidade encosta, e nao e o mesmo assunto:** `rateio_clientes.troca_titularidade` vem NULL em 41 de 41 (a coluna velha), e a nova `em_troca_titularidade` vem `true` em 7 - a `SPEC-001` §11 registra que guardamos so estado corrente da UC, sem historico de titularidade | Vinicius + operacao |
| **Q-CLIENTEDUP-01** | 🔴 | **04/08, tarde — a frase "o faturamento NAO e afetado" estava errada, e o custo sao 5 das 29 UCs.** Ela valia enquanto o documento fosse so o pagador do boleto. Com a R9 medida (ver `Q-PAGADOR-01`), `contrato.ativar()` exige `documento_validado` **do cliente daquela UC** — e `cliente_documento_unico` so permite o CPF em **uma** das duas linhas da mesma pessoa. Medido sobre as 29 UCs faturaveis: **29 linhas de cliente para 24 nomes distintos**, e as **cinco** duplicadas — ATAIDE DE MELO OLIVEIRA - DANIELA/LOURIVAL, GABRIELLA VIEIRA DORNELAS DE MELO RODRIGUES, RENATA FERREIRA ESTEVAM, RENATA LUCY NOGUEIRA DRUMOND TELES LEAO/MINEIRO e THIAGO GONCALVES TAQUARY — tem **nome e telefone identicos** nas duas linhas e **uma UC faturavel em cada**. Consequencia: preenchidos os 24 CPFs possiveis, **5 UCs ficam com cliente sem documento, o contrato nao ativa e elas nao faturam** — o lote sai com 24 das 29. Deixou de ser *"trabalho que trava no meio"* e passou a ser **receita que nao sai ate a decisao**. **O importador nao decide por ninguem:** `npm run documentos` recusa o lote INTEIRO nomeando as duas linhas de cada colisao (`D4a`–`D4d`) em vez de gravar metade — as duas leituras (mesma pessoa duplicada / CPF na linha errada) produzem um arquivo plausivel, e escolher ali seria escolher em silencio. **A opcao (c) segue sendo a unica que conserta na origem**, e ela e uma das tres perguntas da `RESPOSTA-dev-crm-rodada6`. **Corpo de 04/08 pela manha abaixo:** **REMEDIDA EM 04/08, depois do ciclo, e o numero grande esconde quatro coisas diferentes — so uma e defeito.** O dono perguntou por que ha 86 clientes para 29 UCs ativadas. Medido: das **86 linhas**, **41 estao INATIVAS** (as vitimas de merge de 30/07, fundidas pelo ciclo de 04/08 — sao historico, a R6 proibe deletar, e nenhuma tem UC); das **45 ativas**, **29 estao em UC ATIVADA**, **12 em UC nao ativada** (rateio existe no CRM e nao foi ativado — `Q-SITUACAO-01`) e **4 nao tem UC** (tres do Carlos Gabriel, cujas 3 UCs o proprio dev disse que ainda nao foram alocadas, e uma de `Vendas - Integracao`). **O defeito e so o ultimo pedaco: 45 linhas ativas para 36 nomes distintos, e mesmo entre as 29 de UC ativada sao 24 nomes.** **A CAUSA E ESTRUTURAL E NAO E ERRO DO CRM:** o CRM cria **um lead por contrato**, o conector cria **um cliente por lead** (decisao F-01 de 28/07, *espelho fiel*), e uma pessoa com duas UCs vira duas linhas. Medido no dia, sao **sete pessoas com duas linhas cada** — ATAIDE, CARLA GONZAGA, GABRIELLA, Hermani, RENATA FERREIRA, RENATA LUCY e THIAGO, cada uma com duas UCs distintas — mais o Carlos Gabriel com tres. **O dano e concreto e e um so: nove linhas nao podem receber CPF**, porque `cliente` tem `UNIQUE (tenant_id, documento)` e a segunda linha da mesma pessoa colide com 23505 no meio da digitacao. O faturamento NAO e afetado — a fatura segue a UC e o contrato. **E o conserto converge com a §4.1 da `RESPOSTA-dev-crm-rodada6`:** o nosso modelo ja e "um cliente, N UCs" (`unidade_consumidora.cliente_id`), e o que falta para o conector consolidar por PESSOA e uma chave de pessoa — **`documento` numa view**. A mesma coluna que destrava o pagador do boleto destrava isto. Sem ela, as alternativas sao todas ruins: consolidar por NOME (nome nao e chave), pedir ao CRM que funda leads que sao contratos legitimamente distintos, ou largar o `UNIQUE` do documento, que destroi a garantia de que documento identifica pessoa. **Corpo original abaixo:** **Ha 84 linhas de `cliente` para 41 pessoas, e preencher CPF/CNPJ vai colidir com o indice unico.** Medido em 03/08 contra producao: `cliente` tem **84 linhas, 84 `crm_lead_id` distintos e 41 nomes distintos** — cada pessoa aparece em 2 a 4 linhas, porque o CRM tem um lead por card e o espelho e fiel por `crm_lead_id` (R6). Exemplos: `ATAIDE DE MELO OLIVEIRA - LOURIVAL` em 4 linhas, `Hermani Soares de Araujo` em 4, `THIAGO GONCALVES TAQUARY` em 4. **As 39 UCs estao distribuidas em 39 linhas distintas de cliente, uma UC por linha** — entao o faturamento funciona: a UC carrega a identidade e o contrato aponta para ela. **O que quebra e o passo seguinte.** `documento` esta **NULL nas 84**, e `cliente_documento_unico` e `UNIQUE (tenant_id, documento) WHERE documento IS NOT NULL`: no dia em que a operacao digitar o CPF do Ataide na segunda linha dele, o banco recusa com `23505`. Nao e perda de dado — e trabalho que trava no meio, sem caminho obvio, com a pessoa achando que digitou errado. **E ha um efeito silencioso antes disso:** todo relatorio por cliente reparte uma pessoa em ate 4 linhas, e a soma por nome nao e confiavel porque ha variacao de grafia (`Carlos Gabriel Santos Alves` com 3 linhas e `CARLOS GABRIEL SANTOS ALVES` com 2; `NI3-` e `NII3-`). **O que NAO fazer sem decisao:** deduplicar apagando linha viola a R6 (cliente espelhado nunca e deletado, ou ressuscita no ciclo seguinte). **Decidir:** (a) entidade de **pessoa** acima de `cliente`, com as linhas do CRM apontando para ela — resolve relatorio e documento, e e migration; (b) `documento` deixa de ser unico por tenant e passa a ser unico por `(tenant_id, crm_lead_id)`, aceitando o CPF repetido — barato, e desiste de "um cliente, um documento"; (c) pedir ao dev do CRM que funda os leads da mesma pessoa, e o espelho segue fiel — e o unico que conserta na origem, e depende de terceiro. **Vermelha porque entra em producao errado:** o bloqueio aparece durante a digitacao, que e justamente quando ninguem quer parar para decidir | Vinicius + dev do CRM |
| **Q-PAGADOR-01** | 🟡 | **06/08: A GUARDA EXISTE — a opção (a) foi executada, e a questão cai de 🔴 para 🟡.** `boleto.registrar()` recusa com **`PagadorSemDocumento` (422)** antes de tocar a porta, nomeando **qual UC**, qual cliente e o comando que resolve. O `?? ''` saiu. **Duas decisões dentro da guarda, e nenhuma é de estilo:** (1) **a recusa é nossa e não da Sicoob** — sem ela quem recusava era o banco, e a recusa voltava pelo `catch` traduzida em **502**, o código que manda procurar indisponibilidade onde falta um campo; (2) **ela vem ANTES de criar a linha do boleto**, ao contrário de todo o resto do arquivo. Ali a linha nasce antes da chamada porque a rede pode cair no meio; aqui nada foi tentado, e uma linha `pendente` poria na fila do `PRD` §6 um boleto que **não pode dar certo** até alguém digitar um CPF — e a fila, por decisão registrada em `dominio/agenda.ts`, **nunca desiste sozinha**. **`K17c`–`K17e`** exercitam a função de verdade, e a terceira é a que prende a linha não-criada. A `Y5c` de `tests/repos-enderecos.ts` **encolheu em vez de sumir**: ela media o dado e a ausência da guarda; agora mede só o dado, que continua ausente. **O ENDEREÇO NÃO ENTROU NA GUARDA, de propósito:** o que a Sicoob exige de fato de endereço **não está medido** — é o item (c), e recusar por um campo que talvez seja opcional bloquearia boleto que sairia. **O que continua aberto, e é 🟡 e não 🔴 porque já está na fila com dono:** o **dado**. `cliente.documento` segue nulo, e é o mesmo item **1.1** de `PENDENCIAS` (`npm run documentos`, 24 pessoas) — a diferença é que a ausência agora **para no lugar certo, com o nome certo**, em vez de virar erro de integração. Corpo de 05/08 abaixo: **METADE FECHADA — o ENDERECO entrou, o DOCUMENTO nao.** Percorrendo a cadeia do boleto (que e outra que a da fatura) medi os seis campos de `endereco_*` da UC: **vazios em 29 de 29 faturaveis**, e **nenhuma das 10 views do CRM expoe endereco** - mesma forma do documento. Entrou por `npm run enderecos`, com o modelo ja gerado (`enderecos-modelo-20260805.csv`). **Conferido ANTES de construir, e o resultado foi negativo:** o conector NAO escreve `endereco_*` - o objeto `espelho` de `espelharUnidades` tem so `cliente_id`, `usina_id`, `percentual_rateio`, `crm_usina_cliente_id` e as tres de `rateio_*` -, entao preencher e rodar `npm run ciclo` nao apaga, que era o defeito que a R25 corrigiu para `data_vencimento`. **CONTINUA VERMELHA pelo `?? ''` do `boleto.ts:164`**, que e a metade do pagador que segue sem guarda: a verificacao `Y5c` de `tests/repos-enderecos.ts` prende isso de proposito, como registro executavel de que uma metade foi resolvida e a outra nao. Corpo original abaixo:  **04/08, tarde — a frase "nao bloqueia hoje" vale para o BOLETO e NAO vale para a FATURA.** Medido percorrendo o caminho da primeira fatura, e nao por auditoria: `contrato.ativar()` chama `podeAtivarContrato()` (R9), e sem `documento_validado` o contrato fica em **rascunho**. Rascunho nao ocupa a UC, entao a triagem recusa por **`sem_contrato_vigente`** — a **primeira** da ordem, atras da qual nada e medido. Producao tem `documento_validado = false` em **45 de 45** clientes ativos, entao **nenhuma das 29 UCs faturaveis pode virar fatura**, nem por Pix, nem por baixa manual. **O efeito chega uma camada adiante e com o nome errado:** quem digitar os 29 contratos e ler *"29 sem contrato ativo"* procura o defeito no contrato, e ele esta no cliente. **A opcao (a) foi executada na metade que independe de decisao:** a prontidao ganhou a camada **`documento_do_cliente`**, **primeira** da lista e contada em PESSOAS (`K19a`–`K19h`), e o insumo entra por **`npm run documentos`** — importador com o mesmo desenho dos de tarifa e vencimento, que confere o lote inteiro antes de escrever. **Reconferido em 04/08: nenhuma das DEZ views do CRM expoe documento**, entao o dado e coleta da operacao ou nao existe. **Segue pendente** o `?? ''` do `boleto.ts:164` e o endereco do pagador, que sao do boleto e nao da fatura. **Corpo original abaixo:** **O boleto sairia sem CPF/CNPJ e sem endereco do pagador, e o codigo manda string vazia em vez de recusar.** Medido em 03/08 contra producao: `cliente.documento` e **NULL nas 84 linhas**, e os **seis** campos `endereco_*` da UC estao vazios nas **39** (`logradouro=0 numero=0 bairro=0 municipio=0 uf=0 cep=0`). `src/repos/boleto.ts:164` monta o pagador com `documento: uc.cliente.documento ?? ''` — **o `?? ''` e a questao**: sem guarda, a Sicoob recebe um pagador sem identificacao e a recusa vem do outro lado, traduzida em 502, num ponto onde a mensagem util ja se perdeu. **Nao bloqueia hoje** porque nao ha A1 (`Q-SICOOB-01`) e o caminho que funciona e Pix estatico + baixa manual, que nao pedem pagador. **Bloqueia o primeiro boleto real**, e o insumo nao esta em lugar nenhum: `financeiro.parceiros` nao expoe documento, nenhuma das 8 views expoe documento de cliente, e o endereco da UC e campo **local** que nunca existiu no CRM (`SPEC-002` §3.1). **Decidir:** (a) trocar o `?? ''` por recusa nomeada antes de chamar a porta, e acrescentar as duas camadas a `repos/prontidao.ts` — que ja conta dez e cujo trabalho e exatamente este; (b) definir de onde vem o dado: coleta na operacao, ou pedido ao dev do CRM junto com a `Q-CLIENTEDUP-01`; (c) confirmar com a Sicoob **o que e obrigatorio de fato** no convenio — endereco pode ser opcional, documento nao costuma ser. **A (a) e barata e independe das outras duas**, e e o que impede a falha de aparecer como erro de integracao | Vinicius + operacao |
| **Q-INADIMPLENCIA-01** | 🟡 | **A inadimplência existe como *visão* e não existe como *registro* — e o `PRD` pede as duas.** O §4.3 define a entidade como *"visão derivada **+ registro de tratativas, acordos e histórico de contato**"*, e o §9 pede a tela como *"inadimplência **com tratativas**"*. **Medido em 30/07:** a visão derivada existe — `marcarVencidas()` (`src/repos/fatura.ts:296`, idempotente) e o KPI `vencidas_em_aberto` na tela Carteira. **Do registro não existe nada**: nenhuma tabela, nenhuma rota das 85, nenhuma tela das 12, e a palavra `inadimplencia` aparece no código **uma vez**, num comentário de `src/sicoob/porta.ts:89`. **A pergunta operacional que separa as duas:** *"quem não pagou"* o sistema responde hoje; ***"o que já se fez a respeito"* não tem onde ser gravado**, e quando alguém ligar para o cliente essa informação vai para um caderno ou para o WhatsApp. **Há uma trava normativa que precisa estar visível antes de alguém propor o atalho:** o `PRD` §4.3 diz que a etapa `INADIMPLENTES` do funil do CRM **não é fonte** — *"lê-la seria ler a própria saída do financeiro com atraso"* (`F-03`) — e a regra 4 proíbe escrever lá. **A inadimplência é produzida aqui ou não é produzida.** **Não bloqueia hoje:** com 0 faturas em produção não há vencido que registrar. Vira urgente na primeira competência emitida que passe do vencimento. **Decidir:** (a) tabela de tratativa vinculada à fatura, com tipo de contato, data, quem falou e o que ficou combinado; (b) tabela de **acordo** separada, porque acordo altera vencimento e valor e é fato diferente de "liguei e não atenderam"; (c) só a visão, e a tratativa fica fora do sistema enquanto o volume for baixo — que é resposta legítima e precisa estar **escrita**, não implícita. Ver `REVISAO-VERTENTES-2026-07-30.md` §1.1 | Vinicius |
| **Q-EQTL-ESCOPO-01** | 🔴 | **NOVA em 07/08/2026 — a frente de leitura automática da fatura da Equatorial, e esta é a primeira pergunta dela.** O pedido foi *"vinculado somente às UCs ativas"*, e **"ativas" tem duas leituras medidas, ambas defensáveis**: `status = 'ativa'` são **41** UCs; `rateio_situacao = 'ativado'` — as que de fato faturam — são **29**. As 12 de diferença estão `nao_ativado`, e **7 delas em troca de titularidade** (`Q-SITUACAO-01`), que é justamente o estado em que a fatura da distribuidora ainda chega no nome antigo. Coletar as 41 gasta 12 acessos ao portal por mês para dados que não viram fatura nenhuma hoje — e é o que garante histórico no dia em que o CRM ativar. Coletar as 29 é o mínimo que serve. **Não há default e não vai haver:** o script vai exigir `--escopo ativas\|faturaveis`, no precedente do `bootstrap-plataforma-admin.sql`, que também não tem default por escolher errado ser irreversível. **Prazo:** decide colunas da migration 26 | **dono** |
| ~~**Q-EQTL-CANAL-01**~~ | ✅ | **FECHADA EM 08/08/2026 PELO DONO: o canal é o E-MAIL.** *"vamos usar o email"*. **O que isso apaga do plano:** o coletor em Playwright deixa de ser necessário para a coleta de rotina — não há 403 Imperva para contornar, não há reCAPTCHA para pontuar, não há `.aspx` para quebrar quando mudar, e a fatura chega no dia certo de cada UC em vez de a gente ir buscar num dia que *"depende do cliente"*. **O que isso NÃO apaga, e é o preço:** a configuração inicial passa pelo portal, **UC por UC**, com login — e login exige a data de nascimento (`Q-EQTL-NASCIMENTO-01`); é mudança na entrega da fatura **do cliente**, que precisa de consentimento; e o histórico de 156 meses continua só saindo pelo portal, então **backfill segue sendo raspagem** se alguém quiser. **O desenho já acomodava:** `PortaDeColetaDeFatura` não sabe se o PDF veio de navegador ou de caixa de entrada, e era exatamente para isso que ela existia — **nenhuma linha do que foi construído em 07/08 muda**. **Abre uma questão nova e menor:** qual caixa recebe (`Q-EQTL-CAIXA-01`) | **dono** |
| **Q-EQTL-CAIXA-01** | 🟡 | **NOVA em 08/08/2026, filha da decisão do canal.** Decidido que o canal é e-mail, falta **qual caixa** e **como o sistema a lê**: (a) endereço — um só para as 29 UCs, ou um por tenant? o remetente e o assunto da Equatorial são estáveis o bastante para filtrar? (b) protocolo — IMAP com senha de aplicativo, ou API do provedor com OAuth? a diferença importa porque a credencial da caixa é **segredo de plataforma** e não por tenant, então pela regra 5 ela vai para variável de ambiente, e não para o Vault; (c) a fatura vem **anexa** ou como link para o portal? se for link, o e-mail não elimina a raspagem — só muda por onde ela entra, e essa é a única resposta que pode reabrir a `Q-EQTL-CANAL-01`. **O item (c) é o primeiro a medir**, e mede-se recebendo **um** e-mail | **dono** |
| **Q-EQTL-NASCIMENTO-01** | 🔴 | **12/08/2026 — O INSUMO EXISTE, E ELE ESTAVA NUMA PLANILHA.** A entrada dizia que a data de nascimento *"nao existe no nosso schema, nao vem de nenhuma das 10 views do CRM"* — verdadeiro, e incompleto: a **planilha de beneficiarias** tem `DATA NASC` preenchida para **31 dos 32** titulares. **Os 32 CPFs passam no digito verificador**; o que ha sao quatro correcoes de cadastro, nao ausencia: dois anos **futuros** (`27/01/2991` e `26/01/2990` — digito trocado em 1991/1990), um CPF sem formatacao e uma idade de 98 anos a confirmar. **A metade PJ CONTINUA 🔴 e por isso a entrada nao cai de nivel:** a NII3 (`37.200.596/0001-83`) esta na planilha **sem data nenhuma**, o que confirma por evidencia o que a folha do portal ja dizia — para CNPJ ninguem sabe qual e a validacao da Equatorial. Ver `PLANILHA-cobranca-medida-2026-08-12.md` §5. **Corpo original abaixo:** **08/08/2026, mais tarde — MEDIDO CONTRA O CRM, E O DADO NÃO ESTÁ LÁ.** O dono respondeu que *"data de nascimento pode ser puxada da aba clientes, dentro do CRM; todos os clientes devem ter data de nascimento"*. **Conferido antes de obedecer, que é o que a sessão 19 ensinou** — e o resultado é negativo nos três níveis: (1) a view `financeiro.rateio_clientes`, que é a "aba clientes" que o CRM expõe, tem **`contrato_id, codigo_geradora, usina, lead_codigo, cliente, telefone, percentual_rateio, uc, troca_titularidade, numero_protocolo, data_cadastro, data_vencimento, observacoes, created_at, crm_tenant_id`** — e **nenhuma data de nascimento**; (2) **nenhuma das 10 views** expõe o campo; (3) uma varredura do **catálogo inteiro** do banco do CRM por `nasc|aniver|birth|dob|natal` devolve **zero colunas**, e as únicas `jsonb` do banco são internas do `pg_net`. **A nossa base também não tem** (`public`, zero colunas). **A leitura honesta:** ou o dado está numa tela do CRM alimentada por outra fonte que não este banco, ou ele não existe. **Não é improviso decidir isso** — virou pedido ao dev do CRM em `PROMPT-dev-crm-rodada7-2026-08-08.md`, que é o canal que já existe e que já resolveu três perguntas deste tipo. **O que encolhe a questão:** com a `Q-EQTL-CANAL-01` fechada no e-mail, a data de nascimento é necessária **uma vez por UC**, na configuração — e não a cada coleta. Corpo original abaixo: **NOVA em 08/08/2026, e é insumo humano que não estava em lista nenhuma.** A folha do portal, preenchida pelo dono, diz no item 1: o login da agência virtual é **UC + CPF/CNPJ**, e *"a validação é a **data de nascimento** da pessoa que representa o CPF"*. **`data_nascimento` não existe no nosso schema, não vem de nenhuma das 10 views do CRM, e não é o item 1.1** — é um **terceiro** dado por titular, obrigatório para entrar. **E há um buraco dentro do buraco: para CNPJ ninguém sabe qual é a validação**, e parte das 29 UCs é de pessoa jurídica. **É a quinta vez que este projeto encontra um insumo humano percorrendo o caminho em vez de auditando spec** — depois do documento do cliente (04/08), do dia de vencimento, do endereço do pagador (05/08) e do `npm run tarifas` (06/08). **O que o encolhe, e não é pouco:** se a `Q-EQTL-CANAL-01` for decidida pelo e-mail, a data de nascimento só é necessária **uma vez por UC**, na configuração inicial — e não a cada coleta. Pelo portal, ela é permanente | operação (coleta) + **dono** (decidir se coleta) |
| **Q-EQTL-CANAL-01** | 🔴 | **NOVA em 08/08/2026, e ela pode apagar a raspagem inteira do plano.** Medido pelo dono na folha do portal: a Equatorial **oferece fatura por e-mail**, e **dá para apontar para um e-mail nosso** — não o do cliente. A tabela *se X então Y* da própria folha antecipava a consequência: *"não há scraping nenhum. A Equatorial entrega, e a frente vira uma caixa de entrada."* **Por que o e-mail ganha em quase tudo, e não é preferência:** não há proteção de bot para contornar (o portal responde **403 Imperva** a quem não é navegador, e há reCAPTCHA no site); não quebra quando o `.aspx` muda; é recurso **oferecido** pela distribuidora em vez de uso que os termos podem proibir (`Q-EQTL-AUTORIZACAO-01`); **dispensa a data de nascimento** a cada coleta (`Q-EQTL-NASCIMENTO-01`); e a fatura chega no dia certo de cada UC, que a folha diz *"depender do cliente"*. **O que o e-mail NÃO resolve, e é honesto dizer:** a configuração inicial ainda passa pelo portal, UC por UC, com login; é mudança na entrega da fatura **do cliente** e precisa de consentimento; e o histórico de **156 meses** só sai pelo portal. **A recomendação técnica é e-mail como canal principal e portal como exceção e backfill** — e as duas portas já construídas acomodam isso sem mudar uma linha, porque `PortaDeColetaDeFatura` não sabe se o PDF veio de navegador ou de caixa de entrada. **Mas a escolha é do dono**, porque envolve falar com 29 clientes | **dono** |
| **Q-EQTL-CAMPOS-01** | 🔴 | **08/08/2026 — MEIO RESPONDIDA, e a metade que falta é a que importa.** O dono trouxe um PDF real e ele foi lido. **O que ficou sabido:** a **iluminação pública** do `PRD` §5.1 é a linha **`CONTRIB. ILUM. PÚBLICA - MUNICIPAL`**, sob a seção `ITENS FINANCEIROS`; **não existe linha chamada "encargos"** — a própria fatura explica que encargos setoriais estão detalhados só na área do usuário do site, ou seja **embutidos na tarifa** e não destacados como parcela; e os **tributos vêm destacados mas embutidos** (ICMS 19% sobre o fornecimento; PIS 0,4016% e COFINS 1,8638% sobre o fornecimento **menos** o ICMS) — **não são linhas somadas ao total**, e quem as somar cobra tributo duas vezes. **O que NÃO ficou sabido, e é o fio B:** a fatura que veio é `B1 RESIDENCIAL — RESIDENCIAL NORMAL **CONVENCIONAL**`, **sem geração distribuída** — duas linhas cobráveis só (fornecimento + COSIP), somando o total exato, sem energia injetada, sem energia compensada, sem saldo de créditos e **sem nenhuma linha de fio B / TUSD**. A folha marcou *"tem compensação"*; medido, não tem. **Consequência para o código:** `CamposDaFatura` exige as três parcelas e recusaria esta fatura — corretamente, pela regra `F1`, e por um motivo verdadeiro: o conjunto de campos veio do `PRD` e ainda não foi confrontado com uma fatura de GD. **Não foi mexido de propósito** — redesenhar contra uma fatura convencional é a mesma adivinhação de antes, com mais confiança. **Falta uma coisa só: a fatura de uma das 29 UCs que faturam**, todas com rateio ativado e portanto com compensação. Corpo de 07/08 abaixo: **NOVA em 07/08/2026, e é a que separa esta frente de funcionar de dar valor errado em silêncio.** O `PRD` §5.1 define `valor_tarifas_concessionaria` como **fio B, iluminação pública e encargos** — repasse puro, sobre o qual *"ninguém comissiona nem repassa"*. Uma fatura da Equatorial traz **pelo menos três números grandes na mesma página** e os três parecem certos: o **total a pagar**, a soma dessas três parcelas, e o **consumo compensado** (que a G3 já cobra em `valor_consumo`). **Ler o primeiro no lugar do segundo não gera exceção, não gera log e não gera recusa** — gera fatura maior, repasse sobre base errada (§5.2), comissão errada (§5.4) e `liquido_g3` errado (§5.5), porque a fórmula subtrai justamente esta parcela. **O que falta não é código: é uma fatura real.** Ninguém deste lado viu uma fatura da Equatorial GO, então o mapa rótulo→campo é suposição. E há uma parte que nem o dono resolve: **"encargos" inclui bandeira tarifária? ICMS, PIS/COFINS destacados? multa e juros da própria Equatorial?** — essa metade é do **contador**, e a `PAUTA-contador.md` é o canal que já existe. **Mitigado por construção, não fechado:** `src/dominio/fatura-concessionaria.ts` soma **por componente nomeado** e trata `total_a_pagar` como conferência, nunca como fonte; a verificação `F2c` afirma que os dois são **diferentes** de propósito, então igualá-los por construção quebra o teste em vez da fatura de um cliente. **Prazo:** bloqueia o extrator (fase B2/E do plano) | **dono + contador** |
| **Q-EQTL-CRED-01** | 🔴 | **08/08/2026 — a metade boa confirmada, e uma parte nova.** Medido pelo dono: **UC + CPF/CNPJ entra direto, sem conta cadastrada e sem CAPTCHA no login** — então os **29 cadastros** que a folha temia **não existem**, e a credencial reusa o item 1.1 como previsto. **O que apareceu no lugar** é a `Q-EQTL-NASCIMENTO-01`: a validação é a **data de nascimento**, que é um terceiro dado e não está em lugar nenhum. A pergunta original — Vault do `ADR-0005` ou `cliente.documento` direto — **continua aberta e ficou maior**, porque agora são três dados por titular (UC, documento, nascimento) e os três juntos são credencial de acesso a sistema de terceiro. Corpo de 07/08 abaixo: **NOVA em 07/08/2026 — uma fronteira que o `ADR-0005` não previu.** Medido: o acesso à agência virtual da Equatorial é **número da UC + CPF/CNPJ do titular**. Esse par é **dado de negócio** — o CPF já vai viver em `cliente.documento`, e é o **item 1.1 da fila**, sem o qual `contrato.ativar()` recusa (R9) — **e** é **credencial de acesso a sistema de terceiro**, que pela regra 5 vive em armazenamento cifrado e é acessada por `credencial_ref`. As duas coisas ao mesmo tempo, e a escolha não é cosmética: guardar em `cliente.documento` e ler dali é simples e põe uma credencial de terceiro numa coluna em claro; guardar no Vault do `ADR-0005` obedece a regra 5 e duplica o dado com risco de divergirem. **O achado que vale mais que a pergunta:** se UC+CPF bastar (item 1 da folha do portal), **esta frente não tem insumo humano novo nenhum** — ela reusa o 1.1, e o item 1.1 passa a destravar duas filas em vez de uma. Se o portal exigir conta com senha, aparecem **29 cadastros** que não estavam em lista nenhuma. **Prazo:** decide colunas da migration 26 | **dono** |
| **Q-BOLIMP-01** | 🟡 | **NOVA em 17/08/2026 — o boleto emitido a mão entrou no sistema, e a conciliação dele não.** Construído o caminho de **importação de boleto** (`POST /faturas/:id/boleto/importar`, migration 32, aba Faturas): enquanto o certificado A1 não existe, o título é emitido no portal da cooperativa e **transcrito** para cá — linha digitável conferida nos quatro dígitos verificadores, código de barras remontado dela, valor e vencimento lidos de dentro dos 44 dígitos e comparados com a fatura. Isso fecha três silêncios medidos no dia: a fatura dizia *"sem boleto"* para uma fatura que tinha; o documento composto caía no ramo do **Pix estático**, que não concilia, existindo um boleto com nosso número; e a conferência aritmética nunca rodava contra esse título. **O QUE FICA ABERTO, e é só isto:** o boleto importado é **excluído da consulta ativa** do `PRD` §6 (`emAberto` filtra `origem = 'api_sicoob'`) e da baixa pela API (`BoletoImportadoNaoSeBaixaPelaApi`, 409). **A razão é medida e não conservadorismo:** o `nosso_numero` de um importado foi transcrito do **rótulo impresso no papel**, e o rótulo impresso não é necessariamente o identificador que a API de Cobrança v3 aceita em consulta — perguntar por ele não devolve *"este título não é nosso"*, devolve **erro**, e o motor traduz erro em `registrarDivergencia` **num boleto que está certo**. **O custo, dito por extenso:** boleto importado não concilia sozinho; ele é cobrado pela **baixa manual** da aba Faturas, que é o caminho que a operação já usa hoje para tudo. **A decisão que falta, e ela só existe DEPOIS do A1:** (a) manter como está — importado nunca é consultado, e a baixa manual basta; (b) consultar também os importados, o que exige antes **medir** se o "Nosso Número" impresso é aceito pela Cobrança v3 — um `GET` contra o sandbox responde isso em minutos; (c) pedir o identificador da API no momento da importação, transformando um campo lido em um campo digitado a mais. **Não é bloqueio de nada hoje** — sem A1 não há consulta ativa nenhuma, importada ou não —, e por isso é 🟡: ela entra na fila do dia em que a `Q-SICOOB-01` fechar, e não antes | **dono** + operação |
| **Q-EQTL-AUTORIZACAO-01** | 🔴 | **08/08/2026 — CONTINUA ABERTA, e a folha explica por quê: o que foi achado não é o documento certo.** O item 5 pedia os **termos de uso**; o que voltou foi a **política de privacidade** (`go.equatorialenergia.com.br/compliance/politica-privacidade/`). São documentos diferentes, e é o de **uso** que diz se robô pode. A pergunta *"proíbem automação?"* voltou sem resposta — *"eu vi um certificado de recaptcha, mas nada mais"*. **Dois achados que pesam contra raspar:** (a) **não existe perfil de procurador / representante / parceiro** no portal, então a coleta usaria a credencial do **próprio cliente**, e não um acesso concedido à G3 — o que torna o mandato escrito de cada cliente pré-requisito e não formalidade; (b) **há reCAPTCHA no site** mesmo sem CAPTCHA visível no login, e um v3 invisível pontua a sessão — navegador headless costuma pontuar baixo, o que é risco de bloqueio na fase D. **A `Q-EQTL-CANAL-01` contorna os três**, porque fatura por e-mail é recurso oferecido pela distribuidora em vez de uso que os termos podem proibir. Corpo de 07/08 abaixo: **NOVA em 07/08/2026.** Acessar o portal da Equatorial em nome do cliente, com a credencial dele e por robô, levanta três coisas que não são técnicas e que o código não resolve: **(a) mandato** — existe autorização registrada de cada cliente para a G3 acessar a conta dele na distribuidora? o contrato de adesão cobre isso por escrito? **(b) termos de uso** — os termos do portal podem proibir acesso automatizado, e a proteção de bot medida em 07/08 (403 + Imperva/Incapsula nos três hosts) é indício de que o assunto é tratado lá; **(c) LGPD** — a fatura carrega endereço completo, CPF e histórico de consumo, e guardar o PDF cria retenção de dado pessoal que hoje não existe: por quanto tempo, com que base legal, e quem apaga. **Não bloqueia o plano nem a camada já construída** — bloqueia a **fase D**, que é o coletor real tocar o portal. O item 5 da folha `EQUATORIAL-portal-2026-08-07.md` manda achar e ler os termos na mesma passada | **dono** |
| **Q-EQTL-DIVERG-01** | 🟡 | **NOVA em 07/08/2026.** Quando a fatura lida discordar do que já está na fatura da G3 — a UC não existe na competência, o valor difere do que uma planilha já lançou, ou a mesma UC vem duas vezes — o sistema **recusa, marca ou só conta?** O precedente do projeto é consistente e aponta para *contar*: a `prontidao` conta e não decide, o `conferirTarifas` conta as UCs sem tarifa e **não trava a emissão** (`B10f` prende isso), e o `lote-de-documentos` mantém a soma fechada em vez de descartar. **Já resolvido para o caso da duplicata**, e sem decidir nada: `lerLoteDeFaturas` deixa as duas em `lidas` e **nomeia** a colisão (`F10a`/`F10b`) — somar produziria o dobro do repasse sem erro nenhum, e descartar uma seria o módulo escolhendo qual vale. **O que falta é o caso da divergência de valor**, e ele só aparece na fase G. **Prazo:** antes de aplicar leitura a fatura | **dono** |
| **Q-EQTL-GLOSSARIO-01** | 🟢 | **NOVA em 07/08/2026 — confirmação, não decisão.** O `GLOSSARIO.md` afirma, na entrada **distribuidora**: *"Não tem API pública; os dados entram por digitação manual."* A primeira metade continua verdadeira e foi remedida hoje; a segunda deixa de ser verdade na fase F do plano, quando `npm run coletar` existir. É o mesmo modo de falha que o `PATCH-citacoes` tratou e que a `Q-ESCOPO-01` repetiu **três vezes**: o corpo datado certo, o índice vencido, e quem lê só o índice decide errado. Atualizar quando a fase F entrar — não antes, porque hoje a frase ainda está certa | autor do `GLOSSARIO` |
| **Q-DOCG3-01** | 🟡 | **12/08/2026 — TERCEIRA VOLTA DA `Q-DOCFATURA-01`, e ela é uma REVERSÃO.** O dono pediu o design de `github.com/lealvbl-stack/g3_fatura_unificada` (commit `ec1e71b`): *"quero o design exatamente como está nesse github… não quero redundâncias"*. O modelo de lá é uma folha A4 **fixa**, em fluxo — e o documento daqui é **bloco posicionado em milímetro**, que foi decidido duas vezes pelo próprio dono: 30/07 (*"layout configurável por tenant"*, contra a minha recomendação de fixo) e 03/08 (*"colocar os elementos onde eu desejar"* → migration 23). **As duas coisas não coexistem:** uma tabela com `subgrid` e rodapé preso por `margin-top: auto` não é expressável em `x_mm`/`y_mm`. **Decidido em 12/08: o modelo G3 fixo substitui, e o editor de blocos sai.** Registrado aqui porque reverter em silêncio apaga o porquê — o corpo é `PLANO-documento-modelo-g3-2026-08-12.md`. **E foi decidido junto que a virada ESPERA o leitor da Equatorial** entregar a quebra real, o que torna destino e portão duas coisas distintas: **nada foi apagado e nada foi construído em 12/08**, porque retirar o editor antes de existir o substituto deixaria o sistema sem documento nenhum. `layout-visual.ts`, `layout-editor.tsx` e o `EditorDeLayout` seguem vivos até a virada. **O que a decisão NÃO resolve, e virou as seis entradas abaixo:** tarifa cheia, fator de CO₂, série histórica, fonte, código de barras e o cinza dos rótulos. **Um achado que encolheu o trabalho:** a paleta da referência **já é a nossa** — Navy `#14213D`, Cream `#F6F2EA`, Orange `#E8843C`, Gold `#F4A65A` batem com `web/src/tema.ts` valor a valor. Não há trabalho de cor; o que difere é estrutura e tipografia. **E um que o impede de ser cópia:** a referência faz aritmética de dinheiro em **float** (`parseFloat`, `integral * (perc/100)`), o que a **regra 1** proíbe inclusive em cálculo intermediário — o desenho se adota, a conta se reescreve em centavos | Vinicius |
| **Q-DOCG3-02** | 🟡 | **12/08/2026, MAIS TARDE — A PLANILHA DE BENEFICIARIAS RESPONDEU O MODELO, e a pergunta MUDOU de natureza.** Medido em `PLANILHA-cobranca-medida-2026-08-12.md`: **tarifa R$ 1,185396/kWh** (derivada de QUATRO linhas independentes por `Valor Sem G3 / Consumida`, concordando ate a quinta casa) e **desconto de 20,0% igual em todas as 43 linhas**. As tres formulas fecham **ao centavo em 4 de 4** linhas completas. **E o modelo e MAIS SIMPLES do que o do GitHub: nao existem duas tarifas** — o desconto incide sobre o kWh, e a "tarifa G3" que o modelo exibe tachada e derivacao de apresentacao, nao dado. Cai de 🔴 para 🟡 porque o desenho deixou de estar bloqueado; **o que falta nao e mais o numero, e a FONTE dele**: o dono entregou a planilha como referencia e com a ressalva *"pode estar desatualizado"*, e um valor assim nao vira default em codigo (regra 10) quando sai **impresso na fatura**, no campo que existe para provar a economia ao cliente. **A pergunta agora e de onde a tarifa vem TODO MES:** da fatura lida (`Q-EQTL-CAMPOS-01`), da tabela com vigencia (`Q-TARIFA-CONC-01`, e o `npm run tarifas` ja existe), ou digitada por competencia — as tres dao o mesmo numero neste mes e envelhecem de formas muito diferentes. **Corpo original abaixo:** **A TARIFA CHEIA, e ela é o portão de três painéis.** A folha 1 do modelo mostra *"Sem a G3 você pagaria"*, *"Sua economia neste mês"* e *"Seu desconto %"*, e as duas colunas do detalhamento trazem tarifa e valor **cheios tachados** acima dos com desconto. Tudo isso é a mesma pergunta: **o que o cliente pagaria se não fosse cliente.** O financeiro tem `tarifa_reais_por_kwh` (a nossa) e **não tem** a da distribuidora para comparar. A referência resolve com um **prop de default 20%** — que é exatamente o *"valor default escolhido porque parecia razoável"* que a regra 10 proíbe, e num número que vai impresso na fatura do cliente. Três saídas: vem do leitor da Equatorial (mesma frente da `Q-EQTL-CAMPOS-01`), vem da `Q-TARIFA-CONC-01`, ou é parâmetro digitado por tenant — e a terceira faz o desconto virar **declarado** em vez de medido | Vinicius |
| **Q-DOCG3-03** | 🟡 | **12/08/2026 — O FATOR DE EMISSÃO DE CO₂.** A folha 2 traz *"CO₂ evitado"* com a nota *"Fator … kg/kWh · MCTI/SIRENE"*. A referência usa **0,029 kg/kWh** como default de prop. O fator do MCTI/SIRENE **muda por ano e por mês**, então um número cravado envelhece em silêncio numa fatura que o cliente guarda. Parâmetro por tenant, tabela com vigência (como as tarifas), ou o painel sai da folha. **Não vira centavos** — é grandeza física, regra 1 | Vinicius |
| **Q-DOCG3-04** | ✅ | **RESOLVIDA em 14/08/2026 — a serie existe, e ela e tabela com tenant, policy e trilha.** **Migration 29**, `registro_de_fatura_unificada`, chave `(tenant_id, numero_uc, competencia)`. A chave da referencia (`fatura:{uc}:{AAAA-MM}`, em `localStorage`) nao se porta: sem `tenant_id` a UC 000123 do tenant A somaria a economia da UC 000123 do B, e o numero errado sairia IMPRESSO sem erro em lugar nenhum (regra 2). **O que ela conserta esta na folha:** o indicador "Voce ja economizou" imprimia o desconto DESTA fatura e a nota dizia "Primeira fatura com a G3 Solar" — para toda fatura, sempre. A soma e feita pelo Postgres (`aggregate`), e tres CHECKs conferem que as partes somam o todo antes de a linha entrar. O botao "Registrar esta fatura" e ato SEPARADO de imprimir, e "Nova fatura" apaga o rascunho e **preserva** os registros, como a referencia faz desde `36e964e`. Corpo anterior: **12/08/2026 — A SÉRIE HISTÓRICA.** Dois painéis da folha 2 — as **12 barras** de consumo e a **economia acumulada** (*"com a G3 Solar desde …"*) — exigem histórico por UC que **não é gravado hoje**. A referência o guarda em `localStorage` do browser, o que aqui não serve: é dado de negócio, multi-tenant, e cairia nas regras 2, 3 e 9 (tabela com `tenant_id`, RLS com policy, e auditoria na escrita). É migration nova, não ajuste de tela | Vinicius |
| **Q-DOCG3-05** | 🟢 | **12/08/2026 — BARLOW ENTRA?** A referência usa **Barlow** (300/400/500/600/700 + itálico) e **Barlow Semi Condensed** (400/500/600/700); o sistema usa **Inter**. A semi-condensada não é decoração: ela carrega **todo número grande, todo rótulo caixa-alta e todo título** do modelo — sem ela o desenho não é o desenho. São duas famílias novas, e `tema.ts` tem posição registrada: fonte é **servida por nós**, de `web/public/fontes/`, com versão no nome e licença ao lado. Os 15 woff2 estão no bundle da referência e são reaproveitáveis | Vinicius |
| **Q-DOCG3-06**  | ✅ | **12/08/2026 — O CÓDIGO DE BARRAS.** A faixa de pagamento da folha 2 desenha o código de barras do boleto (a referência usa `JsBarcode`). O financeiro **não tem** codificador de barras — tem o do QR, escrito à mão em `src/dominio/qrcode.ts` com 45 verificações. Duas saídas: escrever o Interleaved 2 of 5 pelo mesmo critério (servidor, sem dependência, porque o CRM consome a mesma rota), ou a faixa sai só com **linha digitável + PIX**, que é o que ela já faz hoje | Vinicius — **14/08/2026, CONSTRUIDA A METADE QUE IMPORTAVA, e nao era a que a pergunta descrevia.** A questao era *"escrever um codificador Interleaved 2 of 5 ou sair sem codigo de barras"*. A medicao da referencia (`REFERENCIA-fatura-unificada-2026-08-13.md` §6) mostrou que a parte que faltava nao e **desenhar** — e **saber se a linha esta certa**. Entrou `src/dominio/linha-digitavel.ts`: os quatro digitos verificadores (modulo 10 nos campos 1 a 3, modulo 11 FEBRABAN no geral), a remontagem dos 44 digitos, o decodificador de banco/moeda/fator/valor e `conferirBoleto`. **30 verificacoes em `tests/linha-digitavel.ts`, suite `EXIT=0`.** A linha de referencia e **real** — passa nas quatro, decodifica para Sicoob 756, R$ 596,69, vencimento 17/08/2026. **E a medicao exaustiva achou um buraco que ninguem tinha nomeado:** das 423 corrupcoes de um digito, **26 passam despercebidas — todas no campo de fator+valor**, que so o modulo 11 protege, e a regra de colapso dele (resto 0, 10 ou 11 viram DV 1) deixa varias somas caindo no mesmo digito. Os campos 1 a 3 tem dupla protecao e **zero** cegueira. Quem fecha o buraco do dinheiro e a comparacao do valor do codigo de barras contra `fatura.valor_total_centavos`, que entrou junto. **O que continua aberto e so o desenho:** codificador ITF proprio (como o QR foi feito) ou a faixa segue com linha digitavel + PIX | Vinicius |
| **Q-DOCG3-13**  | ✅ | **14/08/2026 — A CONFERENCIA DO BOLETO EXISTE E NINGUEM DECIDIU O QUE FAZER COM ELA.** `conferirBoleto` entrou hoje e viaja no payload de `GET /faturas/:id/documento` como `pagamento.conferencia`. Ela sabe dizer quatro coisas que o sistema **nao sabia ontem**: a linha digitavel esta corrompida; o `codigo_barras` do banco discorda da linha do banco; o **valor** que o codigo de barras carrega difere de `fatura.valor_total_centavos`; o **vencimento** difere. Construida para **contar e nomear**, nunca recusar — e o precedente do projeto (`prontidao` conta, `conferirTarifas` conta, `Q-EQTL-DIVERG-01` aponta para o mesmo lado), e havia motivo concreto: `src/sicoob/falso.ts` produz linha fora do padrao de proposito, e recusa dura quebraria a suite sem nada de producao estar errado. **A decisao que falta e o que acontece quando diverge:** (a) so registra e a tela do operador mostra; (b) marca a fatura e ela sai do lote de impressao; (c) recusa a emissao do documento. A diferenca e cara nos dois sentidos — (c) impede imprimir boleto que o banco recusa, e tambem impede imprimir por causa de um `falso.ts`. **Hoje o comportamento e (a) por omissao, e omissao nao e decisao** (regra 10) **— FECHADA E CONSTRUIDA EM 14/08, no mesmo dia. O dono decidiu (c): RECUSAR A EMISSAO.** Entrou `BoletoNaoConfere` (status **409** — o pedido esta certo, o estado e que conflita), levantado na composicao do documento, com a mensagem nomeando o NUMERO de cada divergencia e nao so o tipo. **Medido antes de decidir: producao tem ZERO boleto registrado**, entao a recusa nao tornou nenhum documento existente inimprimivel — era de graca agora e cara depois. No lote nao derruba a pilha: `separarCompostos` ja parte entre o que compos e o que nao compos. **E a decisao obrigou a consertar o adaptador falso:** ele devolvia linha de formato inventado, e um falso que precisa de excecao dentro da regra de recusa e o buraco por onde a regra vaza. Agora produz 47 e 44 digitos com os quatro verificadores fechando, pelo `montarLinhaDigitavel` — que existe SO para isso e nao e emissao de boleto. Suite `EXIT=0` **— 14/08: A FAIXA PASSOU A IMPRIMIR A LINHA EM GRUPOS, e o codigo de barras continua fora.** Achado ao fotografar a folha: a faixa saia com os 47 digitos corridos, e o agrupamento (`75691.50043 01727.686907 00000.130013 1 15410000059669`) e o que torna a linha conferivel a olho por quem digita no aplicativo do banco. `linhaDigitavelFormatada` ja existia desde a manha e nao estava ligada; agora viaja como `pagamento.linha_digitavel_br`, **formatada no servidor** como todo o resto da faixa. O campo cru continua ao lado, para quem copia e cola. **O desenho do codigo de barras (ITF) continua a decisao em aberto** — e agora e a unica parte que falta | **Vinicius** |
| **Q-DOCG3-14** | 🟢 | **14/08/2026 — A ERA DO FATOR DE VENCIMENTO, decidida com justificativa datada e registrada aqui para ficar visivel.** O fator de vencimento do codigo de barras contava dias desde **07/10/1997** e **esgotou em 21/02/2025** no fator 9999; o FEBRABAN reiniciou a contagem, e **fator 1000 = 22/02/2025**. A consequencia e que **um fator entre 1000 e 9999 e ambiguo entre as duas eras** — a mesma linha decodifica para duas datas separadas por ~27 anos. `vencimentoDoFator` usa a **era nova** sem hesitar, e a justificativa e que o sistema emite boleto a partir de 2026 e nao existe titulo nosso da era antiga. **O que reabre isto:** leitura de boleto de TERCEIRO anterior a 22/02/2025 — o caminho que a referencia tem (ler boleto por modelo de visao) e que nos nao temos. Nesse dia a funcao devolve a data errada **e nao avisa** | Vinicius |
| **Q-DOCG3-15** | 🟢 | **14/08/2026 — A ABA DOCUMENTO ADOTOU A TINTA DA REFERENCIA, e ela reprova AA. Decidido pelo dono, com os numeros na mao, e registrado aqui para nao virar descoberta de outra pessoa.** O pedido foi *"quero ajustar o layout da interface da aba documentos. A referencia exata deve ser `g3-fatura-unificada.vercel.app`, sem tirar nem por, deve ser exatamente igual, com bordas iguais, sistema de cores, tipografia"*. A referencia usa **`#8F939D`** como tinta apagada e **`#E8843C`** como tinta — e na manha do MESMO dia este projeto tinha trocado os dois pelos derivados `#66686F` e `#995728`, porque a medicao os derrubou (`Q-DOCG3-07`, reaberta e fechada em 14/08). Os numeros que valem, remedidos: **`#8F939D` sobre branco 3,08:1**, **`#E8843C` como texto sobre branco 2,69:1**, **`#fff` sobre o `#E8843C` do botao 2,69:1** — AA pede 4,5 nos tres. O anel de foco da referencia (`outline: 2px solid #E8843C`) da **2,41:1** sobre o cartao, e a WCAG 1.4.11 pede 3. **Consultado com os tres numeros escritos, o dono escolheu "Exato".** **O QUE ISSO NAO E:** nao e a paleta do sistema mudando. A troca vale dentro da ilha **`.g3ref`** e so nela — `--fraco` e `--acento-forte` seguem sendo os derivados nas outras onze telas, e os invariantes **I1d, I1e e I1f** de `web/tests/interface.ts` prendem a fronteira em tres linhas (nenhuma REGRA pinta com o Gray; ele so aparece dentro do bloco `.g3ref`; e so ocupa token `--g3ref-*`). A **T7b** da suite do tema continua medindo o par e dizendo o numero — nada disto aprova a cor, isto registra ONDE ela vale. **O QUE REABRE:** alguem de fora da conversa abrir a aba, medir e achar que e descuido; ou a aba passar a ser usada por quem depende de contraste. **O conserto, se o dia chegar, e de UMA linha** — os tokens do tema claro em `tema.ts`, e a geometria inteira (borda, espaco, raio zero, tipografia) continua identica a referencia. **As outras duas decisoes da mesma consulta:** o tema escuro **acompanha o tema** (a referencia nao tem escuro, entao cada token da ilha remapeia para o equivalente medido da casa) e a **barra navy da referencia NAO entrou** — o sistema ja tem uma faixa navy no topo, e duas empilhadas seriam a referencia copiada e a tela piorada | **Vinicius** |
| **Q-TARIFA-FIN-01** | ✅ | **RESOLVIDA em 14/08/2026, fim do dia, no caminho (b) — A TARIFA E COLUNA DA UC, e a aba SAIU.** O dono fechou os dois lados numa frase: *"remova definitivamente a aba Tarifas, pois ela ja nao possui finalidade e apenas gera redundancia."* **Migration 30:** `unidade_consumidora.tarifa_reais_por_kwh numeric(12,6)`, dois CHECKs (positiva e ordem de grandeza — 118,54 e "digitou centavos no campo de reais"), e `app.tarifa_da_uc(uuid)` no lugar de `app.tarifa_vigente(text, date)` **com o mesmo contrato de ausencia**: levanta `no_data_found`, e a R26 continua inteira. A tabela `tarifa` foi APAGADA — tinha uma linha, com o valor que a medicao de 12/08 ja mostrara estar ~5% abaixo, e zero faturas apontando para ela. **Por que (b) e nao (c):** a (c) — ler da fatura em PDF — produz o numero MEDIDO e ja funciona na aba Documento, mas o lote nao le PDF; e a (a) acoplaria a emissao a disponibilidade do CRM, que e outro banco, de outro time, com outra janela de manutencao. **O defeito que a (b) trazia junto foi consertado no mesmo passo:** o conector preenche o vazio e **nunca apaga** o preenchido (`atual ?? card`), e a divergencia entre os dois vira SINAL — a mesma licao da R25, um degrau mais frouxa de proposito, porque tarifa e campo de origem MISTA e vencimento e campo local puro. Preenche-se na aba **Unidades**, coluna "Tarifa R$/kWh". Corpo anterior: **14/08/2026 — A ABA TARIFAS NAO PUXA DE LUGAR NENHUM, E O VALOR QUE ELA GUARDA ESTA DESATUALIZADO.** O dono perguntou por que a aba ainda existe *"considerando que a tarifa esta sendo puxada diretamente da UC/Card/Negociacao"*. **Medido em producao: nao esta.** A tarifa vive numa tabela so — `tarifa`, chave `distribuidora` + vigencia; **nao ha coluna de tarifa em `unidade_consumidora` nem em `contrato`**; e a composicao le `app.tarifa_vigente(distribuidora, competencia)`. A tabela tem **UMA linha**: Equatorial, **R$ 1,130000**, vigencia `-infinity`, aberta. **A planilha de 12/08 mediu R$ 1,185396** — a aba serve ~5% abaixo, e ninguem percebeu porque **zero faturas foram emitidas**. **A aba nao pode simplesmente sair:** a R26 levanta sem tarifa vigente na competencia, de proposito, para que base nula nao vire zero em silencio. Sem fonte nova, retirar a aba para o faturamento. **Falta decidir DE ONDE ela passa a vir**, e sao tres caminhos: **(a)** do card, lido AO VIVO na emissao (`consumo_reais / consumo_kwh`, 41/41 de cobertura, **sem migration** — mas acopla a emissao a disponibilidade do CRM e usa numero digitado por vendedor); **(b)** do card, ESPELHADO em coluna nossa (tem o defeito ja medido de o conector sobrescrever campo local com nulo); **(c)** da fatura da Equatorial lida em PDF, que e o que a referencia faz — coluna *"Preco unit (R$) com tributos"* da linha `CONSUMO NAO COMPENSADO`, com seis casas. So a **(c)** produz tarifa MEDIDA em vez de digitada, e depende do leitor que nao existe. Cruza com `Q-TARIFA-CRM-01` e `Q-DOCG3-02` | **Vinicius** |
| **Q-TARIFA-CRM-01** | ✅ | **RESOLVIDA em 14/08/2026 — a granularidade e POR CLIENTE, e o campo nao precisa de view nova.** A pergunta era se a tarifa e por distribuidora ou por cliente, e a medicao do mesmo dia respondeu: **35 UCs a 1,130000, 4 a 1,16 e 2 a 1,180000**. O campo do CRM existe e nao se chama assim — `financeiro.vendas_ganhas` expoe `consumo_kwh` e `consumo_reais`, e a divisao da a tarifa, com **41 de 41** de cobertura. **`tarifaDoCliente` em `src/crm/sincronizacao.ts`** faz a conta em `BigInt`, com o arredondamento uma vez no fim. **A RESSALVA CONTINUA E ELA VALE MAIS QUE O NUMERO:** os tres valores sao redondos em duas casas nos 45 cards, sem excecao, enquanto a tarifa medida na fatura tem SEIS — redondo em 45 de 45 e assinatura de digitacao humana. Por isso o card e **semente e nao verdade**: preenche a UC vazia e nunca sobrescreve a digitada. O pedido ao dev do CRM (`PROMPT-dev-crm-rodada8`) deixou de bloquear. Corpo anterior: **14/08/2026 — A TARIFA PASSA A VIR DO CARD DO CRM, decidido pelo dono, E O CAMPO NAO ESTA EM VIEW NENHUMA.** Medido hoje conectado como `financeiro_ro`: **zero** colunas com "tarifa" em qualquer schema visivel, e **0** colunas de `public` visiveis — a role nao alcanca tabela base, que e o que a **regra 4** manda. Entao o campo existe no card e **nao existe para nos** ate estar numa view de `financeiro.*`. Pedido aberto em `PROMPT-dev-crm-rodada8-2026-08-14.md`. **E a pergunta maior nao e a coluna, e a GRANULARIDADE:** hoje tarifa aqui e **uma por distribuidora, versionada por vigencia** (`repos/regras.ts`, R22 e R26); um campo no card e **uma por cliente**. Se for mesmo por cliente, muda migration, composicao da fatura e a R26 — nao e trocar a fonte, e trocar o modelo. **A medicao de 12/08 aponta para "mesmo numero para todos"**: quatro linhas independentes da planilha deram `1,185397 · 1,185405 · 1,185394 · 1,185418`, e por cliente elas nao concordariam ate a quinta casa. Nao decido isso por inferencia de quatro linhas (regra 10) **— 14/08, MEDIDA: A GRANULARIDADE E POR CLIENTE, e a tarifa ja e derivavel hoje sem coluna nova.** A pergunta original era se o CRM tinha um campo `tarifa`. **Nao tem** — zero colunas com esse nome em qualquer schema visivel, remedido hoje. Mas `financeiro.vendas_ganhas` expoe `consumo_kwh` e `consumo_reais`, e a divisao da a tarifa do card. Medido pelo join `rateio_clientes.lead_codigo -> vendas_ganhas.codigo`: **41 das 41 UCs** tem card casado com os dois preenchidos — cobertura total. E ela **VARIA**: 35 UCs a `1,130000`, 4 a `1,16` e 2 a `1,180000`. Entao **nao e "uma por distribuidora"**, que e o que o financeiro assume hoje. **A RESSALVA MUDA O QUE SE FAZ COM O NUMERO:** os valores sao redondos em DUAS casas nos 45 cards, sem excecao, enquanto a tarifa da Equatorial medida na planilha de 12/08 tem seis (`1,185396`). Redondo em 45 de 45 e assinatura de **digitacao humana**, nao de medicao — e usa-lo para faturar poe a estimativa de um vendedor na fatura do cliente. Continua aberta por isso, e nao por falta de dado | **dev do CRM + Vinicius** |
| **Q-TARIFA-CRM-02** | ✅ | **RESOLVIDA em 14/08/2026 — a fonte entrou ANTES de a aba sair, na mesma leva.** Era exatamente o que esta questao pedia: *"a tela nao sai antes da fonte entrar, ou a R26 derruba a composicao"*. A ordem executada foi: coluna na UC + `app.tarifa_da_uc` (migration 30) -> `comporLote` reapontado -> campo na aba Unidades com filtro de pendencia e alerta de quantas faltam -> conector semeando -> so entao a aba, a tabela, as duas rotas, o seed e a suite `repos-tarifas` sairam. **`bash tests/run.sh` e `bash tests/repos.sh` EXIT=0** com a R26 exercitada pelo caminho novo (`tests/regras.sql`: UC sem tarifa levanta `no_data_found`). Corpo anterior: **14/08/2026 — A TELA DE TARIFA NAO PODE SAIR ANTES DE A FONTE ENTRAR, e e a segunda vez neste ciclo que a mesma trava aparece.** `web/src/telas/tarifas.tsx` e onde alguem digita R$/kWh e abre vigencia, e o dono decidiu que isso sai. Mas a R26 levanta quando nao ha tarifa vigente na competencia — de proposito, para que base de faturamento nula nao vire zero em silencio. Retirar a tela antes de a coluna existir na view deixa o sistema **sem tarifa nenhuma**, e a composicao da fatura para de compor. E a mesma sequencia do editor de layout (`REFERENCIA-fatura-unificada-2026-08-13.md` §13.1): o destino nao se rediscute, a ordem e que importa. **O que fica em aberto e se existe um meio-termo aceitavel** — a tela vira somente-leitura mostrando a origem, ou continua editavel como excecao nomeada ate a view chegar | **Vinicius** |
| **Q-DOCG3-07**  | ⚠️ | **REABERTA E CORRIGIDA EM 14/08/2026 — O NUMERO QUE FECHOU ESTA QUESTAO ESTAVA ERRADO.** Ela foi fechada afirmando que o `#8F939D` *"vale como tinta em papel: 4,02:1 contra branco, que passa AA"*. **Remedido com a mesma calculadora WCAG de `web/tests/tema.ts`: 3,08:1 no branco e 2,75:1 no creme.** As duas REPROVAM, e a segunda nem tinha sido olhada — o bloco creme do cliente e o cartao "sem a G3" sao fundo de rotulo cinza na folha 1. **E havia um segundo, que nunca teve numero nenhum:** o `#E8843C` como TEXTO (o valor a pagar da faixa e o "voce ja economizou") da **2,69:1** no branco e **2,41:1** no creme. A nota de 12/08 argumentava por que ele nao podia ser `var(--acento)` e nao perguntava se ele podia ser tinta. **CONSERTADO:** entraram os dois derivados que o `tema.ts` ja tinha achado por busca — `#66686F` (5,56 / 4,98) e `#995728` (5,60 / 5,02) —, o Orange ficou como SUPERFICIE (Navy sobre ele: 5,93:1), e a **T7** de `web/tests/tema.ts` passou a MEDIR cada tinta do papel contra cada fundo em que ela pousa. A licao e de metodo e nao de cor: a lista fechada do `I1c` dizia QUAIS tintas existem e nenhum teste dizia se elas PASSAM. Corpo anterior: **12/08/2026 — O CINZA DOS RÓTULOS, e é conflito com decisão MEDIDA.** A referência escreve todo rótulo em `#8F939D` sobre o creme. `web/src/tema.ts` mediu esse par em **2,75:1** — AA de texto pede 4,5 — e por isso derivou `fraco: #66686F`. Adotar o valor da referência reverte a medição e **`web/tests/tema.ts` falha** (regra 8). *Na folha impressa* o argumento WCAG é mais fraco: é tinta em papel, não tela. Mas o mesmo `#8F939D` aparece no **cromo de tela** da referência, onde ele vale inteiro. Proposta: `--fraco` na tela, cinza da referência só dentro da folha **— FECHADA EM 14/08, na recomendacao: vale na FOLHA, nao vale na TELA.** O `#8F939D` e tinta de impressao contra branco (**4,02:1**) e nao cromo de interface contra creme (**2,75:1**, que e o par medido em `tema.ts` e o que reprova AA). A distincao ja estava aplicada na faixa de pagamento desde 12/08 e agora vale para a folha 1 inteira; a interface continua com o `--fraco` derivado. **Nenhuma cor literal nova entrou:** as cinco da folha sao exatamente as que `web/tests/interface.ts` ja conhece, e a lista continua com seis | Vinicius |
| **Q-DOCG3-08**  | ✅ | **12/08/2026 — O BENEFICIARIO DO BOLETO NAO EXISTE NO SCHEMA, e isto foi achado CONSTRUINDO a faixa, nao auditando spec.** A faixa do modelo G3 abre com quatro campos, e o primeiro e *Beneficiario* — no modelo original ele vem com razao social e CNPJ, e e o campo a que o proprio modelo amarra o aviso anti-golpe (*"confira sempre se o beneficiario e Consorcio G3… CNPJ 66.714.022/0001-21"*). **Medido em 12/08: `identidade_de_cobranca` tem seis colunas — logo (3), chave padrao, e as duas de data — e NENHUMA de razao social ou CNPJ.** No caminho do **Pix** o nome existe (`chave_pix.recebedor_nome`, que e o campo 59 do BR Code) e passou a ser impresso; no caminho do **boleto** nao existe em lugar nenhum. **O que foi feito enquanto a questao nao fecha:** o campo e OMITIDO quando nao ha nome, em vez de sair com travessao — imprimir "Beneficiario: —" num documento cujo proposito ali e ensinar o cliente a conferir o destinatario e pior que nao imprimir o campo, porque treina exatamente o comportamento oposto. **A saida obvia seria reusar o `recebedor_nome` da chave Pix da fatura tambem no boleto, e ela NAO foi tomada:** sao a mesma empresa "quase certamente", e um documento de cobranca com aviso anti-fraude nao e lugar de quase. **Decidir:** coluna de razao social + CNPJ na identidade (que e o que o modelo pede e serve tambem ao cabecalho e ao rodape da folha 1), ou o beneficiario do boleto vem do convenio Sicoob junto com a `Q-SICOOB-01` | Vinicius |
| **Q-DOCG3-09** | 🟡 | **12/08/2026, CORRIGIDA NO MESMO DIA — a primeira versao desta entrada estava ERRADA no que importa, e o erro foi meu.** Ela descrevia o achado como defeito a consertar e eu recomendei conserta-lo primeiro, *"porque esta cobrando errado hoje"*. **O que faltou dizer: nada no repositorio produz esse numero.** Medido em `src/repos/fatura.ts` (`comporLote`) e na migration 16 — o financeiro grava `consumo_kwh = geracao_kwh x percentual_rateio / 100` e `valor_consumo_centavos = app.consumo_centavos(...)` sobre esse mesmo valor. **Ele fatura a COTA DE GERACAO, nunca o consumo do cliente.** A planilha fatura CONSUMO. Sao dois produtos com bases diferentes, e a diferenca nao e um `MIN()` esquecido. <br>**E o `MIN()` da legenda nao e expressavel aqui: o segundo operando nao existe no schema.** `fatura` nao tem consumo real do cliente — quem o traz e o `fatura-concessionaria.ts`, que ainda nao esta ligado a fatura (`Q-EQTL-CAMPOS-01`). **Cuidado com o nome:** a coluna se chama `consumo_kwh` e NAO e consumo; o documento ja a rotula honestamente como *"Credito injetado (kWh)"*. <br>**O QUE CONTINUA VERDADE, e por isso a entrada nao fecha:** os R$ 76,03 da Panificadora Plazza sao reais e foram reproduzidos ao centavo — **80,17 kWh sem credito (saldo 0,00) receberam 20% de desconto**. E defeito do **processo manual** que fatura hoje, e some no dia em que a composicao do financeiro assumir o faturamento dessas UCs. **Vale como aviso, nao como bug de codigo.** <br>**E ELE APONTA UMA PERGUNTA QUE NINGUEM FEZ:** faturando por cota de geracao, o financeiro cobra a cota **independentemente de o cliente te-la consumido** — o espelho do defeito da planilha, virado para o outro lado. Com a cota acima do consumo, a diferenca vira saldo na Equatorial e o cliente paga a G3 por credito que nao usou. A planilha tem coluna `Saldo` e a marca *"considerar essa coluna para ajuste do proximo rateio"*; **o financeiro nao tem essa coluna nem esse ajuste.** <br>**Nao mexi em codigo, e a razao e o dono:** trocar a base de faturamento reabre a **`Q-021 / AUD-03`**, fechada em 28/07 pela resposta 9a do contador (*"a base e a serie REAL por competencia, e nao a alocada em contrato nem o menor dos dois"*) — decisao com dono e com parecer contabil. Aquele *"menor dos dois"* e do eixo da GERACAO (alocado x gerado) e nao do consumo, entao **nao resolve esta**; o que ele mostra e que a base ja foi decidida uma vez com o contador na sala. **Decidir:** o financeiro passa a limitar a cota pelo consumo lido (e ai depende do leitor), mantem a cota cheia com o saldo virando ajuste do proximo rateio (a coluna que a planilha ja tem), ou fica como esta e a divergencia com o processo manual e assumida. Medicao em `PLANILHA-cobranca-medida-2026-08-12.md` §2 | Vinicius + contador |
| ~~**Q-DOCG3-10**~~ | ✅ | **21/08/2026 — RESPONDIDA PELO DONO, E A RESPOSTA E A QUE A MEDICAO SUSPEITAVA.** Verbatim: *"Alem dos valores destinados a equatorial, **que sao as tarifas minimas**, a divisao ocorre da seguinte maneira: 70% vai para o dono da usina e 30% fica na g3"*. Ou seja: **"tarifa minima" e o nome que a planilha da ao que vai para a distribuidora** — nao e custo de disponibilidade, e nao e minimo de coisa nenhuma. A entrada de 12/08 tinha medido exatamente isso sem poder afirmar: *"a coluna carrega outra coisa, provavelmente o resto real da fatura da Equatorial"*, e os quatro valores em kWh equivalente (55,57 · 74,35 · 108,17 · 331,69) nao batiam com nenhum dos tres pisos brasileiros (30/50/100). **O nome estava errado; o numero estava certo.** **E o codigo ja fazia o certo desde 21/08:** a juncao mapeia `total_equatorial_centavos` para `valor_tarifas_concessionaria_centavos`, que `src/dominio/split.ts` trata como **repasse PURO, sem percentual** — o `PRD` §5.1 ja dizia *"nao ha percentual, ha o valor"*. Entao a parte da distribuidora **nao entra na divisao 70/30**, que e literalmente a frase do dono. Nenhuma linha mudou por causa desta resposta; o que mudou foi poder afirmar que esta certo. **O que NAO fecha junto, e continua na `Q-DOCG3-11`:** a QUEBRA dessa parte (nao compensado, iluminacao, bandeira, demais) e o aval fiscal sobre ela. Saber que o total vai inteiro para a distribuidora nao diz como ele se decompoe. Corpo original abaixo: **12/08/2026 — A "TARIFA MINIMA" DA PLANILHA NAO E UMA TARIFA MINIMA.** A legenda diz *"valor fixo… independentemente da geracao solar"* e o cabecalho de cada aba pede **um** valor por usina. **Medida em kWh equivalente (`minima / tarifa`), ela varia por cliente:** 55,57 · 74,35 · 108,17 · **331,69** kWh. Custo de disponibilidade no Brasil e **30/50/100 kWh** (mono/bi/trifasico) — nenhum dos quatro e um desses, e 331,69 kWh nao e minimo de coisa nenhuma. **A coluna carrega outra coisa**, provavelmente o resto real da fatura da Equatorial (iluminacao publica, bandeira, tributos, nao compensado). E ela **entra no boleto**: `Valor a Cobrar = Valor com Desconto G3 + Valor Tarifa Minima`, verificado ao centavo em 4 de 4 linhas. Enquanto nao se souber O QUE ela e, nao da para modelar — e a mesma armadilha do `demais` residual do `g3_fatura_unificada`, que absorve erro de leitura e continua fechando a conta. Cruza com a `Q-EQTL-CAMPOS-01`: se for o resto da fatura, quem responde e o leitor | Vinicius |
| **Q-DOCG3-11** | 🟡 | **14/08/2026 — DECIDIDO PELO DONO: SEGUIR A DECOMPOSIÇÃO DA REFERÊNCIA (Vinicius, regra 10).** A decisão escolhe a lista da referência — **não compensado + iluminação pública + bandeira + demais** — sobre a do `PRD` §5.1 (fio B + iluminação + encargos). **O que ela NÃO fecha, e por isso a questão cai de 🔴 para 🟡 em vez de ✅ — três condições:** **(a)** a metade **fiscal continua do contador** (cruza com `Q-EQTL-CAMPOS-01`): o que "não compensado" e "demais" incluem de tributo (ICMS, PIS/COFINS embutidos), bandeira, multa e juros, e o que é **repasse puro** vs. o que a G3 já cobra em `valor_consumo`; **(b)** a referência **não tem linha de fio B**, e o cliente de GD paga fio B (TUSD sobre a energia compensada, no phase-in do Marco Legal da GD) — então é preciso confirmar, contra uma fatura de GD **real**, que o fio B esteja **capturado** dentro de "não compensado"/"demais", e não perdido do repasse; o `demais` é resíduo por subtração e absorveria essa perda **em silêncio** (`Q-DOCG3-10`); **(c)** a única fatura Equatorial medida até hoje é **convencional, sem GD** (`Q-EQTL-CAMPOS-01`), então o mapa rótulo→campo ainda é suposição. **A base do split NÃO foi reescrita**, de propósito: `src/dominio/fatura-concessionaria.ts:67-70` e a soma em `:309` (`fio_b + iluminacao_publica + encargos`) seguem intactas, porque mover a base do split — que muda comissão, repasse e `liquido_g3` sem erro visível — sobre lógica nunca confrontada com uma fatura de compensação seria o improviso que a própria regra 10 e a regra 1 recusam. **Plano de execução, pendente de (b) uma fatura de GD real + (a) o aval do contador:** trocar o conjunto de campos de `CamposDaFatura` e a soma de `fatura-concessionaria.ts` para `não_compensado + iluminação + bandeira + demais`, mantendo `total_a_pagar` como conferência (`F2c`) e fixando a nova base num teste que falha se ela mudar por construção. Canal do contador: `PAUTA-contador.md`. **Corpo de 13/08/2026 abaixo — A DECOMPOSIÇÃO DO REPASSE NÃO BATE, e as duas fontes são defensáveis.** O `PRD-v2.2` §5.1 define `valor_tarifas_concessionaria` como **fio B + iluminação pública + encargos**, e `src/dominio/fatura-concessionaria.ts` implementa exatamente essas três parcelas. O prompt de extração de `g3_fatura_unificada@36e964e` decompõe o repasse da Equatorial de outro jeito — **consumo não compensado + iluminação pública + bandeira + demais** — e a expressão **"fio B" não aparece em nenhum dos dois arquivos da referência**. As duas decomposições não são traduzíveis uma na outra por inspeção: ou uma está errada, ou elas nomeiam recortes diferentes da mesma fatura. A consequência é direta e cara — é a base do split (`PRD` §5.4 e §5.5) que exclui esta parcela, então errar o recorte erra comissão e repasse sem erro visível. **Não vira suposição minha** (regra 10). Cruza com a `Q-EQTL-CAMPOS-01` (metade é do contador) e com a `Q-DOCG3-10`. Medição em `REFERENCIA-fatura-unificada-2026-08-13.md` §3.1 | **dono + contador** |
| **Q-DOCG3-12**  | ✅ | **13/08/2026 — OS TRÊS CARTÕES DA FOLHA 1 MUDARAM DE SIGNIFICADO NA REFERÊNCIA, e a versão nova é a defensável.** Em `ec1e71b` — o commit que o `PLANO-documento-modelo-g3-2026-08-12` mediu — o primeiro cartão dizia *"Sem a G3 você pagaria"* e mostrava `semG3 = energia + repasses Equatorial`, ou seja **a conta inteira**. Em `fefe073` ele virou *"Seu consumo sem a G3 Solar"* e passou a mostrar **só a energia** (`integral`); o terceiro cartão deixou de ser *"Sua economia neste mês"* e virou *"Seu consumo com a G3 Solar"*; e entrou uma nota em 6,5pt: *"Desconto aplicado sobre a energia compensada. Encargos e tarifas da distribuidora não têm desconto."* **A diferença não é rótulo:** a versão antiga diluía o desconto de 20% contra um total que inclui repasse onde não há desconto nenhum, e é o número que o cliente contesta na primeira conferência. A versão nova compara a mesma grandeza dos dois lados. Adotamos a semântica nova? Registrado em `REFERENCIA-fatura-unificada-2026-08-13.md` §2.1 **— FECHADA E CONSTRUIDA EM 14/08.** O dono mandou seguir a recomendacao: coluna em `identidade_de_cobranca`. A **migration 26** adiciona `razao_social` e `cnpj`, NULAVEIS (a tabela ja tem linha em producao, e coluna NOT NULL sem default exigiria adivinhar um valor que sai IMPRESSO na fatura). O CNPJ e `text` e nao numero, por causa do zero a esquerda; o banco confere o FORMATO (14 digitos) e a aplicacao confere o **digito verificador** (`CnpjDoEmissorInvalido`, 422) — mesma divisao da chave Pix. A trilha da regra 9 e a policy de isolamento **nao precisaram ser criadas**: a tabela ja tem as duas desde a migration 21, e coluna nova entra sozinha — o retorno de ter posto trilha por TABELA e nao por coluna. **APLICADA EM PRODUCAO em 14/08** (`applied_steps_count = 1`, sem rollback), conferida pela `DIRECT_URL`. **E aplicar expos um buraco:** a coluna existia e nao havia formulario — entrou o cartao *"Quem emite a fatura"* na aba Documento, e com ele o **Beneficiario do boleto** deixou de faltar, que era o pedido original desta questao. Producao tem `razao_social` **vazia** ate alguem preencher. **E o achado reapareceu num segundo lugar enquanto se construia:** nao era so o Beneficiario da faixa; o cabecalho e o rodape da folha 1 tambem trazem razao social e CNPJ, e na referencia os dois estao **escritos no codigo** — o que serve a um app de uma empresa so e poria o CNPJ da G3 na fatura de outro tenant. **Ausente continua sendo AUSENTE e nao travessao** (G1d/G1f) **— FECHADA EM 14/08, na recomendacao: adotamos a semantica NOVA.** Os cartoes comparam **energia contra energia** e a folha carrega a nota *"o desconto incide sobre a energia compensada"*. **Decidida e NAO construida**, e a distincao importa: os tres cartoes exigem tarifa cheia e energia compensada, que nao existem deste lado (`Q-DOCG3-02`, `Q-TARIFA-CRM-01`). A folha 1 entrou hoje **sem eles**, e diz isso — `folha.faixas_ausentes` nomeia a faixa, o motivo e a questao que a destrava | **Vinicius** |
| **Q-REF-SEGREDO-01** | 🔴 | **13/08/2026 — `api/ler-fatura.js` PUBLICA METADADO DA CHAVE E É PROXY ABERTO. Medido no ar, hoje, sem autenticação.** `GET https://g3-fatura-unificada.vercel.app/api/ler-fatura` devolve `{"existe":true,"tamanho":108,"comeca":"sk-ant-api03","termina":"JAAA","temEspaco":false}` — `raw.slice(0,12)` e `raw.slice(-4)` da `ANTHROPIC_API_KEY`. O prefixo é público por natureza; **os quatro últimos caracteres e o comprimento exato não são**, e juntos tornam a chave um alvo verificável. E o `POST` repassa o corpo do cliente **inteiro** para `api.anthropic.com/v1/messages` com a chave do servidor, **sem autenticação, sem verificação de origem, sem limite de taxa e sem restrição de modelo ou `max_tokens`** — qualquer pessoa com a URL gasta na conta do dono. **É a regra 5 num ângulo que ela não cobria:** o segredo é de plataforma (variável de ambiente está certo), mas a rota o **reexporta** por outro caminho. Recomendação: **girar a chave**, apagar o ramo `GET` (é diagnóstico que ficou) e pôr autenticação e teto no `POST` **antes** de qualquer coisa deste lado consumir o mesmo desenho. O repositório é de outra conta e **nada foi tocado nele**. Detalhe em `REFERENCIA-fatura-unificada-2026-08-13.md` §10 | **dono** |
| **Q-LEITOR-01** | 🟡 | **14/08/2026, fim do dia — TRES DAS QUATRO FRONTEIRAS FORAM FECHADAS, e a chamada real continua sem prova.** O que mudou: (1) as duas rotas sairam de dentro da transacao — rodavam com uma transacao Postgres de 15 s aberta, e 18 s de chamada produzem **P2028** com o resultado de uma chamada ja PAGA descartado (reproduzido); (2) a permissao passou a ser conferida de verdade — o cabecalho afirmava `exigir('ler')` desde a manha e o handler nao o chamava; (3) o erro da API vira erro NOSSO na fronteira: um 401 dela chegava como 401 nosso e **deslogava quem estava faturando**, levando a fatura em edicao junto. Mais: teto de arquivo ligado ao teto de corpo (o de 32 MB era codigo morto — o servidor recusava em 1 MB), tipo lido dos BYTES e nao do rotulo, e `tests/erros.ts` (73 verificacoes) sobre o tradutor que nao tinha nenhuma. **CONTINUA AMARELA pelo motivo original:** nenhuma chamada real ao modelo foi feita contra um PDF de verdade. Corpo anterior: **14/08/2026 — O LEITOR POR MODELO DE VISAO NUNCA FOI CHAMADO DE VERDADE, e o codigo ja citava esta questao antes de ela existir.** `src/concessionaria/leitor-visao.ts` entrou hoje com 21 verificacoes, e elas prendem **o contrato e a fronteira**: que os prompts continuam os da referencia palavra por palavra, que o schema cobre os 21 campos sem sobra, que nenhum float sobrevive a `numeroParaTexto`, e que PDF e imagem viram blocos diferentes da API. **Nenhuma delas prova que a chamada funciona**, e provar exige duas coisas que este repositorio nao tem: um **PDF real de fatura da Equatorial** e uma **`ANTHROPIC_API_KEY` configurada**. A chave e do dono e cruza com a `Q-REF-SEGREDO-01` (girar antes de reusar); o PDF depende do acesso ao portal, que depende do CPF do titular — o **item 1.1** da fila, em 0 de 29. **Enquanto isso a aba funciona por digitacao manual** e as duas rotas devolvem **503** com a mensagem nomeando o que falta, entao a ausencia nao e silenciosa. O que fica sem prova ate la: se o modelo acerta os rotulos REAIS da fatura (`CONSUMO SCEE`, `CONSUMO NAO COMPENSADO`, `ADC BANDEIRA`, a tabela lateral de 13 meses) e se a tarifa volta com as **seis casas** — truncar em duas custa R$ 2,90 numa UC, medido em `R22`. Registrada porque o codigo ja a citava e ela **nao existia**, que e exatamente o defeito que o cabecalho do `CLAUDE.md` descreve | **Vinicius** |
| ~~**Q-CICLO-01**~~ | ✅ | **21/08/2026, fim do dia — RESOLVIDA PELO DONO: o caminho oficial é o UNIFICADO.** Verbatim: *"vamos com o caminho da fatura unificada"*. **E foi construída no mesmo dia**, porque o custo de construir só sobe: as duas tabelas tinham **zero linhas**, e ligar duas tabelas vazias é uma coluna — depois do primeiro dinheiro seria migration com valor gravado dentro, que é a lição que a `Q-PAGAMENTO-01` já cobrou deste projeto uma vez. **O que entrou:** migration 34 (`registro_de_fatura_unificada.fatura_id`, FK composta, índice **cheio** pela regra 11), `src/dominio/fatura-do-registro.ts` (a triagem, pura, 42 verificações), `src/repos/fatura-do-registro.ts` (o `INSERT ... SELECT` que copia os centavos **de dentro do banco**), duas rotas e o botão *"gerar cobrança"* na aba da fatura unificada — porque um ato que só o `curl` alcança não é um ato, e este projeto já pagou por esse erro. **O achado que tornou a construção barata:** o motor de split **já tinha a forma exata** da conta unificada, e isso foi medido e não suposto — ele lê da fatura exatamente `valor_consumo_centavos` e `valor_tarifas_concessionaria_centavos`, e a conta unificada produz exatamente `energia_g3_centavos` e `total_equatorial_centavos`. O `PRD` §5.1 já descrevia o repasse à concessionária como *"não há percentual, há o valor"*, que é literalmente o que a parte da Equatorial é numa fatura unificada. **Nenhuma linha do motor de split mudou.** **O ganho de operação, medido:** a camada `vencimento` (46 de 46 vazias) **deixa de bloquear** quando a conta traz a data — e ela traz. Não é default inventado: é a data que o cliente já tem no papel. O cadastro fica como segunda fonte. **APLICADA E PROVADA no mesmo dia:** a migration passou pela guarda de identidade, foi conferida no catálogo (coluna, FK composta e índice cheio, os três presentes) e o `npm run ensaio-juncao` exercitou a gravação **contra o schema real** — originador, contrato e conta lida como fixture, fatura criada, **12 verificações**, e `ROLLBACK` no fim com a última delas contando as tabelas para provar que nada ficou. Era o único jeito: produção tem zero contratos e `fatura.contrato_id` é `NOT NULL`, e esta VPS não tem PostgreSQL local para o `tests/repos.sh`. **O que a decisão NÃO removeu, e vale dizer:** contrato, usina, rateio e geração continuam obrigatórios — não para calcular o valor, que a conta dá, mas porque é da usina que sai o repasse e do contrato que sai o tier congelado. Escolher a conta como fonte mudou **de onde vem o valor**, não **quem recebe**. Corpo original abaixo: **21/08/2026 — EXISTEM DOIS CAMINHOS DE FATURA NESTE SISTEMA E ELES NÃO SE ENCONTRAM. Qual é o oficial?** Não é suposição: está escrito em `src/dominio/folha-unificada.ts` — *"As duas convivem enquanto a aba nova nao substitui a Previa. E redundancia, e esta datada: some quando o fluxo antigo sair."* O que nunca aconteceu foi a **decisão** de qual sai. **Caminho A**, a fatura contratual: valor = geração medida × percentual de rateio × tarifa, grava em `fatura`, opera **em lote** pelo mês inteiro, imprime **5 das 7 faixas**, e gera boleto conciliável → liquidação → split → contas a pagar. **Caminho B**, a fatura unificada: valor lido **da conta da Equatorial**, grava em `registro_de_fatura_unificada`, opera **uma UC por vez**, imprime **as 7 faixas** — e não gera nada disso. **A consequência é a que decide:** o documento que o cliente efetivamente recebe, o de 7 faixas que o dono construiu e pediu que substituísse o processo inteiro, é o único dos dois que **não consegue pagar o dono da usina**. Ele não vira `fatura`, então não vira liquidação, então não vira split. Medido em 21/08: `fatura` **0 linhas**, `registro_de_fatura_unificada` **0 linhas** — ou seja, a hora mais barata que vai existir para escolher. **Enquanto isto não for respondido, código escrito da fase 6 em diante pode ser jogado fora.** Mapa completo em `PLANO-ciclo-do-cliente-2026-08-21.md` | Vinicius |
| **Q-CICLO-02** | 🟡 | **21/08/2026, fim do dia — DUAS DAS TRÊS FORAM RESPONDIDAS POR DERIVAÇÃO E CONSTRUÍDAS; a terceira é do contador e continua aberta.** **(a) cria ou enriquece?** — **cria**, e não foi escolha de gosto: `fatura.contrato_id`, `usina_id`, `geracao_kwh_competencia` e `percentual_rateio_aplicado` são `NOT NULL`, então não existe fatura à espera de ser enriquecida — ela nasce da conta lida mais o cadastro local, numa transação. **(b) qual valor vai para a base do split?** — `energia_g3_centavos`, e a resposta estava no motor, não numa decisão nova: `src/repos/split.ts` já lê exatamente as duas parcelas que a conta unificada produz. Mapeamento de um para um, sem recálculo — os centavos são copiados **de dentro do banco** por `INSERT ... SELECT` e nunca sobem para o Node. **(c) o que acontece quando os dois discordam?** — não vira sinal booleano, e a razão é o critério de ruído da R25: `geração × rateio` e `compensada_kwh` são **grandezas diferentes** e quase nunca são iguais, então um `divergiu` seria verdadeiro em toda fatura. Virou `conferirAlocacao()`, que devolve os dois números e a diferença em kWh, exata e sem float — *"aloquei 500 e o cliente compensou 480"* é pergunta de negócio legítima, e é **capacidade nova**: os dois números nunca tinham se encontrado. **AMARELA E NÃO FECHADA** pelo que sobrou: a decomposição da parte da Equatorial que alimenta o repasse é a **`Q-DOCG3-11`**, que tem decisão do dono desde 14/08 e **espera o aval fiscal do contador** e uma fatura de GD real para validar o mapa. Enquanto isso não vier, o que está construído reparte sobre o total da Equatorial como repasse puro — que é o que o `PRD` §5.1 manda — e não sobre a quebra dele. Corpo original abaixo: **21/08/2026 — se a `Q-CICLO-01` responder "o unificado", COMO ele vira `fatura`?** `registro_de_fatura_unificada` não tem `fatura_id` e não ganha um sem decisão: a chave dele é `(tenant_id, numero_uc, competencia)` e a da fatura sai do contrato. Três perguntas dentro de uma: (a) o registro **cria** a fatura, ou a fatura existente é **enriquecida** pelo que foi lido? (b) qual valor vai para a base do split — o consumo derivado da geração medida, ou a energia G3 lida da conta? A escolha muda quanto o dono da usina recebe, e é a mesma fronteira da **`Q-DOCG3-11`**, que já tem decisão do dono e espera o aval fiscal; (c) o que acontece quando os dois discordam — porque vão discordar, e discordar sem que nenhum lado pareça errado é o modo de falha próprio de espelho. **Depende da `Q-CICLO-01` e não deve ser respondida antes dela** | Vinicius + contador |
| **Q-GERACAO-USINA-01** | 🔴 | **21/08/2026 — DUAS DAS QUATRO USINAS NUNCA TIVERAM GERAÇÃO MEDIDA, e uma terceira só tem um mês.** Medido nas **duas pontas** no mesmo dia — o espelho e a `financeiro.geracao_mensal` do CRM concordam, então **não é defeito de sincronia, é medição que ninguém lançou**: `0002` tem 7 meses (01 a 07/2026); `0001` tem **um**, 06/2026; `0003` e `04` têm **zero, em qualquer mês**. A base de faturamento é a geração MEDIDA (`Q-021`, fechada em 28/07), então a UC recusa por `sem_geracao_lancada` e não há default possível. **O custo está contado:** a usina `04` tem **11 UCs** e a `0003` tem **1** — 12 das 46, que não faturam em mês nenhum enquanto isso valer. E as 11 da `04` carregam um segundo bloqueio sobreposto: **nenhuma está `ativado`** no rateio do CRM. Consequência de calendário: **06/2026 fecharia com 28 de 29** UCs faturáveis (R$ 15.367,38 estimados pela `app.consumo_centavos`) e **07/2026 com 9**, porque só a `0002` tem julho. Não há tela para lançar geração deste lado, e não deve haver — é espelho do CRM (regra 4) | operação + dev do CRM |
| **Q-CONTA-ORIG-01** | 🟡 | **21/08/2026 — a conta da distribuidora é lida e NÃO É ARQUIVADA.** `registro_de_fatura_unificada` guarda os 21 campos extraídos e nenhum byte do PDF; não há coluna de arquivo, e não há tabela de anexo em lugar nenhum do schema. **O que isso custa:** quando o cliente contestar um número impresso — e a folha existe justamente para ele conferir —, não há original contra o que conferir a leitura. E a leitura é por modelo de visão: extração errada **não aparece como erro, aparece como valor**, que é a razão registrada de o extrator usar o modelo mais capaz. Decidir: guardar ou não; se sim, onde (coluna, disco, objeto), por quanto tempo, e quem pode abrir — o PDF da distribuidora traz nome, endereço e consumo do cliente | Vinicius |
| **Q-CONTA-LOTE-01** | 🟡 | **24/08/2026 — A PERGUNTA AGORA APARECE NA TELA, e ela ficou mais difícil de adiar.** A prontidão passou a medir o caminho oficial e ganhou a camada `conta_lida_da_competencia`, que acusa **29 de 29** e nomeia esta questão como a que a destrava. Antes o custo das 29 conferências era uma frase num plano; agora é uma linha vermelha que a operação lê todo mês. **Nada foi decidido aqui** — a camada CONTA o trabalho e não escolhe como ele entra, que é o que a regra 10 manda. O que ela removeu foi o silêncio: com contrato digitado, o relatório antigo mandaria preencher 29 vencimentos que o caminho oficial não usa e ficaria verde depois, sem nunca dizer que faltavam as contas. Corpo original abaixo: **21/08/2026 — o caminho da fatura unificada é UMA UC POR VEZ, e a carteira tem 29.** `POST /faturas/unificada/compor` e `POST /faturas/unificada/registros` recebem um conjunto de campos cada, e a tela sobe um PDF por vez: 29 uploads, 29 conferências, 29 registros, todo mês. O caminho contratual tem lote (`ensaiar` → `compor` o mês inteiro) e é justamente o que **não** imprime as 7 faixas — a assimetria é a `Q-CICLO-01` aparecendo de lado. Decidir: se vale um caminho de lote (uma pasta de PDFs, uma fila), ou se a conferência individual **é** o ponto do processo e o custo por conta é aceito. A pergunta tem prazo curto por um motivo simples: a resposta muda de peso entre 29 clientes e 100 | Vinicius + operação |
| **Q-PODEFATURAR-01** | 🟡 | **24/08/2026 — «Pode faturar» NUNCA pode ficar verde, e o motivo é uma compra que ninguém fez.** Achado verificando o ciclo ponta a ponta contra produção. A camada `cobranca_sicoob` está marcada `bloqueia_fatura`, e `pode_faturar` só é verdadeiro quando **toda** camada dessa marca está `ok` — ou seja, o cartão do topo da tela de Pendências fica **NÃO** até o certificado A1 existir, por mais que a operação feche as outras onze. **E a própria camada diz o contrário do que a marca faz:** o texto dela é *«Sem ele a fatura existe e é cobrável por outro meio, mas não há boleto»*, e a decisão de 17/08 tornou isso literal — o Pix estático cobra e o boleto emitido à mão no portal entra pela aba Emissão e cobrança (migration 32). **Medido em 24/08:** 1 chave Pix ativa e escolhida como padrão, modelo padrão definido, e `conector_cobranca` com **0 linhas**. Ou seja, no dia em que a primeira fatura sair e for paga por Pix, a tela estará dizendo que não dá para faturar. **A direção do erro é a menos perigosa** — vermelho falso não autoriza nada, e o projeto teme o verde falso —, mas o cartão perde a função: quem opera aprende a ignorar um NÃO que nunca muda. **As três saídas:** (a) `cobranca_sicoob` vira `bloqueia_split`? não — não é o split que ela estraga, e a marca mentiria de outro jeito; (b) ela deixa de bloquear e vira camada informativa, e `pode_faturar` passa a significar *«a cobrança sai por algum meio»*; (c) `pode_faturar` continua significando *«sai por boleto registrado»* e o cartão ganha um segundo estado, «sai por Pix». **Não foi improvisado (regra 10):** mudar o que `pode_faturar` promete é decisão de quem cobra, não de quem implementa, e a alternativa (b) muda a resposta de um cartão que autoriza trabalho. Nada no código foi alterado por conta desta questão | **Vinicius** |
| **Q-SEGVIA-01** | 🟡 | **21/08/2026 — A SEGUNDA VIA NAO CONSEGUE REPETIR UMA CONFERENCIA, e ela nao finge que consegue.** A migration 29 declarou dois propositos para `registro_de_fatura_unificada`: a economia acumulada e **a segunda via**. A primeira foi construida no dia; a segunda so agora, e ela passou a valer mais com a `Q-CICLO-01` — a folha de sete faixas virou o documento oficial e so existia enquanto os campos estivessem na tela. **O que a volta reproduz:** os 21 campos, os parametros CONGELADOS da linha, a faixa de pagamento, e os nove centavos, que sao **recalculados por `calcular()` e conferidos** contra o gravado (devolver os centavos direto seria uma segunda implementacao da conta). **O que ela NAO reproduz:** `outros_encargos` — o que o extrator LEU como demais encargos —, porque a migration guarda o **residuo derivado** e nao a leitura. Ele existe para acusar `residuo_discorda`, a guarda que impede o residuo de absorver em silencio um erro de leitura das outras tres parcelas. **A segunda via devolve o campo VAZIO de proposito:** `calcular()` so acusa quando a string nao e vazia, entao vazio significa *"nao conferido aqui"* e nao *"conferido e bate"*. Preenche-lo com o residuo faria os dois concordarem **por construcao**, e a folha afirmaria uma conferencia que ninguem fez — que e a classe de silencio que este projeto persegue desde a regra 3. **A decidir:** guardar `outros_encargos_lidos_centavos` na tabela (migration aditiva, e a tabela tem zero linhas hoje — o momento mais barato), ou aceitar que a 2a via nao repete essa conferencia e deixar dito. Provado em `tests/segunda-via.ts` (`V4a`, `V4b`), que mede exatamente o contraste: a 1a via acusa, a 2a se cala sem afirmar | Vinicius |
| O-02 | 🟢 | Quando um cliente novo começa a ser faturado | operação |
| AUD-11 | 🟢 | Sync de 30 min é requisito ou pode relaxar? | Vinicius |

## 6. F3 — split e comissão

| ID | Nível | Pergunta | Quem |
|---|:--:|---|---|
| **Q-022** | 🔴 | Como o contrato é atribuído ao originador, com `partner_id` em 3%? Medir antes o custom field `Comissionamento` | Vinicius |
| ~~**Q-EIXO-FUNCIONARIO-01**~~ | ✅ | **FECHADA EM 03/08/2026 pela resposta do dev do CRM, e a resposta foi "nenhuma das duas".** O eixo nao e `vendedor_origem` nem `responsavel_atual`: e o **credito congelado no momento do ganho**, regra adotada pela G3 em 01/08 e textual na migration deles — *"a venda e de quem e o responsavel do lead quando e dado ganho nos funis de vendas"*. Imutavel por gatilho; corrigir exige revogar e recreditar, com trilha. **`vendedor_origem` e o palpite do round-robin na CRIACAO do lead** — e a propria G3 ja o declarou errado por escrito: em 30/06/2026 18:30:28 rodou uma correcao em 15 leads *"lead criado pela Renata devolvido a ela (havia sido atribuido ao OutSales pelo round-robin na criacao)"*, que arrumou o responsavel e **nao** o `vendedor_origem`, entao ele guarda o palpite ate hoje. Em dois casos o palpite durou **10 e 14 segundos** (`G3-0195`, `G3-0386`). `responsavel_atual` e o dono da negociacao HOJE, coluna viva. **O custo de nao ter perguntado, medido em 03/08:** pelo eixo novo o mapa de 30/07 esta errado em **12 das 41 UCs**, e o Out Sales sai de **24,2% para 4,6%** da carteira — **5.858,8 kWh/mes, 19,6%**, trocam de dono. Digitar pelo mapa velho pagaria a pessoa errada sem erro e sem log, e a R20-b congela no `rascunhar`. **E a `000009997201253` (OSVALDO, lead arquivado) deixou de ser pergunta:** era a unica UC que nenhum caminho de leitura entregava, e o credito a entrega — **Renata**. **Mapa vigente: `ATRIBUICAO-originador-2026-08-03.md`;** o de 30/07 recebeu cabecalho de SUPERADO, corpo intacto. **O eixo e legivel por view, 41 de 41** — ver `Q-VIEWSCRED-01`. Abriu `Q-PARCERIA-01` 🔴 e `Q-COMISPCT-01` 🟡. Corpo original abaixo: **PERGUNTADA AO DONO EM 03/08, e a resposta redirecionou a questao em vez de fecha-la: *"o CRM foi alterado nesse sentido, peca uma pequena auditoria para o DEV do CRM que eu te devolvo corretamente"*.** Ou seja: o eixo **nao** se decide olhando o que as 8 views expoem hoje, porque o CRM mudou desde a medicao de 29/07 e a mudanca ainda nao foi lida por ninguem deste lado. A questao **continua vermelha e continua bloqueando o passo 5** (digitar os 39 contratos), pelo mesmo motivo de antes: a **R20-b congela o tier no `rascunhar`** e `contrato` nao tem edicao. Pedido escrito em **`PROMPT-dev-crm-rodada5-2026-08-03.md`**. **O que ele pergunta, e por que cada uma:** (1) o que mudou em `responsavel`/`vendedor_origem` e quando — sem a data nao da para saber se o mapa de 30/07 ja nasceu velho; (2) se existe coluna, tabela ou etapa **nova** que carregue atribuicao — se existir e nao estiver nas views, o financeiro nao a le e o mapa continua incompleto por construcao; (3) o que as views `financeiro.*` passam a expor, porque a `SPEC-002` R1 proibe ler tabela base e o que nao esta em view **nao existe** para nos; (4) se `merge_leads` segue rotina — a `Q-CRMCODIGO-01` mediu 76 merges num dia, e isso decide se qualquer mapa tem prazo de validade. **Enquanto nao voltar, o eixo em vigor continua sendo o `vendedor_origem`** (decisao de 29/07), e nada foi digitado: `contrato` e `originador` seguem em **0**. Corpo original abaixo:  **A descrição do dono em 30/07 nomeou quem recebe comissão como *"responsáveis dos leads"*, e o eixo decidido é o `vendedor_origem`. São colunas diferentes do CRM.** A decisão em vigor é de 29/07 — *"o originador vai ser o `vendedor_origem` até segunda ordem"* (`Q-ORIGVEND-01`, `ATRIBUICAO-originador-2026-07-30.md`). **O repositório já mediu os dois eixos lado a lado, e é por isso que esta entrada é confirmação e não pesquisa:** eles **divergem em 43 dos 80 ganhos**; nos 15 cards do funil `Rateio` o `responsavel` é a **mesma pessoa em 15 de 15** com o `vendedor_origem` variando — ali `responsavel` é **dono operacional do card, não quem vendeu** —, e por `responsavel_atual` uma pessoa saltava de 1 para 28 atribuições. Foi essa medição que superou a recomendação do `RESUMO-SESSAO-3` (*"`responsavel` paga, `vendedor_origem` só registra"*), **por medição e não por preferência**. **Por que é vermelha, e é por prazo e não por dúvida:** o passo 5 do caminho crítico é digitar os 39 contratos, e a **R20-b congela `originador_tipo_no_fechamento` no `rascunhar`** — `src/repos/contrato.ts` expõe `rascunhar`, `ativar`, `suspender`, `encerrar` e `renovar`, e **nenhuma edição**. Digitar pelo eixo errado paga a pessoa errada **sem erro e sem log**, e o conserto é `encerrar` + `renovar`, que abre linha nova, zera `faturas_cheias_pagas` e deixa na trilha uma renovação que não houve comercialmente. **O mais provável é que "responsáveis dos leads" tenha sido descrição informal de *quem vendeu*** — mas confirmar custa uma frase e não confirmar custa uma reescrita de contratos, e é a mesma classe da `Q-ORIGINADOR-01`: *entra em produção errado e não tem caminho de volta*. **Decidir:** (a) confirmar `vendedor_origem`, e esta entrada fecha sem consequência; (b) trocar para `responsavel`, e aí o `ATRIBUICAO-originador-2026-07-30.md` inteiro é refeito **antes** de qualquer contrato ser digitado. **Segundo ponto que a mesma descrição encosta, e não é o mesmo assunto:** *"funcionário"* no sentido de **folha** — salário, pró-labore, encargos — é F5 (*folha importada*) e não existe no sistema. A comissão ao `vendedor_g3` é `split_item` e roda na liquidação; as duas coisas não se confundem, e só a segunda existe. Ver `REVISAO-VERTENTES-2026-07-30.md` §4 | Vinicius | <br>**O dev respondeu no mesmo dia, e a resposta INVERTEU a pergunta:** em vez do eixo, ele perguntou **de onde saiu a lista de 39**, supondo planilha da operacao com contratos ainda nao ativados. **A suposicao nao se sustenta e foi medida:** as 39 tem `crm_usina_cliente_id` preenchido em **39 de 39** — sao o `contrato_id` do proprio CRM, lidos pelo conector, nunca digitados. A diferenca 39 → 41 tem duas causas nomeadas (`Q-UCMUDOU-01` e atraso de um ciclo), e nao "nao ativados". **O que a rodada expos, e vale mais que a contagem:** tres coisas que o dev enxerga e as views **nao expoem** — `troca_titularidade` vem **NULL em 41 de 41**, a usina `407706301217` nao esta entre os 4 codigos de `financeiro.usinas`, e **nao ha coluna de situacao** do contrato de rateio, entao toda linha da view e lida como valida. Resposta e pedido em `RESPOSTA-dev-crm-rodada5-2026-08-03.md` §4. **A pergunta original do eixo segue sem resposta**
| **Q-PARCERIA-01** | 🔴 | **05/08: ELA SAIU DO CAMINHO CRITICO DAS 29, e continua VERMELHA — a diferenca e de posicao, nao de estado.** Remedido antes de construir o importador de contratos: as tres UCs do Edimar (`000241968901278`, `000006990101222`, `000039416101210`) estao **as tres `nao_ativado`**, na usina `04`, e **nenhuma esta entre as 29 faturaveis**. Conferido pelo outro lado, que e o que fecha a medicao: os **29** creditos vigentes das 29 UCs faturaveis tem **`parceiro_id` NULO em 29 de 29**, e sao **dois** vendedores (Renata 26, Out Sales 3). Ou seja: **os 29 contratos podem ser digitados sem esta decisao** — o README a listava como item 5 travando o passo 6, e isso estava vencido. **O que NAO mudou, e e o motivo de continuar vermelha:** a questao volta a travar no dia em que o CRM ativar aquelas tres, a R20-b continua congelando no `rascunhar` e continua nao havendo edicao. `npm run contratos --modelo` marca as tres com o ID desta questao na coluna `originador_crm` e imprime um bloco proprio, em vez de deixa-las passar — quem digitar por engano paga a pessoa errada sem erro e sem log. Nada digitado: `contrato` = 0. Corpo original abaixo: **O credito congelado do CRM traz vendedor E parceiro na mesma venda, e o nosso `contrato` guarda UM originador so — e a escolha muda a 2a parcela de 25% para zero.** Nova em 03/08, e ela **nasceu da correcao do proprio dev**: ao recreditar as tres vendas do Edimar (carimbo `recredito_parceria`, 03/08), `financeiro.vendas_creditadas` passou a trazer, nas UCs `000241968901278` (9,0%), `000006990101222` (5,0%) e `000039416101210` (4,0%), **`vendedor = Out Sales` E `parceiro_nome = EDIMAR - FERRAGISTA SOL NASCENTE` ao mesmo tempo**, com `parceria_tipo = indicador` e `comissao_pct = 25,00`. **`contrato` tem um `originador_id` e um `originador_tipo_no_fechamento`** — conferido no schema em 03/08 — e nao ha onde guardar os dois. **E a escolha nao e neutra**, medido nas 10 linhas de `regra_comissao` em producao: `terceirizado` paga **25% na 1a e 25% na 2a**; `parceiro_indicador` paga **25% na 1a e 0% na 2a**. A 1a empata, **a 2a e 25% contra zero**. **Nao custa nada HOJE** — as tres estao na usina `407706301217`, que tem `geracao_kwh_mensal` nulo e rende 0 kWh —, mas a **R20-b congela o tipo no `rascunhar`** e `contrato` nao tem edicao: e decisao de antes de digitar, nao de depois. **Isto DESMENTE uma medicao anterior:** a `Q-ORIGVEND-01` registrou que a `SPEC-002` R16 (atribuicao vem de `leads.partner_id`) *"nao colide hoje"* porque **zero** linhas do rateio tinham `partner_id`. **Agora tres tem** — os dois eixos colidem pela primeira vez, e o que mudou foi o dado, nao o metodo. **Decidir, e a pergunta esta na Parte 3 da `RESPOSTA-dev-crm-rodada6-2026-08-03.md`:** (a) paga o **vendedor**, e o parceiro e so registro; (b) paga o **parceiro**, e o vendedor e so registro; (c) **os dois repartem** — e ai o schema muda, e a migration e melhor antes dos 41 contratos do que depois. **Nao escolhi** (regra 10). Nada digitado: `contrato` = 0 | Vinicius + dev do CRM |
| **Q-COMISPCT-01** | 🟡 | **O CRM passou a ter percentual de comissao proprio, em 3 de 41, e nao ha decisao sobre qual manda.** Medido em 03/08: `financeiro.vendas_creditadas.comissao_pct` esta **preenchido em 3 linhas e nulo em 38** — as tres sao as recreditadas do Edimar, com `25,00`. **Hoje os dois lados concordam**: o CRM diz 25,00 para `indicador` e a nossa `regra_comissao` diz 25,00 para `parceiro_indicador` na 1a parcela. **Concordar hoje nao e ter fonte unica.** O CRM **congela o numero no dia do ganho** (imutavel por gatilho); nos resolvemos **por vigencia** (`regra_comissao_sem_sobreposicao`, `EXCLUDE USING gist`). No dia em que a G3 mudar a tabela dela, os dois divergem **em silencio**, e `src/repos/split.ts` usa o nosso. **Nao bloqueia hoje** — nenhuma fatura foi emitida e `split_item` tem 0 linhas — e por isso e amarela e nao vermelha. **Decidir:** (a) o nosso `regra_comissao` manda sempre, e `comissao_pct` do CRM e informativo; (b) o `comissao_pct` congelado manda quando existir, e o nosso e o default — o que exige coluna nova no contrato e vira migration; (c) divergencia vira **sinal** no ciclo, sem escolher — o desenho da R21-b, e o mais barato. **Nao escolhi** (regra 10) | Vinicius + dev do CRM |
| **Q-COMIS-TERC-01** | 🟡 | **O `terceirizado` não está na tabela de comissão do `PRD` §5.4, e a migration 17 precisou repartir o total dele mesmo assim.** O `PRD` §5.4 escalona quatro tipos pela 1ª e 2ª fatura cheia; o `terceirizado` existe no enum e no seed, com **total de 50%**, e não tem linha na tabela do PRD. A migration 17 o backfillou como **25 + 25**, espelhando o `vendedor_g3`, que é o tipo de mesmo total. **O total é fato registrado; a repartição entre as duas parcelas é suposição**, e está escrita no seed em vez de escondida no motor. **A alternativa foi descartada com motivo:** deixar o tipo sem regra faria `app.percentual_comissao` levantar `no_data_found` no primeiro split de um contrato de terceirizado — introduzir uma quebra onde há comportamento funcionando, sendo que a dúvida é sobre o *ritmo* do pagamento e não sobre o *valor devido*. **Decidir:** confirmar 25+25, ou declarar a quebra real | Vinicius |
| ~~**Q-SPEC001-04**~~ | — | ~~`percentual_repasse` vive na usina ou só em `regra_split` versionada?~~ **Resolvida em 26/07 — ver §9** |
| ~~**Q-PRONTIDAO-COMIS-01**~~ | — | ~~A prontidão mostra o mesmo `?` para "não há contratos" e para "39 contratos sem originador"~~ **RESOLVIDA em 29/07 — ver §9.** Corpo original preservado: | Vinicius |
| | | **A camada `regra_de_comissao` da prontidão não distingue "não há contratos" de "há 39 contratos e nenhum tem originador".** Medido em 29/07: `src/repos/prontidao.ts` linhas 137–144 contam `DISTINCT originador_tipo_no_fechamento` **filtrando por `IS NOT NULL` nos dois lados** — tanto em `sem_comissao` quanto em `tiers_em_uso`. Se os 39 contratos forem gravados sem originador (`Q-ORIGINADOR-01`), `tiers_em_uso` fica em **0**, `situar(0, 0, derivada)` devolve `nao_medido` e a tela mostra `?`. É o **mesmo `?` de hoje**, que hoje significa universo vazio e que o `RESUMO-SESSAO-11` §7 explicou como honesto — ali é honesto, aqui esconderia 39 contratos que nunca vão pagar comissão. É exatamente o modo de falha que o `prontidao.ts` foi escrito para não ter: *conta e NÃO decide*, e `nao_medido` não é `ok`. **Depende da `Q-ORIGINADOR-01`:** se a carteira legada não tem comissão, o `nao_medido` está certo e nada muda; se tem, falta uma contagem que acuse contrato ativo sem originador. **Não corrigido de propósito** — qual dos dois é o certo é a decisão, não a implementação | Vinicius |
| AUD-08 | 🟡 | Quem preenche `usinas.dono_lead_id`? Nulo em 3 de 3 — bloqueia repasse | operação |

## 7. F4, F5, F6 e além — o corporativo e o que vem depois

> **Esta seção chamava-se "F6 e além" e passou a nomear a F4 e a F5 em 30/07.** O motivo é o próprio achado da `REVISAO-VERTENTES-2026-07-30.md`: o **módulo corporativo do `PRD` §4.4 — contas a pagar e a receber, pagamento de fornecedor e de funcionário, compras, cartão de crédito — nunca teve entrada neste registro**, e um título que pulava da F3 para a F6 fazia isso parecer intencional. Não era: era ausência.

| ID | Nível | Pergunta | Quem |
|---|:--:|---|---|
| **Q-PAGAMENTO-01** | ✅ | **RESOLVIDA em 03/08/2026 pelo dono, na opcao "`conta_pagar` completa, como o PRD §4.4 desenha"** — contra a alternativa de colunas de quitacao dentro de `split_item`. **Construido no mesmo dia, e a janela fechou com `split_item` ainda em ZERO linhas.** `migration 22`: `conta_pagar`, `pagamento`, `categoria` e `centro_custo`, com RLS FORCE + policy, FK composta, gatilho de auditoria e `DELETE` revogado nas duas que sao prova de dinheiro. `src/repos/split.ts` passou a provisionar a despesa **na mesma transacao** — o PRD §5.5 itens 2 e 3 —, e o `liquido_g3` **nao** entra, porque e receita e provisiona-la como despesa faria a empresa dever a si mesma. <br>**As quatro coisas que o BANCO passou a impedir, e nenhuma depende de alguem conferir:** (1) **pagar a mais** — `CHECK conta_pagar_nao_paga_demais`, exercitado por `INSERT` direto em `P3b`; (2) **provisionar o mesmo `split_item` duas vezes** — indice unico CHEIO sobre coluna GERADA `coalesce(origem_split_item_id, id)`, que e o conserto que a **regra 11 prescreve**: o parcial obvio cobriria exatamente as colunas da FK e viraria relacao to-one no `db pull`, e o `CAT-1` o acusaria; (3) **mudar o valor de uma conta nascida de split** — gatilho `conta_pagar_de_split_imutavel`, porque `CHECK` nao ve o valor anterior; (4) **apagar a prova** — `DELETE` revogado em `pagamento` e `conta_pagar`. <br>**O `status` e `valor_pago_centavos` sao DERIVADOS por gatilho**, nunca escritos pela aplicacao: a alternativa funciona ate o dia em que um caminho novo insere em `pagamento` sem lembrar de atualizar a conta — e o sintoma seria uma conta paga aparecendo como aberta, que e o proprio defeito, reintroduzido pela porta dos fundos. `P2d` afirma a igualdade contra a SOMA da tabela, e nao contra constante minha. <br>**A coluna `Corporativo` da matriz do PRD §3 passou a existir no codigo** (`ler_corporativo`/`escrever_corporativo`): ate 03/08 nao havia entidade dela para governar, e usar o `ler` global daria ao papel `cobranca` — que tem **traco** naquela coluna — a lista de quanto a empresa deve a cada dono de usina. `P6a`–`P6e`. <br>**Verificacoes: 28 com banco** (`tests/repos-conta-pagar.ts`) + **5 no elo apuracao→quitacao** (`K7h`–`K7l` em `tests/repos-carteira.ts`, amarradas aos MESMOS valores que `K7c`/`K7d`/`K7e` conferem no split) + **30 puras** na tela (`web/tests/contas.ts`) — **63 ao todo**, e o delta do `npm test` (1043 → 1106) bate com a contagem na fonte. Tela **Contas a pagar**, a 13a. <br>**O que NAO entrou, e a lista importa tanto quanto a do que entrou:** `conta_receber` (a `fatura` JA E o titulo a receber; uma segunda tabela criaria duas verdades), `conta_bancaria`, `movimento_caixa`, `extrato_importado`, `conciliacao`, `cartao_credito`, `fatura_cartao`, `fornecedor` e `compra` — **nove das treze**, e seguem em `Q-CORPORATIVO-01`. O criterio de corte foi um so: entrou o que impede pagar duas vezes o mesmo repasse, porque so isso tinha prazo. <br>**Dois erros meus que os invariantes do proprio repositorio pegaram:** o gatilho nasceu `SECURITY DEFINER` e o `G4` acusou (invariante 19 — todo `SECURITY DEFINER` do projeto e leitura sem policy, e este ESCREVE; com ele, o `UPDATE` contornaria a RLS de uma tabela de dinheiro a pagar); e o `CASE` do gatilho resolvia para `text` contra coluna enum, `42804`, pego pelo `P2b`. Corpo original abaixo: **O split apura e ninguém quita: não há onde registrar que um repasse ou uma comissão foi PAGO — e a janela para decidir isso sem migration fecha na primeira liquidação.** O `PRD` §5.5 manda **quatro** escritas na mesma transação do split: (1) `movimento_caixa` de entrada, (2) `conta_pagar` provisionada por beneficiário com valor bruto, (3) `conta_pagar` do repasse à Equatorial, (4) receita G3 refletida no fluxo e no DRE. **Medido em 30/07 em `src/repos/split.ts`: a função grava DUAS tabelas** — `split_execucao.create()` (linha 191) e `split_item.createMany()` (linha 205). Mais nada. **E o schema fecha o diagnóstico: `split_item` não tem coluna de pagamento** — não há `pago_em`, não há status, não há vínculo com um pagamento; as colunas são tipo, beneficiário, base, percentual, valor e as duas regras aplicadas. **As três consequências, e nenhuma é teórica:** (a) os relatórios *Repasse por dono de usina* e *Comissão por originador* são extrato de **apuração**, não de **quitação** — a tela responde *"quanto é devido"* e ninguém consegue perguntar *"quanto já saiu"*, porque a resposta não existe no banco; (b) **nada impede pagar duas vezes o mesmo repasse** — não é uma trava faltando, é a entidade inteira; (c) o caixa da empresa não existe: o dinheiro do cliente entra, é repartido em quatro no papel, e o sistema não registra nem a entrada nem as saídas. **Por que decidir AGORA e não quando a F4 chegar** — a pergunta é *onde mora o estado de pagamento*, e as duas respostas têm custos muito diferentes: **(a)** `conta_pagar` com `origem_split_item_id` como o `PRD` §4.4 desenha, mantendo `split_item` imutável e o pagamento como evento separado; **(b)** colunas de quitação em `split_item`, mais barato hoje e **migration em tabela com dinheiro já gravado** no dia em que virar (a). **É exatamente o custo que a `Q-011` evitou ao perguntar antes** — lá a resposta *"não incide retenção"* preservou `split_item` com **um** valor por item. **A janela é agora porque `split_item` tem 0 linhas em produção; depois da primeira liquidação, não tem mais.** **Nota que precisa estar escrita:** o `PRD` §4.4 justifica a entidade `pagamento` dizendo que *"é aqui que a retenção sobre PF mora, sem violar a imutabilidade (`Q-011`)"* — e a **`Q-011` foi resolvida em 28/07: não há retenção** sobre comissão PF, comissão PJ nem repasse. **A justificativa escrita no PRD caducou, e a entidade continua necessária por outro motivo** — registrar que o dinheiro saiu, de qual conta e quando. Sem isso registrado, quem ler a `Q-011` resolvida conclui que `pagamento` deixou de ser preciso. Ver `REVISAO-VERTENTES-2026-07-30.md` §3 | Vinicius |
| **Q-CONTAPAGAR-01** | 🟢 | **Tres escolhas da conta a pagar que o `PRD` nao decide, e sao confirmacao e nao decisao de projeto — mesma forma da `Q-AGENDA-02`.** (a) **O vencimento.** O `PRD` §5.5 diz "vencimento default dia 10 do mes seguinte (**configuravel**)". O dia 10 e do PRD e nao e escolha minha; **o "configuravel" e que nao tem dono nem lugar**. Esta num objeto so (`POLITICA_DE_VENCIMENTO`, em `src/repos/conta_pagar.ts`), com o raciocinio ao lado. **O que NAO e escolha:** o mes seguinte e contado da **COMPETENCIA** e nao do caixa — duas liquidacoes da mesma competencia, uma no dia 2 e outra no dia 28, tem de gerar contas com o mesmo vencimento; contar do caixa faria o repasse de um dono vencer em datas diferentes por acidente de quando o cliente pagou (`K7k`). (b) **O agrupamento do repasse a concessionaria.** O `PRD` §4.4 diz "**agrupavel** por competencia" e nao diz por qual chave. Hoje e **uma conta por `split_item`**, e a escolha tem direcao: agrupar depois e possivel, desagrupar nao — e `origem_split_item_id` e obrigatorio e unico quando existe. (c) **O plano de contas.** `categoria` e `centro_custo` existem como tabela e nascem **VAZIAS**, e as duas FKs em `conta_pagar` sao nullable. Semear um plano na migration decidiria a estrutura do DRE de todo tenant futuro — o mesmo argumento que `layout-do-documento.ts` ja carrega. Consequencia aceita: a conta nascida do split entra **sem classificacao**, e alguem classifica depois. A alternativa (`NOT NULL`) travaria o split, que e o unico caminho que cria conta hoje e que roda sem ninguem por perto | Vinicius |
| **Q-SPEC004-01** | 🟢 | **A F4 ganhou entidade e nao ganhou spec, e isto e divida nomeada e nao esquecimento.** A migration 22 trouxe `conta_pagar`, `pagamento`, `categoria` e `centro_custo`, com o raciocinio inteiro no corpo da migration, no cabecalho de `src/repos/conta_pagar.ts` e nesta questao — mas **nao ha `SPEC-004`**, e o `_TEMPLATE-SPEC` e a hierarquia normativa dizem que fase tem spec. **O que NAO esta faltando:** invariante sem teste (regra 8) — os quatro mecanismos do banco tem verificacao nos dois sentidos, e o elo apuracao→quitacao esta preso aos MESMOS valores que o split confere. **O que esta faltando** e o documento que nomeia as regras com numero (R-alguma-coisa) e lista os testes obrigatorios ao lado de cada uma, como a `SPEC-002` §9 faz — e foi exatamente esse exercicio que, na v1.3 da `SPEC-002`, expos a `Q-ESCOPO-01`. Escrever a spec depois do codigo e a inversao que a propria sessao 12 registrou como problema; fica aberta para nao passar por resolvida | Vinicius |
| **Q-CORPORATIVO-01** | 🟡 | **As treze entidades do `PRD` §4.4 não existem, e a ordem em que entram não está decidida.** Medido em 30/07 por consulta ao catálogo de produção: **30 tabelas em `public`, nenhuma delas** `conta_pagar`, `conta_receber`, `pagamento`, `conta_bancaria`, `categoria`, `centro_custo`, `movimento_caixa`, `extrato_importado`, `conciliacao`, `cartao_credito`, `fatura_cartao`, `fornecedor` ou `compra`. **0 de 85 rotas** tocam qualquer uma; **0 de 12 telas**; e as dez camadas da tela Pendências são **todas** da vertente do cliente. **O de-para com o que o dono descreveu em 30/07:** contas a pagar → `conta_pagar` (F4); contas a receber → `conta_receber` (F4) — **a do cliente é a `fatura`, que existe**, esta é a das *outras* receitas; pagamento de fornecedor (dono de usina) e de comissão → `conta_pagar` + `pagamento`, que é a `Q-PAGAMENTO-01`; compras e cartão de crédito → `compra`, `fornecedor`, `cartao_credito`, `fatura_cartao` (F5); folha → *folha importada* (F5). **O que NÃO recomendo, com três razões independentes:** (1) a regra de fase do `PRD` §10 — *"nenhuma fase avança sem os critérios da anterior verificados"* — e o critério da F2 é *"boleto liquidado no sandbox baixa a fatura"*, que depende do A1 (`Q-SICOOB-01`); (2) construir AP/AR com 0 faturas, 0 liquidações e 0 splits é desenhar contra zero dado real, e as decisões de `conta_pagar` (agrupamento, vencimento, categoria) se tomam melhor olhando o primeiro lote de verdade; (3) **a F4 inteira é grande e a parte que dói é pequena** — registrar que um repasse foi pago é uma fração do módulo, e é a única com prazo. **Decidir:** (a) construir só a fração da `Q-PAGAMENTO-01` agora e o resto da F4 depois da primeira competência faturada; (b) F4 inteira em bloco, depois da F2 fechar; (c) declarar que o controle da empresa fica fora do sistema por ora — resposta legítima que precisa estar **escrita**. Ver `REVISAO-VERTENTES-2026-07-30.md` §2 e §6 | Vinicius |
| AUD-02 | 🟡 | Rateio incompleto (91,20% e 99,78%) é intencional? | operação |
| AUD-10 | 🟡 | Regra dos 25%: de onde vem, sobre o que incide, bloqueia ou alerta? | Vinicius |
| **Q-LOTTIE-01** | 🟢 | **As micro-interações vão ser Lottie ou continuam em CSS?** O pedido de acabamento de 30/07 nomeou Lottie sete vezes e o que foi entregue são as micro-interações **em CSS e SVG** — pulse do alerta, brilho e ondulação do botão, check/X desenhando-se, engrenagem com o sol da G3 girando, interruptor animado. A divergência fica registrada como divergência. **Três razões, nesta ordem:** (1) **não há asset** — Lottie é formato de entrega de animação feita em After Effects, e sem `.json` de designer significaria escrever bodymovin à mão, que **não renderiza nada e não levanta erro** quando está malformado: seis animações que ninguém consegue afirmar que funcionam; (2) `lottie-web` são ~60 KB gzip de runtime para desenhar um check que o Phosphor já desenha e o CSS já anima em três linhas — num sistema cujo servidor é `node:http` puro, isso pede justificativa que eu não tenho; (3) `prefers-reduced-motion` (WCAG 2.3.3) é uma linha em CSS, com teste provando que desliga. **O que a decisão custa se for "sim":** nada de arquitetura. Os pontos de movimento estão todos nomeados em `ICONES_QUE_SE_MOVEM` (`web/src/iconografia.ts`) e nas seis `@keyframes` de `web/src/estilo.ts`, e a troca é local. Detalhe em `INTERFACE-2026-07-30.md` §5 | Vinicius |
| O-01 | 🟢 | Parâmetros da lista de rateio | operação |
| F-05 | 🟢 | Vendas - Integração entra na mesma carteira? | Vinicius |

## 8. Fora do financeiro — dev do CRM

| ID | Nível | Pergunta |
|---|:--:|---|
| **MT-08** | 🟡 | Parametrizar as views `financeiro.*` — hoje o UUID da G3 é literal em 14 pontos. Bloqueia no segundo tenant |
| AUD-07 | 🟡 | Merge de leads duplicados apaga fisicamente um `id`? Afeta a reconciliação por diferença de conjunto |
| Q-SPEC001-07 | 🟢 | O CRM vai quebrar `vendedor_tipo` em cinco valores? **Deixou de bloquear** — a `SPEC-001` R20 chaveia a comissão por `originador.tipo`, que é local. Melhora a semente, não desbloqueia nada |
| — | 🟡 | Aplicar o `VIEWS-PROPOSTAS-r2.sql`, com a correção do `LIMIT 1` sem `ORDER BY` na linha 92 |
| — | 🟡 | 49 tabelas de backup em `public`, 21 com `tenant_id`, nenhuma com policy. Retenção e destino (`P8` §3) |
| — | 🔴 | Segredos em `text` puro na tabela `tenants` (`P8` §4) |

---

| Q-PGNET-01 | 🟡 | **A extensão `pg_net` concede `arwdDxtm` a `PUBLIC` no CRM, e isso dá escrita a toda role do banco — inclusive `financeiro_ro` e `anon`.** Medido em 27/07 ao endurecer a guarda de arranque do conector: `net.http_request_queue` e `net._http_response` têm ACL `=arwdDxtm/supabase_admin`, ou seja, o grant vazio (PUBLIC) com todos os privilégios. Também `extensions.pg_stat_statements` e `..._info` com `SELECT` a PUBLIC. **Nada disso é concessão do dev** — é default da extensão. **Consequência:** quem alcança qualquer credencial daquele banco pode enfileirar requisição HTTP saindo do Postgres do CRM. Não é dado de negócio e não viola a regra 4 no sentido estrito (o financeiro não modifica o CRM), mas também não é nada. **Do nosso lado já está tratado:** o pool do conector abre a sessão com `default_transaction_read_only=on` (teste N25 prova que o INSERT falha), e `conferirRoleDeLeitura()` devolve esses privilégios em `privilegiosDeInfraestrutura` em vez de silenciá-los. **Do lado do CRM, é decisão do dev:** `REVOKE ALL ON ALL TABLES IN SCHEMA net FROM PUBLIC` é o endurecimento usual, e precisa ser conferido contra quem hoje usa `pg_net` lá. **Corrige uma afirmação minha:** o `PROMPT-dev-crm-rodada3` dizia que `financeiro_ro` tinha "0 privilégio de escrita" — a medição que produziu esse zero filtrava `information_schema.table_privileges` por `grantee = current_user`, que **não enxerga grant a PUBLIC nem privilégio herdado por participação em role** | dev do CRM |

## 9. Resolvidas

| ID | Era | Resolução |
|---|---|---|
| **Q-ORIGINADOR-01** | 🔴 F2 | **Resolvida em 29/07 na opção (a): as UCs da carteira LEVAM originador, e a comissão está inteira pela frente.** A resposta do dono foi *"sim, todas"* — e a segunda metade dela é o que destrava: **ninguém recebeu comissão ainda**. Isso responde a pergunta que a questão original não tinha feito, e que só apareceu ao medir: como `NovoContrato` não aceita `faturas_cheias_pagas` e a coluna nasce em 0, um contrato digitado hoje paga a 1ª e a 2ª parcela nas duas primeiras faturas cheias que **este sistema** emitir. Se a comissão já tivesse sido paga por fora, preencher o originador **pagaria de novo** — 25% a 30% do consumo, em silêncio. Não é o caso, então **o contador em 0 é o valor correto**, não uma armadilha. **A premissa foi conferida contra o CRM antes de gravar, não fechou, e o dono a retirou.** A resposta veio acompanhada de *"nenhuma venda foi efetivada ainda, verificável em clientes ativos, assinatura"*. Medido no mesmo dia (29/07 17:31Z): o funil `Clientes ativos - Assinatura`/ATIVOS tem **29 cards**, com 28 em `Rateio`/Desconto Ativo e 44 em `Vendas - Assinatura`/Negócios Ganhos. Devolvido ao dono, que corrigiu: **"erro meu, realmente existem os clientes ativos"**. A premissa caiu inteira. **O que sobrou de pé é a outra metade da resposta, e é ela que sustenta a decisão:** ninguém recebeu comissão ainda. **Isso é testemunho, não medição** — nem o financeiro nem o CRM registram comissão paga por fora, e não existe consulta que confirme ou desminta. O que se mede é só o contorno: `fatura`, `boleto` e `liquidacao` em **0**, ou seja este sistema nunca cobrou ninguém. Cliente ativo diz que ele recebe crédito, não que alguém foi comissionado pela venda dele — as duas coisas são independentes, e é por isso que os 29 não contradizem o testemunho. **Registro de um erro meu, porque ele quase entrou no repositório como fato:** a primeira versão desta entrada conciliava as duas afirmações inventando que *"efetivada" significaria "faturada por nós"*. Não significava. Ninguém disse isso — foi dedução minha para fazer as duas frases caberem juntas, e ela teria ficado escrita como se fosse esclarecimento do dono. **A decisão não muda; a base dela sim: era medição-mais-testemunho, é só testemunho.** Se o testemunho mudar, a regra muda junto — preencher o originador de uma venda já comissionada paga a mesma comissão **duas vezes**. **Implementado:** o campo é obrigatório na tela (`web/src/contrato-regras.ts`, puro e com suíte própria — 9 verificações), e `originador_id` **segue nullable no banco** de propósito: contrato sem comissão é estado legítimo do domínio, e torná-lo `NOT NULL` decidiria por todo tenant e todo contrato futuro uma pergunta que foi respondida sobre 39 contratos desta carteira. No lugar da constraint entrou detecção — a camada `originador_do_contrato` da prontidão, abaixo. **O caminho de cadastro** é `scripts/cadastrar-originadores.ts` (`npm run originadores`), que confere o lote inteiro antes de escrever. **Fica pendente o insumo, não a decisão:** a lista de originadores e quem originou cada UC. <br>**29/07, noite — o eixo foi decidido e o insumo encolheu:** *"o originador vai ser o `vendedor_origem` até segunda ordem"* (dono). A lista fechou em **três nomes** medidos no CRM, e a atribuição por UC ficou derivável para **40 das 41** — mas **12 delas o financeiro não consegue ler**, porque estão em cards de etapa `normal` e a view expõe só `won`. O que sobrou do insumo é humano: três documentos, natureza, confirmação de tipo e uma UC arquivada. Ver **`Q-ORIGVEND-01`** (§5) e o mapa em `ATRIBUICAO-originador-2026-07-29.md` |
| **Q-PRONTIDAO-COMIS-01** | 🟡 F3 | **Resolvida em 29/07, na direção que a `Q-ORIGINADOR-01` determinou.** A questão dizia: *"se a carteira legada não tem comissão, o `nao_medido` está certo e nada muda; se tem, falta uma contagem que acuse contrato ativo sem originador."* Tem — logo, foi a contagem que faltava. **Implementada** como décima camada, `originador_do_contrato`, que conta contrato ativo com `originador_id` nulo sobre o universo dos contratos ativos, com `efeito: bloqueia_split`. Os dois estados que antes eram o mesmo `?` agora são distinguíveis: *"nenhum contrato"* continua `nao_medido` (universo vazio, e a lição do K18a vale para a camada nova) e *"contrato sem originador"* é `pendente`. A `regra_de_comissao` **não mudou de cálculo** — mudou de explicação: ela aponta a camada de cima quando o vazio dela é consequência daquele. **Provada nos dois sentidos** (`K18f`–`K18i`), e a contagem é conferida **contra a tabela**, não contra número fixo: a primeira versão do teste dizia "2 de 3" lendo a fixture e falhou porque os testes anteriores criam contrato — número fixo teria quebrado depois, com a camada parecendo culpada |
| **Q-021 / AUD-03** | Q5 aud. | **Resolvida em 28/07 pela resposta 9a da `PAUTA-contador.md`: a base é a SÉRIE REAL por competência, não a geração nominal.** A questão estava aberta desde a F0 e o `PRD` §5.1 a marcava com ⛔ *"nenhuma spec de faturamento avança sem essa decisão"* — foi ela que impediu a F2 de começar antes. A escolha do contador foi **B, a geração efetivamente medida**, e não a alocada em contrato nem o menor dos dois. **Implementada na `SPEC-003` R30 e na migration 16:** `fatura.geracao_kwh_competencia` é `NOT NULL`, então sem linha em `usina_geracao` a fatura **não nasce**. **Consequência medida, e ela vai além do que a pergunta pedia:** o "quanto a usina já foi usada" da `RATEIO-USO-01` deixa de precisar de invariante própria, porque `Σ faturado = geracao × Σ percentuais / 100` e a R11 já limita `Σ percentuais` a 100. O overbooking de crédito some por construção. **E o caso concreto se resolve por recusa, não por silêncio:** a usina `0003`, com 100% alocado e zero geração lançada, não gera fatura nenhuma — faturar ali seria emitir receita sobre energia que ninguém registrou ter sido gerada. Divergência assumida contra o painel do CRM, que rateia pela nominal |
| **Q-011 / PAUTA 2, 3a e 4b** | Q-011 | **Resolvidas em 28/07: NÃO HÁ RETENÇÃO sobre comissão a PF, comissão a PJ nem repasse ao dono da usina.** Era a pergunta mais estrutural da pauta, e o motivo está registrado nela: com retenção, a regra *"a soma dos itens de repartição é igual ao valor liquidado, ao centavo"* fica **falsa** — parte vai para o fisco —, e passaria a ser *"bruto = liquidado"* e *"líquido + retenções = bruto"*, com quebra por tributo em cada item. **Sem retenção, `split_item` guarda UM valor por item e a invariante inegociável do `PRD` §5.5 sobrevive intacta.** É a diferença entre a tabela que existe hoje e uma com três colunas a mais. A 4b ficou em branco na devolução e foi respondida pelo dono em 28/07, alinhada às outras duas. **Se qualquer uma virar "incide", a migration é de schema em tabela com dinheiro já gravado** — que é exatamente o custo que perguntar antes evitou |
| **PAUTA 1** | — | **Resolvida em 28/07: COMPETÊNCIA governa a receita, CAIXA governa o split.** A devolução veio com **as duas** alternativas marcadas — competência e caixa —, e as duas não coexistem sem dizer o que cada uma governa. O dono decidiu pelo desenho que torna as duas marcas coerentes, porque são eixos diferentes: a receita nasce na **emissão** da fatura, no mês de referência do crédito (`fatura.emitida_em`), e o dinheiro só se reparte na **liquidação**, como o `PRD` §5.2 já mandava — *"exclusivamente na liquidação, nunca na emissão"*. Nada do modelo de eventos mudou; o que mudou é que agora está escrito qual eixo é qual. Descartadas caixa puro (a emissão deixaria de ser fato gerador e o DRE passaria a ser alimentado pela baixa) e competência pura (contrariaria o `PRD` §5.2 e exigiria alterá-lo) |
| **PAUTA 5** | — | **Resolvida em 28/07: a cobrança sem nota fiscal é sustentável, e o BOLETO é o instrumento.** É a resposta que mais encolheu escopo: **não há integração com prefeitura nem com SEFAZ**, que a própria pauta identificava como "a maior variável de escopo da fase — semanas de diferença". A migration 16 não tem uma coluna reservada para documento fiscal, e a `SPEC-003` §2 registra a ausência como decisão, não como pendência. O dono acrescentou por escrito que **o sistema deve integrar com a Sicoob para geração de boletos**, que já era o `PRD` §6 — implementado como porta injetada (`Q-SICOOB-01` para o certificado) |
| **PAUTA 6b, 6c e 7** | — | **Resolvidas em 28/07.** **6b — sem crédito de IBS/CBS a apropriar:** a `fatura` não carrega coluna de tributo recuperável e não nasce conta a receber de crédito fiscal. **6c — Simples:** nenhuma tabela de alíquota por regime é versionada. **7 — a comissão da sócia é COMISSÃO**, despesa dedutível como a dos demais: os 39 ganhos dela (83% de concentração medida) entram no motor de comissão como qualquer outro, e não saem para distribuição de lucro. Se tivesse sido B, esses 39 sairiam do motor para outra entidade, outro momento e outra base tributável — com 83% de concentração, a escolha move o resultado da empresa. **A 6a ficou sem resposta** (*"não compreendido"*) e virou `Q-PAUTA-6A-01`, na §5: com Simples, sem crédito de IBS/CBS e sem nota, o efeito dela sobre o schema é nulo hoje |
| **PAUTA 3b** | — | **Resolvida em 28/07: paga-se direto, a nota do parceiro PJ vem depois.** A pergunta voltou com *"o que seria exatamente?"* — não foi respondida, foi questionada —, e foi reformulada e decidida no mesmo dia. **O que a resposta B teria custado:** o item de comissão nasceria no estado *"bloqueado aguardando nota"*, com máquina de estados a mais no pagamento, campo de nota recebida e tela para destravar. Com A, `split_item` **não tem máquina de estados nenhuma** |
| **PAUTA 8a** | — | **Resolvida em 28/07: arredonda-se NO TOTAL, distribuindo o resíduo.** E a resposta coincide com o `PRD` §5.5 — *"diferenças de arredondamento vão sempre para o líquido G3"* — sem contradizê-lo, porque o líquido G3 é o item **residual**: repasse e comissão arredondam, a concessionária é exata, e o líquido é a **subtração**. A soma dos quatro é o liquidado ao centavo por construção aritmética, e o resíduo dos dois arredondamentos está dentro dele. Não há um quinto lugar onde o centavo possa se perder, e não há caso em que a invariante dependa da ordem de cálculo. Verificado em **2.000 combinações** de consumo, tarifa, juro e percentual (`S5`), das quais 22 produziram líquido G3 negativo — que o `PRD` §5.6 prevê e o `CHECK` permite só para este tipo |
| **Q-FASE-01** | — | **Resolvida em 27/07 por decisão do dono: o `PRD` §10 vence — o conector é entrega da F1.** O critério foi a hierarquia normativa do `CLAUDE.md` (`este arquivo → PRD-v2.2 → ADRs → SPECs`): a `SPEC-002` está *abaixo* do PRD, então o cabeçalho dela é que estava divergente, não o PRD. Descartada a opção de mover o conector para a F2, que fecharia a F1 no mesmo dia — e teria sido **alterar o PRD para acomodar o estado do código**, o precedente que esta mesma sessão recusou três vezes (`CAT-2`, `G2`, `N21`). Consequência: a F1 só fecha com o conector entregando o que a `SPEC-002` §2 declara, e é a `Q-ESCOPO-01` que mede a distância. Cabeçalho da `SPEC-002` corrigido na v1.3 |
| **Q-LOTE-01** | — | **Resolvida em 27/07: `executarCiclo()` passou a abrir as próprias transações, uma por lote.** A correção é uma **inversão**, não um ajuste: o motor deixou de ser passageiro de um `withTenant` do chamador e passou a receber um `AbrirLote` injetado, pela mesma razão que já recebia a `PortaDeLeitura` — ele não conhece `pg`, não conhece o `PrismaClient` e não emite contexto de tenant. Quem emite continua sendo `src/db/contexto.ts`, ponto único, **uma vez por lote**. A abertura (papel, conferências, linha de `conector_execucao`) é a primeira transação, os lotes de cadastro vêm depois, a reconciliação depois, e o fechamento é a última. **A leitura do CRM saiu de dentro da transação**: era rede para outro banco segurando conexão do nosso pool. **O tamanho declarado é 50, e o número saiu de medição.** O que limita o lote é viagem ao banco, não linha: com leitura e escrita em bloco (`findMany`/`createMany`), um lote de 50 custa **4** viagens quando nada mudou, **6** quando os 50 são criados e **54** no pior caso, em que os 50 mudam — porque cada um muda para um valor diferente e não há update em bloco. A 75 ms por viagem (a latência que a própria `Q-LOTE-01` registrou: 15.330 ms para ~205 viagens até `sa-east-1`) o pior lote custa **4,05 s**, com folga de 3,7× sobre os 15 s. Um lote de 200 daria ~15,3 s no mesmo pior caso — o penhasco. **As ~5 viagens por cliente viraram ~0:** a reconsulta desperdiçada em `escreverEstadoCrm()` desapareceu junto com a função, porque o `id` passou a ser gerado do nosso lado para que `createMany` sirva. **`test_lote_respeita_timeout` (§9) são os N26–N31, e o que eles medem não é relógio local** — localmente as 205 viagens que mataram a produção levam menos de um segundo, e cronômetro daria verde para o código morto. Medem **viagens por transação**, contadas por extensão do Prisma (que pega inclusive o que o autor esqueceu de contar), e projetam contra a latência medida. **Verificados nos dois sentidos, com três plantios:** com o lote em 1.000 o **N28** reproduz a `Q-LOTE-01` (1.004 viagens = 75.300 ms projetados > 15.000) e o **N29** falha; removida a gravação de contadores por lote, o **N31** acusa (`0 → 0 → 0`); removida a guarda de reentrância, o **N19c** acusa. Fica registrado o **limite honesto do N27**: sozinho ele passa mesmo com o lote em 1.000, porque criar em massa é barato — quem acusa é o N29, onde a carga é de atualização. **Efeito colateral que virou questão, não improviso:** a abertura agora commita `em_andamento` antes dos lotes, e morte por `kill` deixa o registro preso — `Q-CICLO-ORFAO-01`, na §4 |
| **Q-VIEWS-01** | — | **Resolvida em 27/07, no mesmo dia, pelo dev do CRM.** As 8 views `financeiro.*` passaram a expor `crm_tenant_id uuid`. Conferido contra o CRM: **1** valor distinto e **0** nulos em cada, e o **mesmo** valor nas oito (`d4640f4b-f833-…`). Coluna no **fim** de cada lista, respeitando a restrição do `CREATE OR REPLACE VIEW`. E — o que mais importa — **`security_invoker` não foi ligado**, o que teria quebrado a leitura inteira. A validação por linha da `SPEC-002` R1-b passou a rodar de verdade: `src/crm/leitura.ts` compara toda linha recebida e aborta o ciclo na primeira divergência. **Teste N23** (`test_tenant_divergente_aborta_ciclo` da §9) prova contra um schema `financeiro` local com uma linha plantada de outro tenant; **N24** prova o outro sentido, que o caminho legítimo segue passando. `garantia_de_tenant_degradada` deixou de ser o estado normal e virou o sinal de que alguém removeu a coluna | — |
| **Q-AUDIT-01** | — | **Resolvida em 27/07 por decisão do dono: `usuario_id` entra no `coalesce` de `app.auditar()`, por último.** Migration 13. A ordem é o que torna a correção segura — onde existe `id`, `id` continua vencendo, então as outras quinze tabelas auditadas não mudam de comportamento. `plataforma_admin` era a única das dezesseis sem `id` e sem `cliente_id`, e gravava `registro_id` NULL: a escrita mais privilegiada do sistema era a única cuja trilha não se consultava pelo índice `auditoria_registro_idx`. Descartada a opção de dar `id` próprio à tabela, por mexer em PK, `db pull` e tipos gerados para um ganho que uma linha entrega. **Testes G6 e G7 em `tests/auditoria.sql`, verificados nos dois sentidos:** replantado o `coalesce` antigo, o G6 acusa (`achou 0`) e a suíte aborta; o G7 fixa que `usuario_tenant` — que tem as **duas** colunas — segue identificada pelo próprio `id`, e é ele que pega alguém reordenando o `coalesce` depois |
| **Q-DISTRIB-01** | — | **Resolvida em 27/07 por decisão do dono: policy explícita + `CAT-8`.** Migration 13 dá a `distribuidora` a policy `distribuidora_leitura_publica FOR SELECT USING (true)`, tornando declarada uma intenção que era suposta em comentário. Medido depois de aplicar: a role de runtime passou de **0 para 1** linha visível. O `CAT-8` é a parte que importa mais — ele acusa **qualquer** tabela de `public` com RLS e zero policies, sem o filtro por `tenant_id` que fazia o `CAT-3` olhar para o outro lado, com lista branca nominal para `_prisma_migrations`. Verificado nos dois sentidos em banco local: acusa tabela plantada com RLS sem policy, e respeita a lista branca. **E rodado contra produção**, onde os 8 invariantes passam — o `CAT-8` estava vermelho lá antes da migration, e nenhuma suíte local teria mostrado isso, porque o PG16 de teste não tem o event trigger da plataforma. Fica registrado que o `catalogo.sql` é leitura pura e **deve** ser rodado contra produção: `psql "$DIRECT_URL" -f tests/catalogo.sql`. Reclassifica o `MT-09`, que seguia aberto como risco *previsto* coberto pelo `CAT-3` — a cobertura não existia |
| **MT-06** | — | **Auth PRÓPRIO, decidido em 27/07 pelo dono do projeto.** Sem SSO com o CRM: de lá o financeiro só lê lead ativo, e leitura de dado não é motivo para acoplar identidade. Acoplar significaria que o ciclo de vida da conta no CRM — desativação, rotação de segredo, mudança de provedor — passaria a governar o acesso ao financeiro, que é o sistema de dinheiro. Implementado em `src/auth/jwt.ts` e `src/auth/autenticador.ts`: JWT do Supabase Auth **do projeto do financeiro**, verificado com `node:crypto`, sem dependência nova. `usuario.auth_user_id` é o `sub` desse emissor e de nenhum outro — e o teste J9 recusa token cujo `iss` seja o do CRM. Preferência declarada pelo **JWKS assimétrico** sobre o segredo HS256 legado: com o segredo simétrico o servidor também consegue assinar, e quem o lê forja token de qualquer usuário |
| **Q-RBAC-01** | — | **Alinhada ao PRD em 27/07: `escrever_cadastro` passa a ser `['admin']`.** A implementação dava a `financeiro` escrita em Cadastros, onde o `PRD-v2.2` §3 dá apenas leitura. Critério da decisão: a hierarquia normativa do `CLAUDE.md` é `PRD → ADRs → SPECs` e o código não está nessa lista — ele implementa —, então divergência entre código e PRD é defeito do código. A divergência era **permissiva**, que é a direção perigosa: restringir é reversível, descobrir excesso de permissão em produção não. Descartado um meio-termo que separaria "cadastro operacional" de "cadastro que define dinheiro", por inventar vocabulário ausente de todo documento — que foi exatamente como `escrever_cadastro` nasceu divergente. **Consequência operacional:** só `admin` cadastra cliente, UC, usina, originador e contrato. Se a operação mostrar que isso trava, o conserto é a §3 do PRD, nunca o `contexto.ts` |
| **F-03** | — | **Resolvida por decisão normativa, não por escolha nova.** A regra 4 do `CLAUDE.md` e o `PRD` §7.8 proíbem write-back: inadimplência é dado do **financeiro**, e o CRM consome endpoint exposto por ele se quiser exibir. Ficou vermelha por sessões sem que houvesse decisão a tomar — a hierarquia já a tinha respondido |
| **Q-SPEC001-04** | — | **`regra_repasse` versionada por usina e por vigência**, com `EXCLUDE` no banco; `usina.percentual_repasse` removida. Por usina e não por tenant porque cada dono negocia o seu; por vigência e não congelada no contrato porque o eixo de tempo do repasse é a **competência** — o contrato é com o consumidor, o repasse é com o dono da usina. Migration 10, `SPEC-001` R25 |
| **Autopromoção a plataforma** | — | **Furo medido em 26/07.** `plataforma_admin_self` era policy `FOR ALL` sem `WITH CHECK`, e o `USING` vale como check de escrita: papel `leitura` inseria a própria linha com tier `plataforma_admin` e passava a ler todos os tenants. Policy virou `FOR SELECT`, escrita revogada da role da aplicação. Invariante 16 pega a **classe** — qualquer policy `FOR ALL` sem `WITH CHECK` em tabela gravável |
| **Trilha da R2 burlável** | — | **Furo medido em 26/07.** O gatilho lia `app.current_tier()` no COMMIT e quem escreve controla o GUC: apagar `app.tier` antes de commitar dispensava a trilha. Reancorado no `tier` gravado em `auditoria` **na escrita**, com `xact_id` em vez de `xmin` — que quebraria em subtransação. Um gatilho em `auditoria` no lugar de treze |
| **Cobertura da trilha** | — | **Quatro tabelas de treze, e as quatro erradas.** Sem gatilho ficavam `dono_usina` e `originador`, que guardam chave PIX e conta bancária. Medido: PIX do dono de usina de outro tenant trocado sob tier, sem rastro. Agora dezesseis tabelas, e a invariante 17 confere por catálogo |
| **Regra 9 sem implementação** | — | **A regra exigia *antes e depois* e não havia tabela nenhuma.** `acesso_plataforma_log` é trilha de acesso, não auditoria, e era apagável pela própria role que a escrevia. `auditoria` com imagem de linha, append-only por privilégio (invariante 18) |
| **Preço ausente devolvia NULL** | — | `tarifa_vigente` e `percentual_comissao` devolviam NULL na ausência, e `consumo_centavos(x, NULL)` é NULL: base de faturamento que soma como nada e `coalesce` como zero. As três funções levantam `no_data_found`. `distribuidora` virou tabela de referência com FK — o risco que a própria seed havia nomeado e escolhido aceitar |
| AUD-01 | Q1 | `ADR-0002` r2 — mesma entidade em funis diferentes; disjunção estrutural, não backfill |
| AUD-12 | Q12 | `Q-023` — consumir `financeiro.*` direto; não criar o schema `integracao` |
| Dado monetário | — | `PRD` §7.5 — `leads.consumo_reais`, 100% preenchido. `valor_venda` morto por desenho |
| `auditoria_ro` | — | Role removida em 24/07 (`P8` §6). Único item com prazo, encerrado |
| **ADR-0003** | — | **`SET LOCAL` por transação.** Spike em 24/07 (21 testes), lacuna do `$transaction` fechada em 25/07 (12 testes) — r2. Contrato do middleware na `SPEC-001` §3.2 |
| **AUD-05** | Q5 aud. | Tabela do PRD, escalonada — decisão A1 de 24/07 |
| **AUD-05a** | — | **`PADRAO` é 50%, e sempre foi.** Os 303 leads em `PADRAO` já eram 50% |
| **AUD-06** | — | Senioridade é **local**, em `originador.tipo` (`SPEC-001` R20 e R15). Não depende do CRM |
| **F-01** | — | **Morto pela decisão C1-b.** As 28/36 pessoas em rateio estão homologadas com assinatura não iniciada: não há carteira legada a migrar. Sucessora: F-01b na §5 |
| **Q-SPEC001-06** | — | Organização Supabase **separada** — decisão A2, `ADR-0004` |
| **PRD §2.3** | — | Provisionamento decidido — `ADR-0004`: organização separada, `financeiro.blackhaus.io`, mesmo VPS sob cinco condições |
| Contagem de FKs | — | Sete era estimativa; a varredura nominal rende **nove**. Lista fechada na `SPEC-001` §3.4 |
| **AUD-07** | — | **Merge nao apaga** (marca `removido_do_funil_em` + tag). Mas ha dois caminhos de DELETE fisico fora do merge, um **rotineiro** (sync "Clientes Ativos"). `SPEC-002` §4.3 classifica ausencia em tres |
| **MERGE-01** | — | **Resolvida pelo dev em 26/07.** `public.lead_merges` criada, **sem FK para `leads` de proposito** (a trilha sobrevive a DELETE fisico), com backfill e o codigo gravando antes de arquivar. O par de 10/07 foi recuperado do log pm2, e **os dois lados estao arquivados** — nao ha cliente ativo pendurado. `SPEC-002` R18 funde o espelho em vez de so desativar |
| **ATIVO-01** | — | **Resolvida por fato, nao por opiniao.** O funil `Clientes ativos - Assinatura` esta **vazio**, e a etapa-fonte (`Desconto Ativo`, won do Rateio) tambem: os 29 concluidos param em `Rateio Concluido`, `stage_type='normal'`, que **nao dispara** a automacao. C1 leria vazio nao por volatilidade, mas porque a operacao nao estaciona ninguem na fonte. **Fonte troca para `financeiro.rateio_clientes`** (36 vinculos), que e o estado real |
| **COMISSAO-02** | — | **Nao existe segunda engine.** O CRM **nao calcula** comissao: `app_settings.g3_partner_rules` **carimba tier** no lead na criacao, via RPC. A verdade por lead e o campo `Comissionamento`, e quem transforma em R$ e so o financeiro. Uma verdade por lead. **Mas a investigacao expos um furo real na R20** — ver linha seguinte |
| **R20 lia o tier corrente** | — | **Furo achado em 26/07 pela resposta do dev.** A R20 chaveava a comissao por `originador.tipo`, que e a classificacao de **hoje**: captador promovido a senior faria **todo contrato antigo recalcular a 60%**. Vigencia nao cobre — ela versiona o percentual de um tier, nao o tier de uma pessoa. Corrigido: `contrato.originador_tipo_no_fechamento` congela no fechamento (`SPEC-001` R20-b, migration 5, teste) |
| Atribuicao de originador | — | `leads.partner_id`, **nunca** a tag `indicado_por` — ela e display e editavel, e ha 1 lead com tag sem `partner_id` (`SPEC-002` R16) |
| Schema `integracao` | — | **Nao criar.** Os aliases `vw_*` do PRD sao redundantes: consumir `financeiro.*` direto dispensa, e era o que a AUD-12 ja tinha decidido. O `PRD-v2.2` precisa perder a mencao |
| Senioridade no CRM | — | Aplicada em 25/07 (`+captacao_senior` 60%, `+vendedor_tipos`). **Nao valeu para nada ainda:** 0 leads com a opcao de 60%, e `vendedor_tipos` so carimba lead novo — nada retroativo |
| **F-02** | — | Funil `Parceiros` fica **fora** da base de comissao: `won` ali e "parceiro ativado", nao venda. 48 ganhos = 40 + 1 + 7 (`SPEC-002` R14) |
| Tabelas de backup | — | 50, movidas para schema `backup` pelo dev em 26/07: fora do PostgREST, fora do `search_path`, sem grants. Revisao em 26/10/2026 |
| "RLS sem policy nega tudo" | — | **Premissa corrigida pelo dev em 26/07.** Vale para acesso direto; **falso atraves de view** - a RLS das bases e avaliada contra o dono da view. Virou a invariante 13 da `SPEC-001`, com o furo reproduzido em teste |
| Tarifa | — | `1,13` é **tarifa em R$/kWh**, não fator de consumo. `numeric(12,6)`, não centavos (`SPEC-001` R22) |
| PgBouncer | — | **Conexão direta (5432), não pooler em modo *transaction*.** Deduzido do `ADR-0004`: processo Node de vida longa não precisa de pooler externo. Reverter a decisão reabre o `ADR-0003` (`SPEC-001` §3.2) |
| `CLAUDE.md` | — | Nunca existiu. `CLAUDE.md` v1.0 escrito em 24/07; 18 citações reapontadas |
| `ADR-0001` | — | **Nunca chegou ao repositório**, e a regra 2 do `CLAUDE.md` derivava dele. Escrito retroativamente em 25/07: não reconstrói o original perdido, registra a decisão que o corpus pressupõe, com proveniência por parte. Última citação órfã do corpus |
| Vazamento em `usuario_tenant` | — | A policy da migration 2 deixava qualquer sessão enumerar **a equipe de qualquer tenant** só apontando o contexto. Corrigido na migration 3: só vê a composição quem pertence |
| "bloqueio vermelho" | — | Taxonomia definida na §1 deste arquivo |
