{
  "name": "financeiro",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.18" },
  "scripts": {
    "db:migrate": "prisma migrate deploy",
    "db:pull": "prisma db pull",
    "test:isolamento": "bash tests/run.sh",
    "test:middleware": "node --experimental-strip-types tests/middleware.ts",
    "test": "npm run test:isolamento && npm run test:middleware"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.9.0",
    "@prisma/client": "^7.9.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "prisma": "^7.9.0"
  }
}
