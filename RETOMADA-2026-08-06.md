# RETOMADA — 06/08/2026

| Campo | Valor |
|---|---|
| **Para quem** | Quem abrir a próxima sessão. **Dois minutos**, e diz onde tudo parou |
| **Substitui** | `RETOMADA-2026-08-03.md` |
| **O assunto principal** | **O cadastro no Portal Developers do Sicoob** — é a próxima ação, é do dono, e destrava o maior código que falta no projeto |
| **Suíte** | `EXIT=0`, **1618** verificações · catálogo **9/9**, também **contra produção** |
| **Repositório** | `32e8b92` publicado em `origin/main`. **Há trabalho de 06/08 por publicar, e ele TOCA A SPA** — ver §5 |

---

## 0. O deploy — ✅ CONCLUÍDO às 20:45 de 06/08

> **Esta seção dizia "o deploy está pela metade" e ficou vencida dez minutos depois de escrita.** O corpo datado fica abaixo, intacto, porque ele registra um estado que existiu de verdade por uma hora — e porque a janela que ele descreve está no journal do servidor. O que mudou é só o topo.
>
> **Conferido contra produção depois do restart:** **25 migrations = 25** no repositório · guarda de arranque dizendo *"client gerado cobre as **37** tabelas de public"* · catálogo **9 invariantes, nenhuma falha** · bundle `index-ifeZbjC7.js` · dados intactos — **86 clientes, 41 UCs, 0 contratos, 0 faturas**.
>
> **A chave migrou sem digitação**, como a migration prometia: `chave_pix` com apelido `G3 GESTAO ENERGIA SOLAR`, tipo `cnpj`, chave `66714022000121`, e a identidade apontando para ela como padrão.
>
> **A janela existiu e está registrada:** o journal traz `401 em GET /cobranca/identidade` às 19:47 — a SPA nova batendo na API velha, entre o `web:build` e o restart. Quem for repetir o procedimento: **aplique o schema antes de trocar o bundle**, e não depois.
>
> **O que ainda NÃO foi provado, e nenhum comando prova:** a aba Documento aberta, com a chave listada pelo apelido e o seletor *"Chave usada ao compor o lote"* preenchido. `401` prova que a rota existe e recusa credencial — não que ela funciona. Foi assim que a aba Documento passou quebrada por um deploy inteiro em 30/07.
>
> ### ✅ Provado mais tarde em 06/08 — e vale dizer exatamente o quanto
>
> **A tela monta e o seletor sai preenchido.** O bundle **servido por produção** (`index-ifeZbjC7.js`, `sha256` conferido contra o de `web/dist`: idênticos) foi renderizado em Chromium com a linha real de `chave_pix` de produção, e a aba Documento saiu com **`G3 GESTAO ENERGIA SOLAR — 66714022000121`** no seletor, sem erro em painel nenhum. Os 15 `campo_do_documento` de produção aparecem na ordem gravada; `layout_do_documento` tem 0 linhas, que é o caso normal (o padrão vive em código).
>
> **O que isso NÃO cobre, e a distinção é a mesma que o `401` ensinou:** a camada HTTP do processo no ar não foi exercida com credencial válida — `GET /api/cobranca/chaves-pix` responde **401** e `/api/rota-que-nao-existe` responde **404**, o que prova roteamento e não funcionamento. **O login do dono continua sendo o único teste que fecha os dois lados.** O que mudou é que o modo de falha de 30/07 — a tela quebrar ao montar — passou a estar medido, e não suposto.

### O corpo original, de quando a seção foi escrita

**Não é aviso de rotina.** Em 06/08, no fim da sessão, o VPS recebeu **código e bundle novos** e o banco **não recebeu a migration 25**. O passo foi bloqueado pelo classificador de permissão do harness, não por erro.

| | Estado em 06/08, medido |
|---|---|
| `origin/main` | `17a29cd` |
| VPS — código, client Prisma e `web/dist` | **atualizados** (`index-ifeZbjC7.js`) |
| VPS — serviço | `active`, **rodando o processo ANTIGO** (não reiniciado) |
| Banco de produção | **24 migrations**, `chave_pix` **não existe** |

**O sintoma que isso produz:** a aba Documento chama `GET /cobranca/chaves-pix`, que o servidor no ar ainda não tem. As outras telas seguem.

**O conserto, em ordem — e o `generate` e o `web:build` já foram feitos:**

```bash
npx prisma migrate deploy        # do Codespace, pelo DIRECT_URL
ssh root@financeiro.blackhaus.io 'systemctl restart financeiro'
psql "$DIRECT_URL" -f tests/catalogo.sql     # tem de dizer 9/9
```

**O que a migration 25 faz com o dado que já existe:** move a chave Pix das quatro colunas de `identidade_de_cobranca` para a tabela `chave_pix`, com o apelido `G3 GESTAO ENERGIA SOLAR`. **Não há digitação e não há perda** — a linha é migrada pela própria migration.

---

## 1. O PORTAL SICOOB — a próxima ação, e ela é do dono

### Por que ela vem primeiro

`src/sicoob/http.ts` é **o maior código que falta no projeto**, e ele está não-escrito **por decisão registrada**, não por esquecimento: escrever um adaptador que nada pode chamar é a `Q-PECA-NAO-PLUGADA-01`, o padrão que este repositório registrou depois de achar três peças prontas e não plugadas numa tarde.

**O que muda em 06/08:** a credencial que destrava esse código **não é o certificado A1** e **não passa pela cooperativa**. Medido na documentação: *"Depois de cadastrado no Portal Developers acesse o menu Dashboard você terá disponibilizado às credenciais de acesso ao ambiente Sandbox."*

Ou seja — **o item que estava marcado como bloqueio externo há uma semana é um cadastro de portal.** É a coisa mais barata da fila inteira e ninguém tinha visto.

### O que fazer

1. Cadastrar-se em **`developers.sicoob.com.br`**
2. Abrir o **Dashboard** e pegar as credenciais de **sandbox**
3. Percorrer a tela de **cadastro de aplicativo** e a de **webhook** — mesmo sem concluir o cadastro de produção, é o que elas *oferecem* que responde perguntas de projeto abertas

### O que colher numa passada só

Voltar ao portal custa mais que anotar tudo de uma vez. **Nove itens**, e cada um existe por uma razão:

**Do Dashboard — é o que destrava o código:**

| # | O quê | Por que importa |
|:--:|---|---|
| 1 | `client_id` de sandbox, **e se existe `client_secret`** | Em Keycloak sobre mTLS o certificado pode *ser* a credencial. Isso muda o que a resolvedora do `ADR-0005` precisa devolver |
| 2 | As **URLs base** de sandbox e o **endpoint de token** | O `SICOOB-contrato-medido` traz as que foram lidas; confirmar contra o painel é diferente de ler documentação |
| 3 | **Coleção Postman / spec OpenAPI** para download | Vira fixture do adaptador |

**Do cadastro de aplicativo:**

| # | O quê | Por que importa |
|:--:|---|---|
| 4 | Quais **APIs/escopos** a tela deixa marcar — em especial se **Cobrança Bancária v3** e **Pix** são aplicativos separados | Ver a §1.1: são duas APIs diferentes e a escolha muda a arquitetura |
| 5 | Confirmar o certificado: **`.PFX` com senha + `.CER` Base-64** | É o que a documentação de terceiro diz. Se divergir, o `ADR-0005` precisa saber |
| 6 | Se exige **dados da conta bancária** já no cadastro | Decide se dá para adiantar antes de a conta PJ existir |

**Da tela de configuração de webhook — e esta responde uma decisão já tomada:**

| # | O quê | Por que importa |
|:--:|---|---|
| 7 | **O que o campo aceita além da URL**: cabeçalho customizado? segredo? só a URL? | É a **evidência empírica** da Decisão 1 do `ADR-0006` |
| 8 | Menção a **mTLS, certificado de cliente ou faixa de IP** na entrada | Idem |

**Do contrato de cobrança — pode não estar no portal:**

| # | O quê | Por que importa |
|:--:|---|---|
| 9 | `numeroCliente`, `numeroContratoCobranca`, `codigoModalidade` | São a **identidade do cooperado** e **não se derivam** de nada que o sistema tenha. Se não estiverem no portal, é pergunta para a cooperativa |

### 1.1 São DUAS APIs, e a distinção não é detalhe

Descoberto em 06/08, e ele reordena o §2 do `PENDENCIAS`:

| | O que é | O que resolve |
|---|---|---|
| **Cobrança Bancária v3** | boleto — é o que o `SICOOB-contrato-medido-2026-08-05.md` mediu | boleto registrado, linha digitável, baixa |
| **API Pix** (padrão BACEN) | `cob`, `cobv`, `loc`, `webhook` | **`txid` por cobrança e webhook de recebimento** |

**Por que a segunda importa:** o projeto vinha aceitando que *"Pix estático não concilia, por isso boleto é preferido"*. Isso vale para o **estático**. Com `cob` a cobrança tem `txid` e **concilia sozinha** — e não depende do contrato de cobrança bancária.

**Não é decisão tomada.** É opção que ninguém tinha visto e que precisa ser pesada quando as credenciais existirem. Se a `cob` servir, ela encurta a cadeia do §2 inteira.

### 1.2 O que o portal NÃO resolve

**A conta PJ numa cooperativa singular.** Ela é o que destrava **produção**, e corre em paralelo — não bloqueia o sandbox. Para PJ, a jornada de adesão da API Pix passa pela cooperativa, que abre chamado no **Top Desk**; a credencial volta por e-mail. Só que **isso é a versão de 2021 do manual**, e o portal pode ter substituído o fluxo — é o item 6 do checklist.

### 1.3 O que já está decidido e não se reabre

O **`ADR-0006` foi aceito em 06/08**, com as quatro decisões:

| Decisão | Escolhida |
|---|---|
| 1 — o que autentica o webhook | **mTLS + faixa de IP** |
| 2 — como o tenant é resolvido | **pela credencial** |
| 3 — quem é o "quem" da trilha | **usuário de serviço por tenant**, sem login |
| 4 — escape do Bearer | **a rota declara o modo** |

**A 1 tem um pré-requisito que o portal ajuda a fechar:** não está medido que a Sicoob **apresenta certificado de cliente de fato**. O manual do BACEN declara o canal como mTLS; a medição de terceiro fala de cabeçalhos e não de TLS. Isso é pré-requisito de **ligar** a rota, não de escrevê-la — e o plano B, se não houver certificado, está nomeado dentro da própria decisão.

**E uma armadilha para quem for implementar:** o Sicoob **acrescenta `/pix` ao final da URL cadastrada**. Registrar `…/api/pix` faz o POST chegar em `…/api/pix/pix`. É 404 garantido em quem não souber.

---

## 2. O que mudou nesta sessão

| | |
|---|---|
| **Identidade de cobrança** | ✅ **fechada** — item 1.5 do `PENDENCIAS`. Chave `66714022000121`, recebedor `G3 GESTAO ENERGIA SOLAR`, cidade `GOIANIA` |
| **A chave está no DICT** | ✅ **confirmado pelo dono no Banco Inter**, que devolveu o titular. Era a única coisa que o sistema não consegue conferir sozinho |
| **Guarda da chave Pix** | `src/dominio/chave-pix.ts`, 35 verificações. A tela aceitava máscara e ela ia inteira para o BR Code |
| **Banco de chaves** | migration **25** — `chave_pix` com apelido, a identidade aponta para a padrão, a fatura **congela** a que usou |
| **`ADR-0006`** | ✅ aceito |

**O nome do DICT e o do QR não são iguais** — o Inter mostrou *"consórcio G gestão solar"* e o campo 59 leva `G3 GESTAO ENERGIA SOLAR`. Na prática o app do pagador mostra o do DICT, então é cosmético. Alinhar custa um comando e a hora é agora, enquanto nenhuma fatura foi emitida.

> **⚠️ "Custa um comando" era falso quando isto foi escrito, e passou a ser verdade no fim do dia.** A rota `PUT /cobranca/chaves-pix/:id` existia desde a migration 25 e **não tinha um único chamador**: a aba Documento só *cadastra*, e nenhum script alcançava a rota. Reexecutar o cadastro também não resolveria — `chave_pix_chave_unica` recusa a mesma chave —, então a única saída teria sido `UPDATE` à mão, por fora do contexto de tenant e da conferência de chave. É a quarta ocorrência da `Q-PECA-NAO-PLUGADA-01`, e a primeira achada **procurando o chamador** de uma peça citada como pronta.
>
> **O comando agora existe**, com ensaio e diff campo a campo:
>
> ```bash
> npm run identidade -- --editar "G3 GESTAO ENERGIA SOLAR" --ensaio --auth-user <uuid> \
>      --tipo cnpj --chave 66714022000121 \
>      --nome "CONSORCIO G GESTAO SOLAR" --cidade GOIANIA
> ```
>
> Ele imprime o antes/depois de cada campo e **o BR Code regerado**, e o `--ensaio` faz rollback. Trocar para `--valendo` grava. **A decisão de alinhar ou não continua sendo do dono** — o que mudou é que ela virou executável. Três coisas que o `--editar` preserva de propósito, e cada uma por um modo de falha: `titular_*` e `observacao` (o repositório reescreve a linha inteira, e não devolvê-los **apaga os três** — `W1l`), o `ativa`, e **quem é a chave padrão** (editar não escolhe destino).

---

## 3. A fila, sem o que já saiu

**Nenhum destes é código.**

| # | Pendência | Dono |
|:--:|---|---|
| 1.1 | CPF/CNPJ de **24 pessoas** — `npm run documentos` | operação |
| 1.2 | Dia de vencimento de **29 UCs** — `npm run vencimentos` | operação |
| 1.3 | CPF/CNPJ de **2 originadores** — `npm run originadores` | operação |
| 1.4 | Digitar os **29 contratos** — depende de 1.1 e 1.3 | operação |
| — | **`Q-FATCHEIA-01`** — decidir **antes** de importar contratos | **dono** |
| — | **`Q-CLIENTEDUP-01`** — 5 das 29 UCs são duplicatas | **dono** |
| 1.6 | Escolher a competência — **2026-06 sai com 28 de 29** | **dono** |
| 2.5 | Endereço do pagador de 29 UCs — `npm run enderecos` | operação (só boleto) |

**A `Q-FATCHEIA-01` tem prazo e os outros não.** `data_fechamento` é editável **no CSV, antes de importar**; depois a R20-b congela e não há edição de contrato. Pelas datas que o modelo sugere, 2026-06 sairia com **0 de 29 faturas cheias** — 28 faturas cobráveis e comissão nenhuma.

### 3.1 E um passo da fila que não estava em fila nenhuma

Entre **compor o lote** e **emitir** existe um terceiro ato: **`npm run tarifas`**. Nem a `PENDENCIAS-2026-08-05`, nem o *"caminho mais curto"* da `RESUMO-SESSAO-21` §7 o tinham — os dois vão de compor direto para a competência.

`valor_total_centavos` é coluna **gerada** e a tarifa da concessionária tem default 0: emitir sem lançar produz faturas **válidas, cobráveis e menores**, sem erro, sem log e sem recusa. E `lancarTarifasPorUC` **só aceita rascunho** — depois de emitida, corrigir é **cancelar e recompor**, com motivo na trilha.

**Desde 06/08 isso é contado, e só contado.** A tela de Faturas mostra *"N de M rascunho(s) sem tarifa da concessionária"* com as UCs nomeadas, e o `npm run faturar -- --valendo` imprime o mesmo ao fim do lote. **Nada trava**: zero pode ser o valor certo da competência, e essa é a pergunta (a) da `Q-TARIFA-CONC-01`, que tem dono e não é a tela.

---

## 4. O que ainda NÃO está publicado

**Toca a SPA**, então desta vez o deploy não é opcional para quem opera.

| | |
|---|---|
| **Migration** | **nenhuma.** Produção tem 25, o repositório tem 25 |
| **O que muda** | o aviso de tarifa na tela de Faturas (bundle novo), o `--editar` do `npm run identidade`, o contador no `npm run faturar`, e documento |
| **Ciclo** | `git pull` → **`prisma generate`** → `web:build` → `restart`. Sem migration desta vez, e o `generate` continua não sendo opcional — a guarda de arranque recusa subir sem ele |

**Os scripts não esperam deploy**: `npm run identidade -- --editar` e `npm run faturar` rodam do Codespace contra a `DATABASE_URL` de produção, pelo caminho da aplicação. O que espera deploy é o **aviso na tela**.

---

## 5. O que NÃO fazer em seguida

- **Não escreva o `src/sicoob/http.ts` antes da credencial de sandbox.** As três razões da `RESUMO-SESSAO-21` §5 não mudaram, e a §1 acima diz como obtê-la em minutos.
- **Não digite contrato para as três UCs do Edimar** antes da `Q-PARCERIA-01`. Elas estão `nao_ativado` e fora das 29, mas voltam a travar quando o CRM as ativar.
- **Não aplique migration sem o deploy em seguida.** A guarda de arranque recusa subir com tabela em `public` sem modelo no client, e um restart por qualquer motivo derruba o site.
- **Não confie em `401` como prova de deploy.** Ele prova que a rota existe e recusa credencial — não que ela funciona. Foi assim que a aba Documento passou quebrada por um deploy inteiro em 30/07.
- **Não emita o lote sem olhar o aviso de tarifa da concessionária.** Ele não trava nada de propósito — e é a única coisa entre uma fatura de valor certo e uma de valor menor que ninguém percebe.

---

## 6. Antes de abrir o portal, leia a folha

**`SICOOB-portal-2026-08-06.md`** é para ficar aberta ao lado do navegador. Ela traz os nove itens da §1 com **o consumidor de cada um aqui dentro** e o que muda conforme a resposta — que é a parte que não dá para reconstruir depois olhando a anotação. Traz também o §0, *o que já está resolvido*, para a passada não gastar pergunta com o que já foi decidido.
