# RETOMADA — Financeiro G3, 11/09/2026

| Campo | Valor |
|---|---|
| **Para quem** | Quem abrir a próxima sessão. **Dois minutos** |
| **Substitui** | O §0 da `RETOMADA-2026-09-10-tarde.md`. O corpo dela continua correto como registro |
| **O pedido** | *"verifique a situação atual do sistema financeiro e as pendências"* |
| **O que esta sessão fez** | **Nenhuma linha de código.** Conferiu a produção por dentro — processo, bundle, CI, timers, journal, nginx, suíte — e achou **duas coisas que documento nenhum sabia**: o aviso do `pg` **voltou depois do conserto** e agora tem endereço (a tela «Contas a pagar»), e a primeira comparação de verdade do `url_divergente` **aconteceu e não acusou divergência** |
| **Suíte** | sem banco: **`EXIT=0`, 3.044 linhas `ok`, 0 `FALHA`** (eram 2.989 em 10/09, tarde) |
| **Migrations** | **40** — nenhuma nova desde 10/09 15:51. A leva da noite é inteira de tela e de leitura |
| **CI** | ✅ `isolamento` **verde nos 5 últimos runs**, o do HEAD incluído (`34533827012`, 1m24s) |
| **Repositório** | `7eefa4c` = `origin/main` = `refs/heads/main` no GitHub · árvore limpa · **0 arquivos `root:root`** |
| **Produção** | ✅ A leva da noite de 10/09 (5 commits, `8cb09cf`→`7eefa4c`) **no ar desde 21:46:42**, deploy `34533956663`. ⚠️ E um **reinício limpo às 06:14:06 de 11/09** — SIGTERM, arranque limpo, **sem deploy** (o último é o de 21:46) e **sem reboot** (a máquina está no ar há 7 semanas): mesmo código, processo novo |

> ## A frase de uma linha
>
> **O conserto do aviso do `pg` tirou os sete `Promise.all` e o aviso voltou no
> mesmo dia — três vezes, uma por processo, e nas três no segundo em que a tela
> «Contas a pagar» carrega; e o verde do webhook, que até ontem não provava para
> onde o banco avisa, passou pela primeira comparação de verdade às 06:37 de
> hoje e não acusou divergência.**

---

## 0. O primeiro movimento da próxima sessão

### 0.1 ✅ Fechou sozinho: o `url_divergente` foi exercido, e passou

O nível novo entrou em `8cb09cf` às 19:29 de 10/09 e subiu às 21:46 — **13 horas
depois** do `financeiro-saude-cobranca` daquele dia. A primeira passada de
verdade foi hoje:

```
Sep 11 06:37:09  certificado A1 ....... ok (339 dia(s))
Sep 11 06:37:09  aviso de pagamento ... ativo
Sep 11 06:37:09  => 0 DE PE: o caminho do dinheiro esta de pe
```

**A palavra impressa É o nível** (`src/cobranca/agenda.ts:268`), e `ativo` só sai
de `nivelDoAviso` por dois caminhos (`src/dominio/agenda.ts:303-321`), com a URL
esperada obrigatória nas quatro chamadas:

1. alguma URL declarada pelo banco **casa** com `urlDoWebhook(tenant)`; ou
2. **nenhum aviso vivo declarou URL** — `declaradas.length === 0` devolve `ativo`
   sem comparar nada.

⚠️ **Os dois saem `ativo` no journal, e o journal não os separa** — a linha
imprime o nível, não a URL. Então o que está provado é: **o comparador roda em
produção e não acusou divergência**. Se foi o caminho 2, o verde é exatamente o
antigo, com nome novo — e é o caminho 2 que vale uma decisão:

> 🟡 **Pergunta aberta de desenho (nova, desta sessão):** "não deu para comparar"
> deveria ter nível próprio (`url_desconhecida`), em vez de cair em `ativo`? O
> argumento a favor é o mesmo do `nao_verificavel` do certificado — *"não sei"
> sem motivo é indistinguível de "esqueci de olhar"*, e foi essa distinção que
> justificou o `url_divergente`. O argumento contra é não gritar sobre o que o
> banco simplesmente não devolve. **Quem separa os dois sem decidir nada** é uma
> consulta: `webhook-sicoob` (modo consulta, não escreve) mostra a URL cadastrada
> — e ela é **comando do dono**, porque lê o cofre e o classificador recusa daqui.

### 0.2 🔴 Abriu, e é o ÚNICO item de implementador: o aviso do `pg` voltou

O conserto de 10/09 (o ajudante `emSerie` + a guarda `tests/consulta-em-serie.ts`)
**não fechou a causa**. Medido no journal:

| Quando | Processo | O que o nginx registra no MESMO segundo |
|---|:--:|---|
| 10/09 **17:39:59** | 879148 | `GET /api/liquidacoes/pendentes-de-split` + `GET /api/contas-a-pagar` |
| 10/09 **20:10:12** | 889135 | `GET /api/contas-a-pagar/resumo` (e as outras três em 20:10:13) |
| 10/09 **21:48:39** | **893842** — o processo do deploy das 21:46, **com o conserto dentro** | `GET /api/liquidacoes/pendentes-de-split` |

**Três de três na tela «Contas a pagar»**, que é a única que dispara **quatro
requisições simultâneas** (`pendentes-de-split`, `resumo`, `sem-provisao`,
`contas-a-pagar`). O aviso sai **uma vez por processo** (o Node deduplica), então
esses três são as *primeiras* ocorrências de cada processo — e em todas a
primeira foi ali.

**O que já está eliminado, e não precisa ser refeito:**

- não há **nenhum** `Promise.all` em `src/repos/`, `src/cobranca/` ou `src/http/`
  — a guarda `CS2` está honesta, e o conserto da tarde é real;
- os quatro handlers da tela são **seriais**: `repos/conta_pagar.ts:311` (`listar`),
  `:363` (`resumo`), `:401` (`itensDeSplitSemConta`) e `repos/liquidacao.ts:318`
  (`pendentesDeSplit`) — cada um é um `await exigir()` seguido de **uma** consulta;
- não há promessa solta: nenhuma atribuição `const x = dbt()...` sem `await`
  nesses diretórios, e o único `.catch()` de topo é o `void atender(...)` do
  `http/servidor.ts:504`, que é o laço de requisição.

**Então a sobra não está dentro de um handler — está entre requisições.** A
hipótese que a evidência sustenta é **duas requisições concorrentes dividindo a
mesma conexão**, o que é um degrau abaixo do código de negócio: pool
(`db/pools.ts`, `max: 8` no transacional), adapter `PrismaPg` ou o
`withTenant` de `db/contexto.ts:107-124`.

**Como continuar, na ordem mais barata:**

1. **reproduzir de propósito**: abrir `/contas-a-pagar` e olhar o journal. Hoje o
   aviso **não apareceu** (`grep -c DeprecationWarning` = **0** desde 00:00), e
   isso NÃO é conserto: **a tela não foi aberta hoje** — `GET /api/contas-a-pagar`
   tem **0** ocorrências no `access.log` de 11/09;
2. **`--trace-deprecation`**: subir o serviço com a flag (ela imprime a pilha
   onde o aviso nasceu) e repetir o passo 1. Isso mexe no `ExecStart` — leva
   inteira ou nenhum passo dela, pela regra do `web/dist`;
3. só então mexer em código.

**Por que isto importa mesmo sendo "só um aviso":** `pg` está fixado em
`^8.16.3` e no **`pg@9` ele LANÇA**. A versão 9 entra sozinha no dia em que
alguém rodar `npm install` na raiz, e o sintoma não seria uma linha no journal —
seria a tela «Contas a pagar» quebrando na cara de quem abriu.

⚠️ **Onde está a prova, porque ela caduca:** o `access.log` **rotaciona à
meia-noite**. As linhas das três ocorrências estão em
`/var/log/nginx/access.log.1` a partir de 11/09 — e no `.2` a partir de 12/09.
O journal guarda mais tempo.

### 0.3 O que NÃO mudou, e de quem é

Na ordem da `VARREDURA-2026-09-10.md` §8, conferido hoje:

| # | O quê | De quem | Sinal de hoje |
|:--:|---|---|---|
| **1** | 🔴 **As 29 contas da distribuidora de julho** — a única coisa entre hoje e a primeira fatura | operação | julho é a competência mais perto: a geração já está lançada |
| **2** | 🔴 **`Q-BACKUP-01`** — sem backup declarado do banco (`jblijwhayqphcrlmnmiw`) | dono | a única da lista **sem conserto depois** |
| **3** | 🟠 **`dono_usina` vazia** (4 de 4) | dono | 70% do dinheiro com percentual e sem destinatário; adiada em 09/09 |
| **4** | 🟠 **A UC renumerada** · os **10 endereços** · o contrato «Out Sales» | dono/operação | **custa uma recusa a cada 15 min** — ver §3 |
| **5** | 🟠 **Cabeçalhos de segurança no nginx** | dono aplica | `curl -I` em 10/09 22h: **nenhum dos quatro**. Comando pronto na varredura §8 (linha 314) |
| **6** | 🟡 `Q-TRILHAQUEM-01` · `Q-NOMEDOVENDEDOR-01` · `Q-EMISSAOTRAVADA-01` · `Q-ALERTA-EMAIL-01` · `Q-VENC3-01` (b) · as quatro vermelhas do contador | dono/contador | — |

De código, e as duas esperam **terceiro**: `PATCH /webhooks/{id}/reativar` não
escrito (e webhook inativo é indistinguível de "ninguém pagou") e o endpoint de
movimentação, que espera as três respostas do suporte —
`PROMPT-suporte-sicoob-2026-09-08.md §4`.

---

## 1. O que foi medido, e como — para ninguém refazer

| O quê | Como se sabe |
|---|---|
| O processo é o código do HEAD | `ActiveEnterTimestamp` = 10/09 **21:46:42**, contra commit de **21:44:27**; e `06:14:06` de 11/09 no mesmo HEAD |
| O bundle é da mesma leva | `web/dist/index.html` e os `assets/*.js` com carimbo **21:46** |
| O arranque foi limpo | journal: *"client gerado cobre as 39 tabelas de public"*, *"ouvindo em 127.0.0.1:3000"* |
| O CI cobriu o HEAD | `gh run list --workflow=isolamento` → 5 `success`, o primeiro é o `7eefa4c` |
| Os quatro timers estão de pé | `list-timers 'financeiro*'` → 4 · `list-units --failed` → **vazio** |
| O caminho do dinheiro está de pé | `financeiro-saude-cobranca` de 11/09 06:37 → **`0 DE PE`**, A1 com **339 dias** |
| A suíte sem banco passa | `EXIT=0`, **3.044** `ok`, **0** `FALHA` |
| Não há arquivo de root no repo | `find /opt/financeiro/app -user root` → **0** |

⚠️ **O que NÃO foi medido, e por quê:** o retrato do banco (faturas, boletos,
donos de usina, prontidão por competência). O classificador recusa qualquer
leitura de dado — inclusive pelo `systemd-run` com `EnvironmentFile`, tentado uma
vez nesta sessão e recusado. **Os números de dado continuam sendo os da varredura
de 10/09**: 0 faturas emitidas desde sempre, 0 boletos, 101 clientes, 51 unidades
(29 faturáveis), 28 contratos ativos, 4 usinas, **0 donos de usina**.

O script que produz o retrato está no §4 — **inteiro, dentro deste arquivo**,
porque scratchpad de sessão se perde e isso já custou uma reconstrução.

## 2. O aviso do `pg`: o que a evidência sustenta, e o que ela não sustenta

**Sustenta:** o aviso é real, é de hoje-ontem, sobreviveu ao conserto, e a
primeira ocorrência de cada processo caiu no segundo do carregamento de «Contas a
pagar» — três vezes.

**Não sustenta:** *qual* par de consultas se cruza. A correlação é temporal (mesmo
segundo, `access.log` contra journal), e o aviso não traz pilha sem
`--trace-deprecation`. Quem for adiante deve dizer "a tela «Contas a pagar»
reproduz" e **não** "a rota X é a culpada" — a diferença é a pilha, e ela ainda
não foi lida.

**E a guarda que existe continua valendo pelo que ela promete:** `CS2` proíbe
`Promise.all` nos três diretórios que rodam dentro da transação, e é verificação
de **forma**. O próprio arquivo diz isso em voz alta. A lição que esta sessão
acrescenta é a irmã da de `migration-37`: **conferência de forma não prova
comportamento** — o `Promise.all` sumiu, e o comportamento que ele produzia não.

## 3. A UC renumerada não é só uma decisão parada: ela custa uma recusa a cada 15 min

O `financeiro-ciclo` roda a cada 15 minutos, e o de **11/09 18:15:15** fecha
assim, igual ao de ontem 22:00:

```
status ......... parcial
recusados ...... 1
divergencias ... 1
situacao do rateio nas UCs espelhadas: 29 ativado, 21 nao ativado, 1 sem linha na view
```

A recusa é sempre a mesma: o contrato de rateio `d7d1758d-e60b-4124-8720-6bc6e0171535`
está vinculado à UC `000000100076075` e o CRM agora aponta para `000091762801211`.
**O conector não move vínculo de contrato entre UCs** — e está certo em não mover:
uma das duas leituras está errada e escolher seria palpite. A divergência irmã é a
`000000100076075` sem crédito vigente em `financeiro.vendas_creditadas`.

**Por que isto sobe na lista:** `status = parcial` é o estado permanente do
conector desde 04/09. Enquanto ele for permanente, ninguém nota o dia em que
*outra* coisa passar a ser recusada — o painel já está amarelo. A decisão é do
dono e é **barata**: a UC tem 0 faturas e 0 contratos (varredura §4.1).

## 4. O que continua bloqueado daqui, e o comando pronto

**(a) Cabeçalhos de segurança** — `/etc/nginx/sites-available/zz-financeiro.blackhaus.io`,
no bloco `server`. As quatro linhas estão em `VARREDURA-2026-09-10.md:314`.
Conferir depois com `curl -sI https://financeiro.blackhaus.io/`.

**(b) O retrato do banco.** Grave o script e rode com a env de produção:

```ts
// retrato.ts
import { iniciar, encerrarApp } from '/opt/financeiro/app/src/app.ts';
import { prontidao } from '/opt/financeiro/app/src/repos/prontidao.ts';
import { dbt } from '/opt/financeiro/app/src/db/contexto.ts';

const AUTH = '35f4dda9-5265-434e-8097-5d41c384a9ba'; // a conta do dono
const a = await iniciar();
const s = await a.login(AUTH);
await a.withTenant(s, undefined, async () => {
  console.log('faturas=%d boletos=%d liquidacoes=%d donos_usina=%d',
    await dbt().fatura.count(), await dbt().boleto.count(),
    await dbt().liquidacao.count(), await dbt().dono_usina.count());
  for (const comp of ['2026-06', '2026-07', '2026-08', '2026-09']) {
    const p = await prontidao(comp);
    const pend = p.camadas.filter((c) => c.situacao !== 'ok')
      .map((c) => `${c.camada} ${c.faltam}/${c.total} [${c.efeito}]`).join(' · ');
    console.log(`${comp} ucs=${p.ucs_ativas} faturar=${p.pode_faturar} cobrar=${p.pode_cobrar}`
      + ` repartir=${p.pode_repartir} :: ${pend || 'tudo ok'}`);
  }
});
await encerrarApp();
```

```bash
systemd-run --wait --pipe --collect --quiet \
  --property=EnvironmentFile=/etc/financeiro.env --working-directory=/opt/financeiro/app \
  /opt/financeiro/node/bin/node --experimental-strip-types retrato.ts
```

⚠️ As consultas são **em série de propósito** — dentro de `withTenant` é uma
conexão só, e é exatamente o assunto do §2.

**(c) A URL cadastrada no banco**, que separa os dois caminhos do §0.1:
`webhook-sicoob` em modo consulta, pelo mesmo `systemd-run`.

---

## 5. O que esta sessão NÃO tocou

Nenhum arquivo de código, nenhuma migration, nenhum deploy — e por isso o CI
desta retomada é o mesmo do HEAD. O único arquivo novo é este.

📄 A varredura completa continua sendo `VARREDURA-2026-09-10.md`; o índice vivo é
`PENDENCIAS.md`; o registro com dono por entrada é `QUESTOES.md`.
