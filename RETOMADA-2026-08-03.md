# RETOMADA — 03/08/2026, fim da sessão 18

> **Leia este primeiro, inteiro. Dois minutos, e diz onde tudo parou.** Ele não substitui os resumos: diz **o que ficou commitado**, **o que falta publicar** e **qual é a próxima ação**. Se algo aqui divergir de outro documento, o outro é mais antigo.
>
> Substitui o `RETOMADA-2026-07-30.md`, que era da sessão 17.

---

## 1. Em uma tela

| | |
|---|---|
| **Branch** | `sessao-14-cobranca-e-documento` · **9 commits à frente de `main`** e **6 à frente do próprio remoto** — a branch existe em `origin` mas não recebe push desde 30/07, e nunca foi mesclada em `main` |
| **O que esta sessão fez** | As **quatro perguntas** que travavam trabalho caro foram feitas ao dono e respondidas. Três viraram construção no mesmo dia; a quarta virou pedido ao dev do CRM |
| **Commitado** | ✅ tudo. Árvore limpa |
| **Produção** | **inalterada.** Nada foi escrito no banco (só `SELECT` e um `--ensaio` com rollback conferido). Continua com **21 migrations** e o bundle `index-BPNacouw.js` |
| **Suíte** | `EXIT=0`, **1106** verificações em 43 suítes. Catálogo **9/9** contra banco de teste |

---

## 2. O que falta publicar, e agora tem migration

**Duas coisas pendentes, e a segunda muda o procedimento:**

1. **`3fd7b22`** — o conserto da sessão expirada, pendente desde 30/07. Sem migration.
2. **A sessão 18 inteira** — e ela traz a **migration 22** (`contas_a_pagar`).

```bash
cd /opt/financeiro/app
sudo -u financeiro git pull

# 1. O SCHEMA. Transacional POR MIGRATION.
sudo -u financeiro env PATH=/opt/financeiro/node/bin:$PATH npx prisma migrate deploy

# 2. O CLIENT. Sem ele o servidor RECUSA SUBIR - guarda de arranque da sessao 16.
sudo -u financeiro env PATH=/opt/financeiro/node/bin:$PATH npx prisma generate

sudo -u financeiro env PATH=/opt/financeiro/node/bin:$PATH npm run web:build
systemctl restart financeiro
```

Conferir depois, e é leitura pura:

```bash
psql "$DIRECT_URL" -f tests/catalogo.sql     # tem de dizer "9 invariantes, nenhuma falha"
```

**A migration 22 pode ser aplicada de fora**, pelo `DIRECT_URL`, como as 21 anteriores — foi o que se fez com a 21 em 30/07. O `web:build` e o `restart` são do dono.

---

## 3. O que a sessão encontrou, em ordem de consequência

### 3.1 🔴→✅ O conector apagava o vencimento antes de alguém preencher

`src/crm/sincronizacao.ts` levava `data_vencimento` no objeto `espelho`, e o CRM a tem **vazia em 41 de 41** linhas. **Preencher as 39 UCs e rodar `npm run ciclo` apagaria as 39** — sem erro, sem log e sem recusa. O sintoma chegaria um passo adiante, apontando para o lugar errado.

Consertado pela **R25 da `SPEC-002`**: campo local, o usuário vence (R5), divergência vira sinal. O `N55` **falha contra o código anterior**.

**Isto foi encontrado ao construir o importador que o dono pediu — não por auditoria.**

### 3.2 🔴→✅ O split apurava e ninguém quitava

Fechada a `Q-PAGAMENTO-01` na opção que o dono escolheu: `conta_pagar` completa. **Migration 22**, e ela entrou **enquanto `split_item` ainda tinha 0 linhas** — a janela não chegou a fechar.

### 3.3 🔴 Duas vermelhas novas, e as duas apareceram na mesma consulta

- **`Q-CLIENTEDUP-01`** — **84 linhas de `cliente` para 41 pessoas.** O faturamento funciona; o que quebra é digitar CPF, que colide com o índice único **no meio da digitação**.
- **`Q-PAGADOR-01`** — o boleto sairia com pagador **sem CPF e sem endereço**, e o código manda `?? ''` em vez de recusar.

### 3.4 🔴 O eixo do originador — perguntado, e o dev devolveu outra pergunta

O `PROMPT-dev-crm-rodada5` foi enviado e **respondido no mesmo dia**. O dev não respondeu o eixo: perguntou **de onde saiu a lista de 39**, supondo planilha da operação com contratos ainda não ativados.

**A suposição não se sustenta, e foi medida:** as 39 têm `crm_usina_cliente_id` preenchido em **39 de 39** — são o `contrato_id` do próprio CRM, lidos pelo conector em 28/07. Nada nosso é digitado.

Resposta escrita em **`RESPOSTA-dev-crm-rodada5-2026-08-03.md`**, com as 41 marcadas UC a UC. **A pergunta do eixo segue sem resposta**, e continua sendo a que trava a digitação.

### 3.5 🔴 `Q-UCMUDOU-01` — uma atribuição nossa está errada AGORA

Achado ao responder o dev. O contrato `524a4866-…` **mudou de UC** no CRM: espelhamos apontando para `000041446801282` (Fernando Albino) e hoje ele aponta para `000307301401201`. E a `000041446801282` passou a ser de **outro** contrato — **Renata Lucy**.

**Digitar contrato por essa linha creditaria comissão à pessoa errada**, sem erro e sem log. O conector **não corrige sozinho**, por decisão (R23): mover vínculo entre UCs seria escolher qual leitura vale. Provado por `--ensaio`: `recusados 1`, com as duas UCs nomeadas.

### 3.6 🟡 Três coisas que as views não expõem

`troca_titularidade` vem **NULL em 41 de 41**; a usina `407706301217` que o dev cita não está entre os 4 códigos de `financeiro.usinas`; e **não há coluna de situação** do contrato de rateio. É por isso que a conta dele dá 44 e a minha dá 41 — e o pedido está na §4 da resposta.

---

## 4. A próxima ação

**Três, e nenhuma é código.**

| # | O quê | Por que agora |
|:--:|---|---|
| 1 | **Enviar a `RESPOSTA-dev-crm-rodada5` ao dev** | ela devolve UMA pergunta que trava tudo (`Q-UCMUDOU-01` §3) e pede duas colunas nas views. O eixo do originador **continua sem resposta** |
| 2 | **Preencher o dia de vencimento das 39 UCs** | o importador existe e o conector não apaga mais. Fazer isto **antes** dos contratos é o que evita a segunda passada |
| 3 | **Aplicar a migration 22 e publicar** | ver §2 |

O passo 2, em três comandos:

```bash
npm run vencimentos -- --modelo --auth-user <uuid> --saida vencimentos.csv
# preencher a coluna dia_vencimento no Excel
npm run vencimentos -- --ensaio  --auth-user <uuid> --arquivo vencimentos.csv
npm run vencimentos -- --valendo --auth-user <uuid> --arquivo vencimentos.csv
```

**O trabalho principal não mudou:** continua sendo o caminho para a primeira fatura, `RESUMO-SESSAO-16.md` §5.5.

---

## 5. O que NÃO fazer na próxima sessão

**Não continue a F4 pelas nove entidades que faltam.** O que entrou nesta sessão foi a fatia que tinha **prazo** — a que impede pagar duas vezes o mesmo repasse. As nove restantes (fluxo de caixa, conciliação, cartão, fornecedor, compra) são DRE, e as decisões delas se tomam melhor olhando o primeiro lote real. Continua valendo o `RETOMADA-2026-07-30.md` §5, e o critério da F2 do `PRD` §10 continua não fechado — falta o A1.

**Não digite contrato antes da resposta do dev do CRM.** A R20-b congela o tier no `rascunhar` e `contrato` não tem edição.

---

## 6. Estado medido de produção — 03/08

Só leitura, pelo `DIRECT_URL`.

| | | | |
|---|--:|---|--:|
| clientes (linhas) | 84 | **pessoas distintas** | **41** |
| UCs (todas ativas, com rateio) | 39 | **com `data_vencimento`** | **0** |
| usinas | 4 | **contratos** | **0** |
| tarifa vigente | 1 | **originadores** | **0** |
| regras de comissão | 10 | **donos de usina** | **0** |
| **faturas · boletos · liquidações** | **0** | **`split_execucao` · `split_item`** | **0** |
| migrations aplicadas | **21** | no repositório | **22** |

---

## 7. Onde ler o resto

| Documento | Para quê |
|---|---|
| **`RESUMO-SESSAO-18.md`** | a sessão inteira. A §5 lista **seis erros meus** e o que cada um ensinou |
| **`RESPOSTA-dev-crm-rodada5-2026-08-03.md`** | **a enviar.** As 41 UCs marcadas uma a uma, a diferença 39→41 explicada nas duas causas, e o pedido de duas colunas nas views |
| `PROMPT-dev-crm-rodada5-2026-08-03.md` | o pedido original, já respondido |
| `QUESTOES.md` §5 e §7 | as questões novas e as duas fechadas, com o corpo completo |
| `RESUMO-SESSAO-16.md` §5.5 | o caminho para a primeira fatura — **continua sendo o trabalho principal** |
| `ROTEIRO-REVISAO.md` | o que abrir no navegador. A **parte 5 (ler o QR com a câmera)** continua sendo o teste que nenhum comando substitui |
